# MCP 工具权限审批（HITL）· 交付交接事项

> 生成于 mcp-hitl 实现完成之际（PR #23，2026-09-08）。
> 三件需要人来处理的事：一个待开的工程 ticket、一份真机验收清单、一条上线提醒。

## 1. 待开 ticket：`TestSourceSetConventionsTest` 在全新 clone 下必失败

**问题**：`backend/src/test/java/com/portfolio/invest/architecture/TestSourceSetConventionsTest.java:83` 对 `build/classes/java/integrationTest` 做 ArchUnit 检查，但 `test` 任务不编译该源集（integrationTest 是独立 JvmTestSuite，见 `backend/build.gradle` testing 块）。失败场景：全新 clone 后 `cd backend && ./gradlew clean test` 必失败（empty should）。

**归属**：与本特性无关的既有缺陷（引入于 `0fe0ed0`，早于 mcp-hitl 分支）；本机增量开发时因 integrationTest 类已存在而不触发，故 CI/日常开发未暴露。

**建议修复方向**（开 issue 后实施）：让该测试自行保证输入存在（改挂到同时编译两源集的任务，或检查前触发 `integrationTestClasses`），而非依赖外部构建顺序。

## 2. 真机验收清单（自动化/mock 层钉不住，需浏览器人工确认）

以下行为属库内部状态或真实运行时，单测与集成测试均不可观测，合并后请在浏览器走一遍：

- [ ] **线程切换时审批卡片的消失/保留**：`useInterrupt` 的 pending 状态归属 agent/thread 的真实行为——切会话后旧线程的待审卡片是否消失。规格评审时以此「以实测为准」推迟；实测结果请回填到设计规格 §三 ThreadArea 集成一节。
- [ ] **FR-8 场景 A（刷新后同线程发消息）**：页面刷新丢卡后，在同线程发普通消息 → 应看到中文横幅「当前对话有未完成的审批（可能因页面刷新或服务重启失效），请开启新对话继续。」而非英文契约错误。
- [ ] **FR-8 场景 B（服务端重启后点残留卡片）**：后端重启后前端卡片仍在，点「批准」→ 同样应看到上述中文引导。
- [ ] **审批后卡片时序**：点批准/拒绝后至续跑启动的间隙卡片是否即时消失；重复点击理论无害（库对已处理 interruptId 幂等），如实际观察到困扰再考虑消费侧禁用（终审 Minor，已裁决不做）。

## 3. 上线提醒：打扰面与治理路径

- **全量弹审批是知情选择**（需求决策 1）：当前 MCP 数据源工具大多未标 `readOnlyHint`，本特性上线后其调用将**全部**弹审批，直到治理措施落地。若打扰面不可接受，止血手段是 git revert 本特性（无开关后门，ADR-0010）。
- **治理已列入非目标待立项**：`ConfirmResult.rules` 规则沉淀（「本次会话不再询问」）、provider/endpoint 级只读信任配置、刷新/重启后的中断恢复（见需求规格「非目标」与 ADR-0010「后续治理」）。
- **观察点**：拒绝后 LLM 重试导致的反复弹卡（FR-7 提示词约束收敛，无机械防护）——线上如高频出现，优先考虑治理立项而非补丁。
