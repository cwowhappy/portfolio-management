# 文档体系全面更新 · 实施计划（2026-09-12）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把项目文档从"落后一代"拉回与代码完全一致——修正全部事实性偏差、补齐 4 个已交付特性 + 7 个域的缺失文档、按"现行/历史分离"重组 docs/ 目录。

**Architecture:** 纯文档变更（markdown only）。四阶段：Phase 0 结构移动（git mv，独立 commit）→ Phase 1 现有文档纠错（Batch A–G）→ Phase 2 新增文档（modules 05–13 + ADR-0011 + vendored 说明）→ Phase 3 导航收敛与链接门禁验证。单分支 `docs/revamp-2026-09` 单 PR，按任务分 commit。

**Tech Stack:** Markdown；`git mv` 保历史；验证用临时 python 链接检查脚本（不入库，见附录）。

**Spec:** 本文件即 spec。所有"实际值"来自 2026-09-12 四路并行子代理对照真实代码的全量审计，均附代码证据。

## 全局约束

- **纯文档**：不改任何代码/配置行为——`.env.example`、`application.yml`、源码、构建脚本一律不动（用户已裁决：collector/.env.example 库名对齐**不做**）。
- **落笔前核对**：所有事实性声明（类名/路径/端点/版本号/数字）必须 Read 真实代码或配置核对后再落笔，禁止凭记忆或只凭本计划转述。本计划的"实际值"是审计时点快照——执行时若发现不符，以代码为准，并在任务报告中说明差异。
- 中文文档，风格对齐现有文档：表格化、简洁、篇首「对应代码」真实路径标注。
- 链接一律相对路径；不新增入库的工具/脚本。
- commit 按仓库惯例 `type(scope): 中文描述`；全部完成后单 PR 合入 main。
- 用户已裁决的三项：4 特性以 **MS-16~MS-19**（标已交付+日期+PR）追加进产品落地计划；**单 PR 分 commit**；**不做**任何代码文件修改。

## 审计事实基线（各任务对照用，执行时仍须重验）

| 主题 | 过时口径 | 实际（证据） |
|---|---|---|
| Agent 数据工具 | 6 个 | **7 个 @Tool**：search_stock / get_quote / get_kline / get_financials / get_news / get_market_overview / get_valuation（`backend/src/main/java/com/portfolio/invest/agent/InvestTools.java`，@Tool 在 44/54/64/91/115/126/144 行） |
| AgentScope | 2.0.1，3 构件 | **2.0.3，4 构件**（core / extensions-model-openai / agui-spring-boot-starter / **agentscope-harness**）`backend/build.gradle:72-79` |
| Flyway | V1–V9 | **V1–V11**（V10__mcp.sql、V11__skill.sql）`backend/src/main/resources/db/migration/` |
| ADR | 0001–0009 | **0001–0010**（0010-mcp-hitl-permission，已接受 2026-09-08） |
| @RestController | 5 个 | **13 个**（Auth/UserAdmin/Conversation/Market/Health + Valuation/Portfolio/Allocation/Screening/Journal/McpConfig/SkillConfig + InvestAguiRuntimeContextResolver）`backend/.../web/` |
| DDD 域 | 3 个（user/conversation/market） | **10 个**（+valuation/portfolio/allocation/screening/journal/mcp/skill） |
| application/ 子包 | 4 个 | **11 个**（+cache/journal/mcp/portfolio/screening/skill/valuation/allocation） |
| agent/ 包内容 | 3 类 | +**chart/（ChartSpec+ChartSpecs）、HarnessAgentFactory、UserToolkitFactory、McpClientPool、CurrentUserHolder、AguiEventNonNullCodec、AguiWirejsonConfig** |
| Agent 装配 | `Agent investAgent(Model, InvestTools)` + ReActAgent.builder().maxIters(10) | **AgentConfig 注册 AguiAgentRegistryCustomizer → 按请求经 CurrentUserHolder 取 userId → HarnessAgentFactory 构建 HarnessAgent**（workspace/stateStore/compaction/memory/skillRepository/skillFilter），**无 maxIters**（`AgentConfig.java:39-51`、`HarnessAgentFactory.java:38-60`） |
| 会话内存 | server-side-memory: false，前端全量历史 | **true**——HarnessAgent 的 JsonFileAgentStateStore 服务端持久化会话历史，前端只发最新一条（`application.yml:45-46`、`HarnessAgentFactory.java:50`）。**该机制变更需 ADR-0011 承接，0008 标注 superseded** |
| get_kline limit | 默认 60 / 描述上限 120 | **默认 120 / 最大 500**（`InvestTools.java:72,76`） |
| ArchUnit | 1 类（PackageConventionsTest） | **3 类**：PackageConventionsTest（13 条）/ CodingConventionsTest（5 条）/ TestSourceSetConventionsTest（3 条），合计 **21 条** |
| 后端测试规模 | test 62 类（51 单元/8 切片/3 ArchUnit）、integrationTest 18 类 | **test 78 类（68 单元/7 切片/3 ArchUnit）、integrationTest 23 类**（执行时重测） |
| BDD | 5 feature | **10 feature 13 场景**（`backend/src/bdd/resources/features/`） |
| MCP_SECRET_KEY | README 环境变量表在列 | **代码中不存在**（token 走明文 `auth_secret_enc` 直读，`agent/UserToolkitFactory.java:36-37`）——README 须改标注"二期规划，未实现" |
| collector 国债源 | bond_zh_us_rate | **bond_china_yield**（2026-09-06 起，`collector/collector/sources/plugins.py:156-159`） |
| make collect | "运行一次采集" | **列任务**（`python -m collector.cli list`）；跑一次是 **make collect-run**；另有 collect-backfill（根 Makefile:74-83） |
| /api/agent/health | "LLM 配置+行情连通+延迟" | **纯 liveness 仅 LLM 配置、零外呼**；行情连通/延迟在 **/api/agent/status**（30s 缓存）（`HealthController.java:40-59`） |
| 前端 e2e | 导航/鉴权/对话/会话/行情/估值/持仓/管理/错误态 | **15 spec / 34 用例**（+allocation/screener/industry/journal/hitl/chat-chart）`frontend/e2e/` |
| 前端 lint 强制 | 4 条 | **5 条**（+`no-restricted-imports` 禁 `echarts` 全量入口，唯一注册点 `frontend/lib/echarts-setup.ts`，`eslint.config.mjs:63-80`） |
| 前端单测统计 | 320 用例/41 文件（2026-08-30 口径） | **~763 用例/71 文件**（执行时实测为准） |
| 页面路由 | 2 页（产品概览口径） | **13 路由**：`/` `/admin` `/allocation` `/industry` `/journal` `/login` `/market` `/portfolio` `/register` `/screener` `/settings/mcp` `/settings/skills` `/valuation` |
| collector 集成 schema 回放 | V3/V4 | **V3/V4/V7，共 12 表**（`collector/tests/conftest.py:27-29`） |
| collector 幂等用例 | 6/6 表（valuation_snapshot 单独） | test_writer_idempotency.py 已含 **6 个幂等用例**（+index_constituent 重跑替换） |

**4 个已交付但文档零覆盖的特性**（PR 号与日期执行时用 `git log` 复核）：

| 特性 | PR | 合并日期 | 要点 |
|---|---|---|---|
| mcp-integration | #19 等 | 2026-09-07 | 用户级 MCP server 配置/连接测试/工具发现；V10 三表；6 端点 |
| skill-integration | #20 | 2026-09-08 | 内置 tushare_data/wind_finance 两个 SKILL.md；用户级启停 |
| mcp-hitl | #22/#23，修复 #28/#30 | 2026-09-08~09-12 | MCP 写工具触发中断审批卡、多卡逐张应答；ADR-0010 |
| chat-rich-content | #29 | 2026-09-12 | 4 工具图表双通道（ChartSpec 契约）、ECharts 全站替换 recharts、DataTable |

另有 #17（部署方案 09-05）、#31（Descartes PIT 09-12）进产品落地计划变更记录。

---

## Task 1: 分支、计划落盘、结构移动与断链修复

**Files:**
- Create: `features/plans/2026-09-12-文档体系全面更新.md`（本文件）
- Create: `docs/archive/README.md`
- Move: `docs/plans/2026-08-18-一期实施计划.md`、`docs/plans/2026-08-18-投资分析AI-Agent一期设计.md`、`docs/plans/2026-08-19-前端迁移CopilotKit.md`、`docs/plans/2026-08-19-skill评估-test-driven-development.md` → `docs/archive/`
- Move: `docs/code-review-lessons.md` → `docs/reviews/code-review-lessons.md`
- Modify: `README.md`（仅 :5 架构入口链接）、`docs/README.md`（导航表）、`AGENTS.md`（仅 lessons 路径）、`features/README.md`（登记本计划）、`features/mcp-hitl/03-plan/实施计划.md:1040`（断链）

**说明：** 本任务只做**路径层面**的变更（移动+引用改指+断链修复），不做内容纠错——纠错归属 Task 2–8，避免同一文件被两个任务交叉改动。

- [ ] **Step 1:** 确认分支：`git branch --show-current` 应为 `docs/revamp-2026-09`（不存在则 `git checkout -b docs/revamp-2026-09`）
- [ ] **Step 2:** 本计划文件已在分支上（本任务执行时视为已完成）
- [ ] **Step 3:** `mkdir -p docs/archive && git mv docs/plans/2026-08-18-一期实施计划.md docs/plans/2026-08-18-投资分析AI-Agent一期设计.md docs/plans/2026-08-19-前端迁移CopilotKit.md docs/plans/2026-08-19-skill评估-test-driven-development.md docs/archive/`
- [ ] **Step 4:** `git mv docs/code-review-lessons.md docs/reviews/code-review-lessons.md`
- [ ] **Step 5:** 写 `docs/archive/README.md`：定位（历史归档，记录当时决策、不再随代码维护）、4 份文档清单与一句话说明、指路现行文档（architecture/01 系统架构、technology/README）
- [ ] **Step 6:** 更新引用（只改路径不改内容）：
  - 根 `README.md:5`「架构设计」链接 `docs/plans/2026-08-18-投资分析AI-Agent一期设计.md` → `docs/technology/architecture/01-系统架构.md`
  - `docs/README.md`：目录导航表中 plans/ 行改为只指产品落地计划；reviews/ 行补 lessons；新增 archive/ 行；删掉「plans/ 目录」「reviews/ 目录」两节中已移走文件的行（保留产品落地计划与 3 份报告行，报告路径不变）
  - `AGENTS.md:97` `docs/code-review-lessons.md` → `docs/reviews/code-review-lessons.md`
  - `features/README.md` 目录结构小节的 `plans/` 说明处登记本计划文件名
- [ ] **Step 7:** 修断链 `features/mcp-hitl/03-plan/实施计划.md:1040`：`../research/copilotkit/agui-hitl.md` → `../../../docs/technology/research/copilotkit/agui-hitl.md`
- [ ] **Step 8:** 残留引用扫描（预期仅 docs/archive/ 内部自引与 features/plans/ 本文件命中）：`grep -rn "2026-08-18-一期\|2026-08-18-投资分析\|2026-08-19-前端迁移\|2026-08-19-skill评估\|code-review-lessons" --include="*.md" . | grep -v "docs/archive\|features/plans\|docs/reviews/code-review-lessons.md"`，有命中则逐一改为新路径
- [ ] **Step 9:** 跑附录链接门禁脚本，预期**零断链**
- [ ] **Step 10:** `git add -A && git commit -m "docs(structure): 历史档归档 archive/，lessons 归位 reviews/，修断链与架构入口引用"`

### Task 2 (Batch A): 根与导航内容纠错——README.md、AGENTS.md、docs/README.md

**Files:**
- Modify: `README.md`、`AGENTS.md`、`docs/README.md`

**README.md 修复清单：**

| 位置 | 声称 | 改为（先核对） |
|---|---|---|
| :14 | 6 个数据工具 | 7 个数据工具 |
| :23 技术栈行 | AgentScope Java（未标版本可补） | AgentScope Java 2.0.3（含 harness）；前端补 echarts、TanStack Table |
| :53 | make test = 后端 + 前端 | 后端 + 前端 + collector（pytest） |
| :56-60 e2e 描述 | 导航/行情台/AI 对话/用户管理/会话持久化 | 对齐 15 spec 实际范围（+估值/持仓/配置/筛选/行业/日志/HITL/聊天图表/错误态），执行时 `ls frontend/e2e/` 复核 |
| :62-143 API 表 | 缺 MCP、journal、持仓细端点 | 补 **`/api/mcp/**` 6 端点**（GET /api/mcp/providers、GET /api/mcp/configs、PUT /api/mcp/configs/{providerId}、DELETE /api/mcp/configs/{providerId}、POST /api/mcp/providers/test、GET /api/mcp/configs/{providerId}/tools，需登录）+ **`/api/journal/**` 6 端点**（GET/POST /api/journal/entries、GET/PUT/DELETE /api/journal/entries/{entryId}、GET /api/journal/timeline，需登录）+ 持仓表补 9 端点（PUT/DELETE /api/portfolio/groups/{groupId}、DELETE /api/portfolio/positions/{positionId}、GET /positions/{positionId}/trades、PUT /positions/{positionId}/trades/{tradeId}、GET /positions/{positionId}/dividends、POST/GET /api/portfolio/cash-transactions）——逐一对照 `web/McpConfigController.java`、`web/JournalController.java`、`web/PortfolioController.java:57-130` |
| :148-156 目录结构 | web/application/domain 清单缺 journal/mcp/skill（application 另缺 cache）；agent 缺 7 新类；infrastructure 缺 cache；resources 缺 skills/ | 按 `backend/src/main/java/com/portfolio/invest/` 与 `src/main/resources/` 实际补全；db/migration 改 **(V1~V11)** |
| :159 前端目录描述 | 缺 journal 页与 settings 页 | 补「投资日志 / MCP与Skill设置页」 |
| :177 环境变量表 MCP_SECRET_KEY 行 | 现行变量口径 | 改为「**规划中（二期），代码尚未实现**——当前 token 为明文 `auth_secret_enc` 直读」或移入正文说明 |
| :179 MCP 数据源段 | 内容基本准确 | 保留，链接核对 |

**AGENTS.md 修复清单：**

| 位置 | 声称 | 改为 |
|---|---|---|
| :7,:21 | 6 个行情数据工具 | 7 个（补 get_valuation） |
| :9 | AgentScope Java 2.0.1 | 2.0.3（4 构件，含 agentscope-harness） |
| :56 web/ 职责 | 5 控制器 | 13 个 @RestController（列全） |
| :59 infrastructure | 4 子包 | +cache/（TtlCache+CacheConfig） |
| :60 agent/ | 3 类 | 补 chart/、HarnessAgentFactory、UserToolkitFactory、McpClientPool、CurrentUserHolder、AguiEventNonNullCodec、AguiWirejsonConfig 及一句话职责 |
| :63 | 只提 PackageConventionsTest | ArchUnit 3 类 21 条规则 |
| :82 | 迁移 V1–V9 | V1–V11（V10=MCP 三表、V11=Skill） |
| :92 | decisions 0001–0009 | 0001–0010 |
| :10 前端技术栈行 | 未列 echarts/TanStack | 补 echarts 6、@tanstack/react-table 9 |
| 架构节 | 无 MCP/Skill/HITL 痕迹 | 增一段「Agent 能力域现状」：HarnessAgent 按请求构建 + 服务端会话状态（server-side-memory: true）、用户自配 MCP 工具（写工具触发 HITL 审批）、内置 Skill；细节链接 modules/05–08 |
| 参考文档节 | lessons 路径（Task 1 已改）；modules 导航 | 补 modules/05–13 新篇入口（Task 9/10 完成后此处链接才有效——**本任务先写文字，链接由 Task 12 收敛时统一补**，或直接在本任务创建占位小节注明「新篇见 docs/technology/modules/」） |

**docs/README.md：** 索引表补 reviews/ 的 09-03 报告行；其余以 Task 1 改后的结构为准校对一遍。

- [ ] **Step 1:** 逐项 Read 对应代码核对上表"改为"列（InvestTools.java、build.gradle:72-79、web/ 目录、application/、infrastructure/、agent/、db/migration/、三个 Controller、Makefile:25、frontend/e2e/、frontend/package.json）
- [ ] **Step 2:** 按清单修改三个文件
- [ ] **Step 3:** 抽验：`grep -c "@Tool" backend/src/main/java/com/portfolio/invest/agent/InvestTools.java` = 7；`ls backend/src/main/resources/db/migration/ | tail -3` 含 V10、V11；`ls backend/src/main/java/com/portfolio/invest/web/*Controller.java | wc -l` ≥ 8（含非 Controller 前缀的 InvestAguiRuntimeContextResolver 等，以 @RestController 注解计）
- [ ] **Step 4:** 链接门禁脚本跑这三个文件范围，零断链
- [ ] **Step 5:** `git commit -m "docs(nav): 根README/AGENTS/docs索引对齐代码现状——7工具/V11/13控制器/补MCP与journal端点"`

### Task 3 (Batch B): technology/architecture/01–05 纠错

**Files:**
- Modify: `docs/technology/architecture/01-系统架构.md`、`02-技术栈与工程结构.md`、`03-后端测试架构.md`、`04-数据采集服务架构.md`（05 只查交叉引用）

**01-系统架构.md：** :23-28 架构图（5 控制器/3 域/6 工具 → 13 控制器/10 域/7 工具，域补 valuation/portfolio/allocation/screening/journal/mcp/skill）；:35 表清单（4 张 → V1–V11 的 20+ 张，点名 mcp_provider/mcp_endpoint/mcp_user_config/skill_user_config）；:46 ReActAgent 循环 → HarnessAgent 装配（链接 modules/01）；:62 server-side-memory **false → true** 及机制改写（服务端 stateStore，前端只发最新一条）。

**02-技术栈与工程结构.md：** :15 AgentScope 2.0.1 三构件 → 2.0.3 四构件；:13/:36 V1/V2 → V1–V11；:28-36 目录树按实际包结构全量刷新（13 控制器、application 11 子包、domain 10 子包、infrastructure +cache、agent 新类、resources +skills/）。

**03-后端测试架构.md：** :17 规模数字重测更新（test 78 类=68 单元+7 切片+3 ArchUnit、integrationTest 23 类——执行时实测）；:61-65 规则数 20→21、TestSourceSetConventionsTest 2→3 条（新增测试方法 GWT 命名+@DisplayName 规则）。其余（PIT 1.20.2/DemoWare、JaCoCo 聚合）审计确认准确，不动。

**04-数据采集服务架构.md：整体重写**（现文档停留在框架重构前）。以现行代码为准重写以下全部章节：
- 结构图：`collector/collector/` 实际为 sources/（plugins.py、configurable.py、registry.py、ratelimit.py）+ converters/ + validators/ + calc/ + store/ + executor/（selector.py 等）+ repositories/ + model/ + scheduler/ + cli.py（先 `find collector/collector -name "*.py" | sort` 实测）
- §3.2 collect_once 免费付费分段 → 任务级 SourceSelector + 熔断 + SourceError 换源（`executor/selector.py`）
- §4 限速：已有 RateLimiter（fina_indicator 0.35s 间隔）+ ThreadPoolExecutor(max_workers=4)（`sources/plugins.py:9-13,352`）
- §3.3 ERP 股息率：已有 make_index_dividend_fetch（index_weight × 成分 dv_ttm 加权）（`plugins.py:83-103`）
- 已知缺口：tzdata 已装（Dockerfile:6）、交易日历已接（TradingCalendar + trading_day_gated + 月度刷新 cron `10 3 1 * *` + 陈旧告警，`scheduler/jobs.py:45-48,270-299`）
- 数据源矩阵：国债 bond_zh_us_rate → **bond_china_yield**（`plugins.py:156-159`）
- 申万映射频率：每日 → 7 天 interval（`collector/tasks/shenwan_mapping.yaml`）
- make collect 语义 → collect-run（`Makefile:74-83`）
- 重写时大量素材可直接引用 `architecture/05-数据类型与来源.md`（2026-09-06 更新，审计确认准确）与 `conventions/04`，避免重复——04 聚焦**服务架构与运行机制**，数据明细链接 05

- [ ] **Step 1:** Read 全部证据文件核对
- [ ] **Step 2:** 修 01/02/03，重写 04，05 交叉引用检查
- [ ] **Step 3:** 抽验：`grep -n "server-side-memory" backend/src/main/resources/application.yml` 为 true；`grep -rn "bond_china_yield\|bond_zh_us_rate" collector/collector/sources/plugins.py`
- [ ] **Step 4:** 链接门禁（technology/ 范围）
- [ ] **Step 5:** `git commit -m "docs(arch): 系统架构/技术栈/测试架构对齐现状，数据采集服务架构按重构后框架重写"`

### Task 4 (Batch C): technology/conventions/01–04 纠错

**Files:**
- Modify: `docs/technology/conventions/01-后端DDD分包规范.md`、`02-后端架构与代码规范.md`、`03-前端架构与代码规范.md`、`04-采集服务架构与代码规范.md`

**01-后端DDD分包规范.md：** :15 DDD 分层域 3→10 个（+allocation/journal/mcp/portfolio/screening/skill/valuation）；:41 控制器 5→13；:46 application 服务列全（+Journal/Portfolio/Valuation/Allocation/Screening/McpConfig/Skill ApplicationService）；:51 domain 子包列全；:62 agent 消费关系补「+ application.skill（SkillApplicationService）+ domain.mcp（McpConfigRepository 等）」。**依赖方向规则与落位规则本体不动**（与 PackageConventionsTest 一致）。

**02-后端架构与代码规范.md：** :126（G8）Cucumber feature 5→10 个 13 场景。其余审计一致不动。

**03-前端架构与代码规范.md：** lint 强制清单补第 5 条（`no-restricted-imports` 禁 echarts 全量入口，唯一注册点 `lib/echarts-setup.ts`，`eslint.config.mjs:63-80`）；**新增「图表基建」一节**：`components/charts/`（EChart.tsx、useECharts.ts、optionBuilders.ts）、`components/chat/charts/`（ChartCard.tsx、DataTable.tsx）、`lib/chart-spec.ts`、`lib/chart-theme.ts`、`lib/echarts-setup.ts`、`tests/charts/`、`tests/chat/`、`scripts/assert-chart-bundle.mjs` 与 `pnpm test:bundle`（gzip ≤240KB 断言）；e2e 范围对齐 15 spec；统计数字刷新（~763 用例/71 文件，标注日期）。

**04-采集服务架构与代码规范.md：** E1/T3 集成 schema 改「alembic + 回放后端 Flyway V3/V4/V7（12 表）」（`tests/conftest.py:27-29`）；T3 幂等口径改「test_writer_idempotency.py 6 用例 + test_writer.py」（实测）；E3 统计刷新标注日期。其余审计一致不动。

- [ ] **Step 1:** Read 证据文件（PackageConventionsTest、eslint.config.mjs、conftest.py、test_writer_idempotency.py、frontend/ 对应目录、`src/bdd/resources/features/`）
- [ ] **Step 2:** 按清单修改 4 文件
- [ ] **Step 3:** 抽验：`ls backend/src/bdd/resources/features/ | wc -l` = 10；`grep -n "V7" collector/tests/conftest.py`
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(conv): 四份规范对齐现状——10域/21条规则/echarts lint/回放V3V4V7"`

### Task 5 (Batch D): technology/modules/ 现有 4 篇修正（含 03 瘦身）

> **执行顺序：本任务在 Task 9/10 之后执行**——03-接口设计瘦身与 01-Agent实现重写需链接 modules/05–13 新篇，新篇落地后再改，链接门禁才能通过。

**Files:**
- Modify: `docs/technology/modules/01-Agent实现.md`（核心重写）、`02-行情数据服务.md`、`03-接口设计.md`（角色转变）、`04-工程与运维.md`

**01-Agent实现.md（偏差最大，按真实装配重写）：**
- 删除 `Agent investAgent(Model, InvestTools)` 直接产出 bean 与 `ReActAgent.builder().maxIters(10)` 样例，替换为真实链路：`AgentConfig` 注册 `AguiAgentRegistryCustomizer` → 请求进入经 `CurrentUserHolder` 取 userId → `HarnessAgentFactory` 按请求构建 `HarnessAgent`（workspace/stateStore/compaction/memory/skillRepository/skillFilter 参数，对照 `HarnessAgentFactory.java:38-60`）
- 工具表 6→7（补 get_valuation：估值查询，返回 ToolResultBlock + ToolEmitter 图表双通道，链接 modules/08）
- get_kline limit 默认 60/上限 120 → **默认 120/最大 500**
- yml 样例：`server-side-memory: false` → **true** 并解释服务端状态机制；补 `invest.mcp.*`（connect-timeout/tool-timeout/pool-max-size）与 harness workspace/state/compaction/memory 配置段（对照 `application.yml:40-92` 实抄）
- 系统提示词概述补「MCP 扩展数据源」章节（`InvestSystemPrompt.java:26-35` 的优先级规则）
- 篇首「对应代码」核对更新

**02-行情数据服务.md：** :5 「对应代码 `backend/.../invest/market/`」该路径不存在 → 改为三处真实分布（infrastructure/market/ 客户端+缓存、domain/market/ 解析+值对象、application/market/ 编排）；文内标题「# 03 · 行情数据服务」与链接「04-接口.md」编号残留修正。数值（TTL/限流/超时/重试）审计一致不动。

**03-接口设计.md（瘦身为端点总览索引）：** 定位从「全量接口细节」改为「端点总览 + 指路」：
- 保留现有六组（auth/admin/conversations/market/agui/agent）的参数/错误码细节（它们无专属域文档）
- 新增 7 组端点总览表（valuation 3 端点→链接 modules/09、screening 1→10、portfolio 20→11、allocation 7→12、journal 6→13、mcp 6→05、skills 2→06），每组只列「方法+路径+一句话+权限」，细节由域文档承载，**避免双头维护**
- 权限口径对齐 `PublicEndpointPaths.java`：公开=auth/login|register、agent/health|status、market/**、valuation/**、screening/**、actuator/**；登录=conversations/journal/portfolio/allocation/mcp/skills/copilotkit；ADMIN=admin/users/**

**04-工程与运维.md：** :63 `make collect` → `make collect-run`（collect=列任务，另有 collect-backfill）；:88 health 描述改纯 liveness、行情连通移到 /api/agent/status；:112 已知限制「每轮重传全部历史」删除（已改服务端状态）；:46-53 collector 目录树补 converters/executor/model/repositories/validators/cli.py。

- [ ] **Step 1:** Read：AgentConfig.java、HarnessAgentFactory.java、CurrentUserHolder、InvestTools.java、InvestSystemPrompt.java、application.yml、MarketData* 三层、全部 Controller、PublicEndpointPaths.java、Makefile、collector/collector/ 树
- [ ] **Step 2:** 按清单修改 4 文件
- [ ] **Step 3:** 抽验：`grep -n "maxIters\|ReActAgent" docs/technology/modules/01-Agent实现.md` 零命中；`grep -n "server-side-memory" docs/technology/modules/01-Agent实现.md` 语境为 true
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(modules): Agent实现按HarnessAgent真实装配重写，接口设计瘦身为端点总览"`

### Task 6 (Batch E): function/ 产品功能文档全量更新

**Files:**
- Modify: `docs/function/README.md`、`00-功能模块概览.md`、`01-产品概览.md`、`99-附录-数据需求与技术扩展.md`、`modules/03-对话式投研问答.md`、`modules/05-AI投研能力.md`、`modules/06-市场估值仪表盘.md`、`modules/08-持仓组合管理.md`、`modules/09-价值投资筛选器.md`、`modules/14-系统与工程.md`
- 不动（审计确认一致）：modules/01、02、04、07、10、11、12、13

**修复清单：**

| 文件 | 修复点 |
|---|---|
| README.md | 删「MVP（⏳ 待开发）」段（估值/配置/持仓/筛选均已上线）；「一句话定位」路线补 MCP/Skill/HITL/图表能力 |
| 00-功能模块概览.md | ① 修内部矛盾：68-71 行「MVP 96%」vs 88 行「MVP 🚧 进行中（33%）」统一为当前真实口径 ② 看板补 4 特性功能点：M03 补 HITL 审批卡/图表卡/MCP 动态工具，M05 补 MCP 工具与 Skill，M14 补 MCP/Skill/HITL/图表工程项 ③ 重算总功能点统计（原 105 口径未含新能力）④ 基线日期刷新 |
| 01-产品概览.md | 「两个前端页面」→ 13 路由全列；5.4 与 In/Out 范围中「（二期）」已交付项翻转 |
| 99-附录.md | 「前端新增页面（规划）」改为已上线清单；「后端新增领域」补 domain/mcp、domain/skill |
| modules/03 | F04「6 类工具」→ 7 类（含 get_valuation 估值查询）；**新增功能点**：HITL 审批卡（InterruptApprovalCard：批准/拒绝/多卡逐张已处理态）、聊天图表卡（ChartCard K线/折线/饼/表格，DataTable 可排序） |
| modules/05 | 「一期已交付 6 类数据查询工具」口径补 MCP 动态工具（用户自配 server，写工具触发审批）与内置 Skill（tushare_data/wind_finance） |
| modules/06 | F02/F05 删「ERP 数据积累中待接入股息率」（已按沪深300股息率×10Y国债真实计算，`ValuationApplicationService.java:140-150`）；补「公开访问」标注 |
| modules/08 | 「功能点详情」F01–F11 标题 ⏳ → ✅（与表格一致） |
| modules/09 | F01 条件清单对齐实现：保留 PE-TTM/PB/股息率/ROE/ROA/毛利率/资产负债率/流动比率/营收净利增速/总市值/**换手率**/行业限定；**删** PE 分位、ROE 连续 N 年、经营现金流>净利润、日均成交额（API/UI 均无，对照 `ScreeningController.java` + `ScreeningForm.tsx`） |
| modules/14 | F02 迁移「当前 V1、V2」→ V1–V11；补 PIT/Descartes 变异测试工程项 |

- [ ] **Step 1:** Read：`frontend/app/` 路由树、`InterruptApprovalCard.tsx`、`ChartCard.tsx`、`DataTable.tsx`、`ScreeningController.java`、`ScreeningForm.tsx`、`ValuationApplicationService.java:140-150`、db/migration/、features/ 四特性需求文档（取功能点口径）
- [ ] **Step 2:** 按清单修改 10 文件
- [ ] **Step 3:** 抽验：`ls frontend/app` 与 01-产品概览路由清单一致；modules/09 中无「日均成交额」「PE 分位」字样
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(function): 看板与模块文档补齐4特性，修矛盾与夸大项，路由对齐13页面"`

### Task 7 (Batch F): 产品落地计划、部署文档、reviews 索引、lessons 经验补沉淀

**Files:**
- Modify: `docs/plans/2026-08-27-产品落地计划.md`、`docs/deployment/首次发布方案.md`、`docs/README.md`（reviews 表，若 Task 2 未覆盖）、`docs/reviews/code-review-lessons.md`

**产品落地计划：**
- 「当前基线」表刷新至 2026-09-12（用最新看板统计，与 Task 6 产出的 00-概览口径一致）
- **追加 MS-16~MS-19**（用户已裁决）：

| 里程碑 | 内容 | 状态 |
|---|---|---|
| MS-16 MCP 数据源集成 | 用户级 MCP server 配置/连接测试/工具发现 | ✅ 已交付 2026-09-07（PR #19 等，`git log` 复核） |
| MS-17 Skill 系统 | 内置 tushare_data/wind_finance，用户级启停 | ✅ 已交付 2026-09-08（PR #20） |
| MS-18 MCP 写工具 HITL 审批 | readOnlyHint≠true 触发中断审批，多卡逐张应答（ADR-0010） | ✅ 已交付 2026-09-08（PR #22/#23），修复 #28（09-11）/#30（09-12） |
| MS-19 聊天富内容图表 | 4 工具图表双通道，ECharts 全站替换 | ✅ 已交付 2026-09-12（PR #29） |

- 「里程碑变更记录」补 2026-09-05~09-12 的 8 个 PR（#17 部署方案、#19、#20、#22/#23、#28、#29、#30、#31 Descartes）
- MVP 发布门槛：补一行裁决注记（e2e 已进 CI、首次发布方案已写；journal 复盘记录的 flaky 待复核——状态据实写，不夸大）

**部署文档（首次发布方案.md）：**
- 「Flyway V1–V9 建表」→ V1–V11
- `sudo cp -r public /opt/invest/frontend/public` 不可执行（仓库无 frontend/public）→ 改 `sudo mkdir -p /opt/invest/frontend/public && sudo cp -r public/* …` 或删除该步（对照 `frontend/Dockerfile:10` 的 mkdir 规避法，写清可操作步骤）
- 补「MCP 数据源 token 填充」一节：部署后对 `mcp_provider.auth_secret_enc` 执行 UPDATE（妙想 em_api_key / Tushare token / Wind ak token），V10 仅 seed NULL 占位；手动握手冒烟 `backend/scripts/mcp-smoke.sh`（读 MX_DS_TOKEN/TUSHARE_TOKEN/WIND_TOKEN）——对照根 README:179 口径
- 补 Skill 说明一句（V11、内置两 skill、无需部署动作）
- MCP_SECRET_KEY：说明「二期规划，当前版本不需要」

**lessons（code-review-lessons.md）：** 追加 2026-09-07~09-12 经验——`git log v1b727d0 的 PR #19–#31 diff 与 commit message`（重点：mcp-integration 的 P0–P3 终审修复、skill P0 验证、mcp-hitl #25 契约修复与多轮终审、chat-rich-content P1–P5 每阶段终审修复、Descartes SPI 对齐）提炼为「问题模式 → 教训」条目，风格对齐既有条目。**每条须有 commit/PR 证据，不臆造。**

- [ ] **Step 1:** `git log --oneline --since=2026-09-05` 复核 PR 号与日期；Read 部署证据（docker-compose.yml、frontend/Dockerfile、mcp-smoke.sh）
- [ ] **Step 2:** 按清单修改 4 文件
- [ ] **Step 3:** 抽验：MS-16~19 存在且状态 ✅；部署文档无 V1–V9 字样
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(plans): 落地计划补MS-16~19与8个PR变更记录，部署文档修可执行性，lessons补4轮经验"`

### Task 8 (Batch G): features/ 索引翻转 + mcp-hitl 验证记录补写

**Files:**
- Modify: `features/README.md`
- Create: `features/mcp-hitl/03-plan/验证记录.md`

**features/README.md 索引表 4 行状态翻转：**

| 特性 | 改为 |
|---|---|
| mcp-integration | 已交付（2026-09-07，PR #19）｜MS-16 |
| skill-integration | 已交付（2026-09-08，PR #20）｜MS-17 |
| mcp-hitl | 已交付（2026-09-08，PR #22/#23；修复 #28/#30）｜MS-18 |
| chat-rich-content | 已交付（2026-09-12，PR #29）｜MS-19 |

（里程碑列与 Task 7 的 MS-16~19 对应；PR 号 `git log` 复核。）

**补写 `features/mcp-hitl/03-plan/验证记录.md`：** P0-report（`.superpowers/sdd/mcp-hitl/P0-report.md`）声称"完整版已写入本文件"但从未提交。从 P0-report 升格补写，内容至少含：agentscope 2.0.1 权限确认 HITL 的真实协议勘误——interrupt 以 RAW 事件透传（无 `agentscope.interruptKind` metadata）、`resume[]` 对权限确认流一律 `AGUI_INTERRUPT_CONTRACT_ERROR`、真实续跑需消息 metadata `agentscope_confirm_results` 挂 `List<ConfirmResult>`、AG-UI `AguiMessage` 无 metadata 字段无法承载；加上后续 2.0.3 升级与 #28/#30 修复的闭环记录（从 git log 与交接事项.md 取）。头部注明「2026-09-12 补写，源自 P0-report + PR 记录」。

- [ ] **Step 1:** Read `.superpowers/sdd/mcp-hitl/P0-report.md`、`交接事项.md`、`git log --oneline --grep=hitl -i`
- [ ] **Step 2:** 修改索引 + 写验证记录
- [ ] **Step 3:** 链接门禁；`git commit -m "docs(features): 索引4特性状态翻转，补写mcp-hitl验证记录（P0协议勘误升格）"`

### Task 9: modules/README + 机制篇 05–08（4 篇新文档）

**Files:**
- Create: `docs/technology/modules/README.md`、`05-MCP数据源集成.md`、`06-Skill系统.md`、`07-HITL人工审批.md`、`08-聊天图表双通道.md`

**modules/README.md：** 篇目索引（01–04 现有 + 05–08 机制篇 + 09–13 业务域篇）、编号约定（后续新增：机制类续 1x，业务域续 2x，若 05–13 不足可扩）、「新增模块文档归入本目录并在此登记」约定重申。

**每篇结构（统一）：** 篇首「对应代码」真实路径 → 概述（解决什么问题）→ 架构/数据流 → 关键类与配置 → 端点（如有）→ 测试（单测/BDD/e2e 位置）→ 已知限制。

**05-MCP数据源集成.md 必读代码：** `web/McpConfigController.java`（6 端点）、`domain/mcp/`（McpProvider/McpEndpoint/仓库接口）、`application/mcp/`、`agent/McpClientPool.java`（streamable HTTP + initialize 握手）、`agent/UserToolkitFactory.java`（token 明文直读 :36-37；readOnlyHint 判定 :56-72）、`agent/HarnessAgentFactory.java`（装配）、V10__mcp.sql（三表）、`application.yml` `invest.mcp.*`（connect-timeout/tool-timeout/pool-max-size）、`backend/scripts/mcp-smoke.sh`、前端 `app/settings/mcp/page.tsx` + `components/mcp/McpSettingsPage.tsx`。**必写**：token 不随迁移进 git、部署时 UPDATE 填充、内置 provider（妙想/Tushare/Wind）seed NULL 占位。

**06-Skill系统.md 必读代码：** `web/SkillConfigController.java`（GET /api/skills、PUT /api/skills/config）、`domain/skill/`、`application/skill/`、V11__skill.sql、`resources/skills/tushare_data/SKILL.md` 与 `wind_finance/SKILL.md`（内置两个）、`HarnessAgentFactory.java`（ClasspathSkillRepository + SkillFilter 按用户启用集装配）、前端 `app/settings/skills/` + `components/skill/SkillSettingsPage.tsx`。

**07-HITL人工审批.md 必读代码：** `docs/technology/decisions/0010-mcp-hitl-permission.md`（决策）、`UserToolkitFactory.java:56-72`（readOnlyHint 缺省视为写）、前端 `components/chat/InterruptApprovalCard.tsx`（98 行：批准/拒绝/多卡已处理态）、`ThreadArea.tsx`（useInterrupt 挂载）、`infrastructure/seed/HitlE2eSeedRunner.java`、`frontend/e2e/` hitl spec；**协议勘误引用** `features/mcp-hitl/03-plan/验证记录.md`（Task 8 产出）。

**08-聊天图表双通道.md 必读代码：** `agent/chart/ChartSpec.java` + `ChartSpecs.java`（契约工厂）、`InvestTools.java` 四工具双通道（get_kline/get_valuation/get_market_overview/get_financials：emit 全量 ChartSpec + 返回摘要）、`AguiEventNonNullCodec.java` + `AguiWirejsonConfig.java`（NON_NULL 语义：null 字段省略）、契约测试 fixtures（双端共享）、前端 `lib/chart-spec.ts`（zod）、`components/chat/charts/ChartCard.tsx`（骨架/渲染/降级三态）、`DataTable.tsx`（TanStack Table v9）、`components/charts/`（EChart/useECharts/optionBuilders）、`lib/echarts-setup.ts`（唯一注册点）、`scripts/assert-chart-bundle.mjs`（gzip ≤240KB）；设计规格 `features/chat-rich-content/02-design/设计规格说明.md`。

- [ ] **Step 1:** 逐篇 Read 上列代码（**全部路径先验证存在**，写前核对每个类名/端点/配置键）
- [ ] **Step 2:** 写 5 个文件
- [ ] **Step 3:** 抽验：每篇「对应代码」路径逐一 `test -f` 通过；端点与 Controller @GetMapping/@PutMapping 实测一致
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(modules): 新增机制篇05-08——MCP集成/Skill/HITL审批/图表双通道"`

### Task 10: modules/ 业务域篇 09–13（5 篇新文档）

**Files:**
- Create: `docs/technology/modules/09-估值域.md`、`10-筛选域.md`、`11-持仓域.md`、`12-资产配置域.md`、`13-投研日志域.md`

结构同 Task 9 统一模板。各篇必读代码与必写要点：

| 篇 | 必读代码 | 必写要点 |
|---|---|---|
| 09-估值域 | `domain/valuation/`、`application/valuation/ValuationApplicationService.java`（ERP 计算 :140-150）、`web/ValuationController.java`（overview/industries/history，**公开只读**）、collector 估值任务（链 architecture/05） | 全 A 中位数/分位、ERP=沪深300股息率×10Y 国债、温度计；数据来自 collector 快照 |
| 10-筛选域 | `domain/screening/`、`application/screening/`、`web/ScreeningController.java` | 12 筛选条件 AND 组合（与 modules/09 功能文档同一口径，Task 6 已修正）+ 行业 + 排序 + limit≤200、至少一个条件；公开只读 |
| 11-持仓域 | `domain/portfolio/`（最大域，含成本盈亏引擎）、`application/portfolio/`、`web/PortfolioController.java`（**20 端点**逐一列） | buy/sell/两种分红/trades/cash-transactions/分组/集中度；成本盈亏计算规则 |
| 12-资产配置域 | `domain/allocation/AllocationTemplate.java`（4 内置模板）、`web/AllocationController.java`（templates/plans CRUD/activate/deviation） | 经典模板一键套用、激活、与持仓偏离度 |
| 13-投研日志域 | `domain/journal/JournalEntryType.java`（四类）、`web/JournalController.java`（6 端点含 timeline）、`JournalApplicationService` import portfolio（持仓联动） | 四类条目、时间线、tradeId 反查联动 |

- [ ] **Step 1:** 逐篇 Read 代码核对（端点数用 grep @*Mapping 实测）
- [ ] **Step 2:** 写 5 文件
- [ ] **Step 3:** 抽验：11-持仓域端点数 = PortfolioController 实测 @*Mapping 计数
- [ ] **Step 4:** 链接门禁；`git commit -m "docs(modules): 新增业务域篇09-13——估值/筛选/持仓/配置/日志"`

### Task 11: ADR-0011 + 0008 标注 + decisions 索引

**Files:**
- Create: `docs/technology/decisions/0011-server-side-agent-state.md`
- Modify: `docs/technology/decisions/0008-conversation-persistence.md`、`docs/technology/decisions/README.md`

**ADR-0011（服务端 Agent 会话状态）：** 格式对齐现有 ADR（状态/日期/背景/决策/后果）。内容：背景=0008 时代前端持全量历史、每轮 PUT；决策=引入 agentscope-harness（2.0.3），`server-side-memory: true`，HarnessAgent 以 JsonFileAgentStateStore 服务端持久化会话历史（workspace/compaction 30-10/memory flush 30m），前端只发最新一条（前端 /api/copilotkit 路由边界 trimToLatestUserMessage 拦截——CopilotKit 无单消息发送路径的边界结论，源自 `.superpowers/sdd/P3-mcp前端设置页与收尾/progress.md`）；会话 REST（GET/PUT messages）保留作历史回灌。**Status: 已接受 2026-09-12（补记，机制生效于 2026-09-07 mcp-integration P3）**，supersedes 0008 的内存模型部分（0008 的持久化表结构仍有效）。

**0008：** 头部加「⚠️ 2026-09-12：内存模型部分已被 [0011](0011-server-side-agent-state.md) 取代（表结构仍有效）」。

**decisions/README.md：** 索引补 0010（漏登记）与 0011；主题速查补 MCP/HITL/会话状态线索。

- [ ] **Step 1:** Read `application.yml:40-92`、`HarnessAgentFactory.java`、0008 全文、一个现有 ADR 对齐格式；`git log --oneline --grep="server-side\|8df2ac3"` 复核生效时间
- [ ] **Step 2:** 写 0011、修 0008 与 README
- [ ] **Step 3:** 链接门禁；`git commit -m "docs(adr): 0011服务端Agent会话状态承接HarnessAgent机制变更，0008标注取代关系"`

### Task 12: vendored 说明 + 全部导航收敛

**Files:**
- Create: `docs/technology/research/agentscope/README.md`
- Modify: `docs/technology/README.md`、`docs/README.md`、`docs/function/README.md`、`features/README.md`、`AGENTS.md`（参考文档节）、`README.md`（目录结构行）

**agentscope/README.md：** vendored 说明——本目录为 AgentScope 官方文档摘录拷贝（版本与 pin 见各文内标注），内部 `../harness/*`、`../building-blocks/*` 等约 60 处链接指向未拷贝的上游章节，**预期不可达**，查全文请到上游；copilotkit/ 同理补一行说明（可在其目录加头部注释或并入本说明）。**另：P0 协议勘误的正式沉淀在 `features/mcp-hitl/03-plan/验证记录.md`，此处 FAQ/agui.md 与实测有出入时以后者为准**（在 agui.md 头部加一行勘误指针）。

**technology/README.md：** :28 decisions「0001–0009」→ 0001–0011；:75「2.0.1」→ 2.0.3；:19/:55「6 个 @Tool」→ 7 个；modules 导航补 05–13 九篇（Task 9/10 产出）。

**docs/README.md：** technology 行的描述补 modules 扩充；function 行核对；archive 行核对（Task 1 已建）。

**function/README.md：** 导航与 00-概览链路口径核对。

**features/README.md：** 目录结构小节确认本计划已登记（Task 1）。

**AGENTS.md 参考文档节：** 补 modules/05–08、09–13 入口行；decisions 0001–0011。

**根 README.md:160 目录结构行：** docs/ 描述补 archive/ 与 reviews 新布局。

- [ ] **Step 1:** 逐文件 Read 后修改
- [ ] **Step 2:** 抽验：`grep -rn "0001–0009\|0001-0009" docs/ README.md AGENTS.md` 零命中；`grep -rn "2\.0\.1" docs/technology/README.md` 零命中（agentscope/research 内的 2.0.1 历史标注除外——仅指 technology/README.md 文件内）
- [ ] **Step 3:** 链接门禁；`git commit -m "docs(nav): 全站导航收敛——modules新篇登记，ADR索引补全，vendored说明"`

### Task 13: 终验 + PR

- [ ] **Step 1:** 附录链接门禁脚本全仓跑（docs/ + features/ + README.md + AGENTS.md），**零断链**
- [ ] **Step 2:** 旧路径残留扫描：`grep -rn "docs/code-review-lessons\|docs/plans/2026-08-1" --include="*.md" . | grep -v docs/archive | grep -v features/plans` 零命中
- [ ] **Step 3:** 派 1 个 verify 子代理：从新文档（05–13、0011）随机抽 15 条事实性声明对照代码验证，全部通过
- [ ] **Step 4:** `grep -rn "6 个\|6个" README.md AGENTS.md docs/technology/README.md` 等工具数口径复查（语境判断，e2e 用例数等其它"6"不算）
- [ ] **Step 5:** push 并开 PR：`gh pr create --base main --title "docs: 文档体系全面更新——纠错+补齐4特性7域+目录重组" --body`（body 含：审计来源、四阶段摘要、文件统计、验证结果；附 🤖 生成标注）
- [ ] **Step 6:** 在本文件勾选全部任务框，PR 描述链接本计划

---

## 附录：链接门禁脚本（临时执行，不入库）

```bash
python3 - <<'EOF'
import os, re, sys
targets = ['README.md', 'AGENTS.md']
for root in ('docs', 'features'):
    for dirpath, _, files in os.walk(root):
        for f in files:
            if f.endswith('.md'):
                targets.append(os.path.join(dirpath, f))
bad = []
for path in targets:
    if not os.path.exists(path):
        continue
    text = open(path, encoding='utf-8').read()
    for m in re.finditer(r'\[[^\]]*\]\(([^)\s]+)\)', text):
        link = m.group(1).split('#')[0]
        if not link or link.startswith(('http://', 'https://', 'mailto:')):
            continue
        target = os.path.normpath(os.path.join(os.path.dirname(path) or '.', link))
        if not os.path.exists(target):
            bad.append(f'{path} -> {link}')
print('\n'.join(bad) if bad else 'OK: 零断链')
sys.exit(1 if bad else 0)
EOF
```

> 已知豁免：`docs/technology/research/agentscope/` 内约 60 处指向上游未拷贝章节的链接为**预期断链**（vendored 摘录，Task 12 加说明）——脚本输出中该目录的命中可忽略，或执行时把该目录从 walk 中排除后单列。
