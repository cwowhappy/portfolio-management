# 前端测试体系加固计划

日期：2026-08-30
分支：`feature/frontend-test-hardening`
状态：已完成（P0–P3 全做、DEEPSEEK_API_KEY 已配 GitHub secrets、全局 80% + portfolio 目录 80% 门槛、标准流程）

## 背景与现状

实测（`pnpm vitest run --coverage`）：35 个测试文件 / 229 用例全绿，语句 96.06% / 分支 84.74% / 函数 85.09%。全局门槛通过，但 portfolio 域是洼地（分支仅 67.2%）。

| # | 缺口 | 证据 |
|---|---|---|
| 1 | e2e 不进 CI | ci.yml frontend job 只有 lint+vitest；关键链路 CI 零守护 |
| 2 | portfolio 写路径契约无守护 | portfolioRoute.test.ts 仅 2 用例（违反规范 7.3）；portfolioApi.ts 17 函数仅测 6 个 |
| 3 | portfolio 组件只剩 happy-path | PortfolioBoard 1 用例；图表三件套 38-57%；错误/交互/边界分支无测 |
| 4 | admin 页 305 行违反薄壳规范 | 逻辑塞在 page.tsx，天然逃避覆盖率；停用/启用/重置密码无 e2e |
| 5 | SSE/AG-UI 事件流零覆盖 | CopilotKit hooks 全 mock，流中断/RUN_ERROR/断流持久化无测 |
| 6 | valuationApi 2/3 函数未测、ValuationBoard 错误分支未测 | — |
| 7 | 规范脱节 | 「208 测试锁定」过时、e2e 范围描述滞后、全局门槛掩盖洼地 |

## 实施计划

### P0 — 契约与 CI 防线 ✅
1. ✅ `tests/lib/portfolioRoute.test.ts` 按规范 7.3 模板补齐：POST/DELETE handler、Cookie 透传、502（2 → 11 用例）
2. ✅ `tests/lib/portfolioApi.test.ts` 逐函数补齐 11 个未测函数（buy/sell/deleteGroup/deletePosition/addCashDividend/addStockDividend/fetchPositions/fetchAllocation 等）+ 204 分支 + 错误归一（22 用例）
3. ✅ `.github/workflows/ci.yml` 增加 Playwright e2e job（postgres:16 service，secrets: ADMIN_*、DEEPSEEK_API_KEY，失败上传 test-results）

### P1 — portfolio/估值域补测 ✅
4. ✅ portfolio 组件补失败/边界/交互：PortfolioBoard 5、BuyForm 7、PositionActions 9、PositionTable 6；AllocationPie/IndustryBar/ConcentrationList 新建测试
5. ✅ `valuationApi` 补至 9 用例 + `ValuationBoard` 错误分支 4 用例
6. ✅ vitest 门槛：全局 80% + portfolio 目录（components/portfolio + lib/portfolioApi）分支 ≥80%，见 `vitest.config.ts`

### P2 — SSE 链路 + 结构性问题 ✅
7. ✅ SSE/AG-UI 事件流双层单测：`tests/agui-stream.test.ts`（真实 HttpAgent + 注入 fake fetch 假事件帧，6 用例）+ `tests/ThreadArea.stream.test.tsx`（mock 边界测持久化，6 用例）
8. ✅ admin 页重构：305 行业务逻辑下沉 `components/admin/AdminBoard.tsx`（唯一生产代码改动），AdminBoard 13 用例；停用/启用/重置密码 e2e 由另一代理补充中
9. ✅ 错误态 e2e：`e2e/error-states.spec.ts` 2 用例（Playwright `page.route` 拦截制造 502）

### P3 — 文档同步 ✅
10. ✅ 03 规范：测试锁定数更新（208 → 320）、e2e 范围描述、7.3 教训（portfolio 路由只测 GET）、7.2 per-directory 门槛条款、新增 7.6 SSE 双层测试条款、1.1 薄壳反面案例补注

## 实施结果

最终实测（2026-08-30，`pnpm vitest run --coverage`）：

| 指标 | 加固前 | 加固后 |
|---|---|---|
| 测试文件 / 用例 | 35 / 229 | 41 / 320 |
| 语句覆盖 | 96.06% | 98.79% |
| 分支覆盖 | 84.74% | 89.72% |
| 函数覆盖 | 85.09% | 94.17% |
| portfolio 目录分支 | 67.2%（洼地） | 88.15%（≥80% 门槛通过） |
| lib/portfolioApi.ts 分支 | — | 96.87%（≥80% 门槛通过） |
| e2e 用例 | 20（不进 CI） | 22 + admin 用例并入中（约 25），CI 独立 job 守护 |

### 验收核对

1. ✅ `pnpm lint && pnpm test` 全绿，portfolio 域分支 88.15% ≥ 80%
2. ✅ e2e 本地通过；CI e2e job 已配置（chat.spec 在 CI 用 DEEPSEEK_API_KEY secret 真实跑）
3. ✅ 缺口 #1–#7 全部闭环
4. ✅ 03 规范同步（本节 P3）

### 遗留事项

- `next start` 与 `output: standalone` 组合的既有警告（构建期提示，不影响测试与部署，未在本次范围处理）。
- `PositionActions.tsx`（分支 76.47%）与 `GroupManager.tsx`（分支 76.92%）文件级分支仍略低于 80%，目录聚合（88.15%）达标；后续迭代可顺手补齐，不阻塞门槛。
- admin 停用/启用/重置密码 e2e（2–3 条）由另一代理补充，e2e 总数将增至约 25 条。
