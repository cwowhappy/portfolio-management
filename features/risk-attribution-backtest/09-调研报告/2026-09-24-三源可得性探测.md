# 三源可得性探测报告（MS-13 Task 0）

- **探测日期**：2026-09-24（分支 `feature/risk-attribution-backtest`，commit `6dde0e5` 之上）
- **探测性质**：只读调研——tushare/akshare API 拉取 + 本地 PG `SELECT`，未写任何库表；token 不入文。
- **token 来源**：`collector/.env`（`TUSHARE_TOKEN`/`DATABASE_URL`，经 `collector.config.load()` 读取；brief 中 `load_config` 为示意写法，实际模块为 `collector.config` 的 `load()`）。
- **探测脚本**：`/Users/lixiaoyi/.claude/jobs/bb336660/tmp/`（step1_sw_daily.py / step2_index_fund.py / step3_index_classify.py / step3b_align.py / step4_akshare_sw.py，仓库外，未提交）。

## 一、结论速览

| 数据源 | 接口 | 样本 | 结果 | 裁决 |
|---|---|---|---|---|
| 申万一级行业指数 | tushare `sw_daily` | 801010.SI | **无接口访问权限**（积分门槛，详见 §二） | ❌ **不可得（重大降级，须用户拍板）** |
| 中证全债 | tushare `index_daily` | H11001.CSI | 17 行（20260901–20260923），close 可用 | ✅ 可得 |
| 黄金 ETF | tushare `fund_daily` | 518880.SH | 17 行（20260901–20260923），全字段 | ✅ 可得 |
| 31 行业码对齐 | 库表 vs 计划 vs `index_classify` vs akshare | — | 四方集合**零缺失、零多余** | ✅ 对齐 |

**一句话**：Task 12（债券/黄金）两源直接可得；Task 1（申万行业指数）tushare 主源被积分门槛挡住，数据本身经 akshare 申万官网源实证可得——属重大降级，源选择**须用户拍板**（选项见 §五）。

## 二、Step 1：sw_daily 探测（❌ 无权限）

命令（修正 import 后实际执行）：

```python
pro.sw_daily(ts_code='801010.SI', start_date='20260901', end_date='20260924')
```

返回异常（原文）：

> 抱歉，您没有接口(sw_daily)访问权限，权限的具体详情访问：https://tushare.pro/document/1?doc_id=108。

- 换 801980.SI 复测一次，**同样报错**——非偶发限频，是 token 积分档位不满足 `sw_daily` 权限。
- 同 token 下 `index_daily`/`fund_daily`/`index_classify`/`index_dailybasic`（现有任务在用）均正常，排除 token 本身失效。
- 错误指向 tushare 积分说明（doc_id=108）：即提升账户积分档位可解锁，属账户侧配置而非代码问题。

## 三、Step 2：H11001.CSI 与 518880.SH 探测（✅ 均可得）

窗口均为 `20260901–20260924`，今日未收盘故不含 0924。

| 接口 | 代码 | 行数 | 字段（11 列） | 备注 |
|---|---|---|---|---|
| `index_daily` | H11001.CSI | 17 | ts_code, trade_date, close, open, high, low, pre_close, change, pct_chg, vol, amount | 最新 20260923；**open/high/low/amount 为 None**（债券指数仅发布收盘口径），close/pre_close/change/pct_chg 完整——本用途只需 close，无影响 |
| `fund_daily` | 518880.SH | 17 | 同上（pre_close/open/high/low/close/change/pct_chg/vol/amount） | 最新 20260923，全字段有值 |

对照与健康度：

- `index_daily` 930950.CSI 同窗 16 行（最新 20260922，比 H11001 滞后一日）——token 健康，CSI 代码族延续既有实证先例；顺带记录**不同 CSI 指数更新节拍可差一日**，Task 12 回补口径以各代码实际最新交易日为准。
- **附加探测（降级路径验证）**：`index_daily` 拉 801010.SI 返回**空 DataFrame（0 行）**——申万代码不在 `index_daily` 数据范围，「申万码走 index_daily」这条路不通（对 §五选项 C 有影响）。

**限频表现**：整个探测会话共 8 次 tushare 调用（含 2 次失败的 sw_daily 与 2 次 index_classify），零限频报错。Task 1 落地时单次任务约 31 个代码循环调用（区间拉取、`supports_range=True` 模式），在当前额度内；实现仍建议调用间保守 sleep（0.3–0.5s）。

## 四、Step 3：31 行业码四方对齐核验（✅ 零差异）

库内（本地 PG，只读 SELECT）：

```
SELECT COUNT(DISTINCT industry_code), MIN(industry_code), MAX(industry_code)
FROM shenwan_industry_mapping;
-- 31 | 801010 | 801980
```

四个来源的 31 码集合程序化比对（去 `.SI` 后缀）：

| 来源 | 数量 | 相对计划清单缺失 | 多余 |
|---|---|---|---|
| 实施计划 Task 1 `INDUSTRY_INDEX_CODES`（基准） | 31 | — | — |
| 本地 `shenwan_industry_mapping` DISTINCT industry_code | 31 | 无 | 无 |
| tushare `index_classify(level='L1', src='SW2021')` | 31 | 无 | 无 |
| akshare `sw_index_first_info()`（申万宏源官网） | 31 | 无 | 无 |

行业名称四方亦逐一名一致（库内 801010 农林牧渔 … 801980 美容护理，与计划 dict 完全相同）。`index_classify` 本身当前 token 可得（周更 `shenwan_mapping` 任务既用它 + `index_member_all` 落映射表，生产在跑，同为本探测旁证）。

**结论**：计划 Task 1 的 31 码清单无需任何修正；`index_code` 去 `.SI` 落库与 `industry_code` 同构的 Join 设计成立。

## 五、降级路径实证与裁决（需求规格 §七）

### 5.1 申万行业指数（Task 1 主源不可得）——**重大降级，须用户拍板**

`sw_daily` 被权限挡住后，对 §七各预案逐一实证：

| 选项 | 路径 | 实证结果 | 评价 |
|---|---|---|---|
| A | 提升 tushare 积分解锁 `sw_daily` | 未测（账户操作非代码探测范围） | 最小改动（计划 Task 1 原样），但有成本且依赖外部账户 |
| B | akshare 申万官网源 `index_hist_sw` | ✅ **实证可得**：801010 日线 **6462 行全历史**（含 2026-09-23），字段 `代码/日期/收盘/开盘/最高/最低/成交量/成交额`，日期 `YYYY-MM-DD` 需转库内格式；无日期参数、一次全量返回，需本地裁剪窗口 | 申万 2021 口径原生同构（31 码已对齐）；akshare 1.16.72 已在 venv；注意 `index_analysis_daily_sw` 在该版本**解析损坏**（KeyError '发布日期'），勿用 |
| C | `index_daily` 中证行业指数 | ⚠️ 部分可行：CSI 代码族可得（930950/H11001 实证），但**申万 801xxx 码经 index_daily 返回空**；若改用中证自家行业指数体系则与库内 SW 2021 映射**不同构**，归因 Join 与基准口径需重设计 | 改动面大于 B，不推荐 |
| D | 个股级简化（停 P1 Task 6–8） | — | §七兜底，仅当 A/B 均否决时启用 |

**建议（供拍板，未改计划）**：选项 B（akshare `index_hist_sw`，申万官网源）实证最充分、口径零转换；若用户倾向保持 tushare 单一依赖则选 A。**本报告不改动实施计划**——Task 1 的源选择（及 brief Step 4「结论写回 Task 1/Task 12 注释位」）待用户裁决后由后续任务落盘。

### 5.2 中证全债 H11001.CSI（Task 12 债券源）——✅ 可得

`index_daily` 直接返回 close 序列（17 行样本验证），无降级必要。落库沿用 930950 先例即可。

### 5.3 黄金 ETF 518880.SH（Task 12 黄金源）——✅ 可得

`fund_daily` 全字段可得，含成交量额。§七「不可得降级 akshare SGE 现货」**不触发**。

## 六、裁决汇总

1. **sw_daily（申万一级）**：❌ 不可得（token 积分档位无 `sw_daily` 权限，复测确认）→ 触发需求规格 §七重大降级流程，**须用户拍板**（推荐选项 B：akshare `index_hist_sw`，实证 6462 行全历史可得）。
2. **index_daily H11001.CSI**：✅ 可得（17 行/11 列，close 完整；注意该接口对债券指数仅 close 口径）。
3. **fund_daily 518880.SH**：✅ 可得（17 行/11 列全字段）。
4. **31 码对齐**：✅ 库表/计划/tushare/akshare 四方零缺失零多余，名称逐一致，Task 1 清单免修。
5. 限频：探测量级（6 次/会话）无限频报错；31 码循环调用建议保守间隔。
