# P1 · 个股基本面数据底座 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增个股估值日快照（`stock_valuation_daily`）与个股财务指标季数据（`stock_financial`）两张表（Flyway V7），并在 collector 落地 tushare 采集（daily_basic + fina_indicator）与幂等写入，同时扩展行业加权 ROE/股息率，为筛选器（P2）与行业估值（P2）提供数据地基。

**Architecture:** 采集（写）与查询（读）分离。本计划只做「schema + collector 写入」，不做任何后端读侧。两张新表为 Python collector 写入的跨服务契约，表名/列类型不可随意变更。行业 ROE/股息率通过扩展既有 `industry_valuation` 任务的 source→converter→calc→writer 链路实现，行业 PE/PB 口径保持不变（仍 akshare）。

**Tech Stack:** Python 3.12 / pandas / akshare / tushare / psycopg / APScheduler / pytest · PostgreSQL 16（Flyway 管 schema）

**Spec:** [需求规格说明](../01-requirement/需求规格说明.md)（§三 FR-A1/A2/A3）· [数据需求附录](../../../docs/function/99-附录-数据需求与技术扩展.md) · collector 现有任务与注册器（`collector/collector/scheduler/jobs.py`、`collector/collector/sources/plugins.py`、`collector/collector/store/writer.py`）

## Global Constraints

- **schema 由 Flyway（Java）单一拥有**；本计划新增 `V7__stock_fundamental.sql`，collector **不建表、不迁移**，只 INSERT/UPDATE，表名/列名逐字对齐 V7。
- 个股估值日快照以 `(trading_day, stock_code)` 唯一；个股财务季数据以 `(report_date, stock_code)` 唯一，均幂等 `ON CONFLICT DO UPDATE`。
- 口径：估值 PE 用 tushare `pe_ttm`（TTM）、股息率用 `dv_ttm`；财务指标来自 `fina_indicator`（`roe`/`roa`/`grossprofit_margin`/`debt_to_assets`/`current_ratio`/`or_yoy`/`netprofit_yoy`）。
- 覆盖范围：仅沪深正常交易股（剔除 ST/退市，不含北交所）。
- 财务回填：近 3 年季报（约 12 期）。
- 行业 ROE/股息率：市值加权算术平均（与既有 PE/PB 一致）。
- 测试：pytest + coverage ≥ 80%（`make collect-test` 强制），adapter 测试用 mock/fixtures 不碰真实网络，writer 测试用真实 PG（无 `DATABASE_URL` 则 skip）。

## File Structure

**新建：**

| 文件 | 职责 |
|---|---|
| `backend/src/main/resources/db/migration/V7__stock_fundamental.sql` | 两张表建表 |
| `backend/src/main/resources/db/seed/valuation-dev-seed.sql`（扩展） | 追加 dev/e2e 样例数据 |
| `collector/tasks/stock_valuation_daily.yaml` | 个股估值日快照任务定义 |
| `collector/tasks/stock_financial.yaml` | 个股财务季数据任务定义 |

**修改：**

| 文件 | 改动 |
|---|---|
| `collector/collector/sources/plugins.py` | 新增 `StockValuationDailySource`、`StockFinancialSource`；扩展 `IndustryUniverseSource` |
| `collector/collector/calc/snapshot.py` | 扩展 `IndustryValuationCalc` 输出 roe/dividend_yield |
| `collector/collector/store/writer.py` | 新增 `stock_valuation_daily`/`stock_financial` upsert；扩展 `industry_valuation` upsert 加 roe/dividend_yield |
| `collector/collector/scheduler/jobs.py` | `_field_columns()` 加两个转换器；`build_registries` 注册两个 source |

**测试新建：**

| 文件 | 职责 |
|---|---|
| `collector/tests/test_sources_stock.py` | 两个新 source + 行业 source 扩展单测（mock tushare/DB） |
| `collector/tests/test_calc_industry_fundamental.py` | 行业 ROE/股息率加权单测 |
| `collector/tests/test_writer_stock.py` | 新表写入幂等集成测试 |

---

### Task 1: Flyway V7 建表 + dev seed

**Files:**
- Create: `backend/src/main/resources/db/migration/V7__stock_fundamental.sql`
- Modify: `backend/src/main/resources/db/seed/valuation-dev-seed.sql`

**Interfaces:**
- Consumes: 无（纯 DDL/seed）。
- Produces: `stock_valuation_daily`、`stock_financial` 两张表（P2 读侧 + P3 前端联调的数据契约）。

- [ ] **Step 1: 写 V7 迁移**

创建 `V7__stock_fundamental.sql`：

```sql
-- 个股基本面数据底座：估值日快照 / 财务指标季数据
-- 本 schema 为 Python 采集服务（collector）写入的跨服务契约，表名与列类型不可随意变更。
-- total_mv/circ_mv 单位：元（collector 由 tushare 万元换算）；dividend_yield 为 dv_ttm 口径。

CREATE TABLE stock_valuation_daily (
    id BIGSERIAL PRIMARY KEY,
    trading_day DATE NOT NULL,
    stock_code VARCHAR(16) NOT NULL,
    stock_name VARCHAR(64) NOT NULL,
    pe_ttm NUMERIC(12,4),
    pb NUMERIC(12,4),
    dividend_yield NUMERIC(12,4),
    total_mv NUMERIC(20,2),
    circ_mv NUMERIC(20,2),
    turnover_rate NUMERIC(12,4),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (trading_day, stock_code)
);

CREATE TABLE stock_financial (
    id BIGSERIAL PRIMARY KEY,
    report_date DATE NOT NULL,
    stock_code VARCHAR(16) NOT NULL,
    roe NUMERIC(12,4),
    roa NUMERIC(12,4),
    gross_margin NUMERIC(12,4),
    debt_to_assets NUMERIC(12,4),
    current_ratio NUMERIC(12,4),
    revenue_yoy NUMERIC(12,4),
    netprofit_yoy NUMERIC(12,4),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (report_date, stock_code)
);

CREATE INDEX idx_stock_valuation_daily_day ON stock_valuation_daily (trading_day);
CREATE INDEX idx_stock_valuation_daily_code ON stock_valuation_daily (stock_code);
CREATE INDEX idx_stock_financial_code ON stock_financial (stock_code, report_date);
```

- [ ] **Step 2: 追加 dev seed（样例数据，非迁移）**

在 `db/seed/valuation-dev-seed.sql` 末尾追加（手工执行，供 P2/P3 联调与 e2e；不随 Flyway 自动跑）：

```sql
-- 个股基本面样例：供 /screener、/industry 联调与 e2e。
INSERT INTO stock_valuation_daily (trading_day, stock_code, stock_name, pe_ttm, pb, dividend_yield, total_mv, circ_mv, turnover_rate) VALUES
  ('2026-08-27', '600519', '贵州茅台', 22.50, 7.80, 2.10, 2100000000000, 2100000000000, 0.35),
  ('2026-08-27', '000858', '五粮液', 18.20, 4.50, 2.80, 580000000000, 580000000000, 0.62),
  ('2026-08-27', '601398', '工商银行', 5.60, 0.62, 5.40, 2200000000000, 2100000000000, 0.18),
  ('2026-08-27', '600036', '招商银行', 9.80, 1.05, 3.90, 980000000000, 980000000000, 0.44),
  ('2026-08-27', '300750', '宁德时代', 28.40, 4.20, 0.60, 1100000000000, 900000000000, 1.25);

INSERT INTO stock_financial (report_date, stock_code, roe, roa, gross_margin, debt_to_assets, current_ratio, revenue_yoy, netprofit_yoy) VALUES
  ('2026-06-30', '600519', 24.50, 18.20, 91.20, 21.30, 3.80, 16.80, 15.20),
  ('2026-06-30', '000858', 20.10, 15.40, 75.60, 30.10, 2.60, 12.50, 11.30),
  ('2026-06-30', '601398', 11.80, 0.95, 0.00, 91.80, 0.90, 2.10, 1.80),
  ('2026-06-30', '600036', 16.40, 1.35, 0.00, 90.20, 1.10, 4.60, 5.20),
  ('2026-06-30', '300750', 19.20, 6.80, 25.30, 58.40, 1.40, 32.60, 28.90);
```

- [ ] **Step 3: 运行确认迁移可加载**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.InvestAgentApplicationTest"`
Expected: PASS（启动上下文含 Flyway V7，无 SQL 语法错误）。

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/resources/db/migration/V7__stock_fundamental.sql backend/src/main/resources/db/seed/valuation-dev-seed.sql
git commit -m "feat(backend): Flyway V7 个股基本面两表 + dev seed"
```

---

### Task 2: 个股估值日快照采集（source + 注册）

**Files:**
- Modify: `collector/collector/sources/plugins.py`（新增 `StockValuationDailySource`）
- Modify: `collector/collector/scheduler/jobs.py`（注册 source）
- Test: `collector/tests/test_sources_stock.py`

**Interfaces:**
- Consumes: `Source`（`collector.sources.base`）、`normalize_date`/`_date_param`（`plugins.py` 既有）。
- Produces: `StockValuationDailySource(source_id, pro_factory)`，`fetch(params) -> pd.DataFrame`，列 `[stock_code, stock_name, pe_ttm, pb, dividend_yield, total_mv, circ_mv, turnover_rate]`；仅沪深正常交易股（剔除 ST/退市/北交所）。

- [ ] **Step 1: 写 source 单测（先红）**

创建 `collector/tests/test_sources_stock.py`：

```python
import datetime as dt

import pandas as pd
import pytest

import collector.sources.plugins as plugins


def _fake_stock_basic():
    return pd.DataFrame({
        "ts_code": ["600519.SH", "000858.SZ", "830000.BJ", "600001.SH"],
        "name": ["贵州茅台", "五粮液", "某北交所", "ST某某"],
    })


def _fake_daily_basic():
    return pd.DataFrame({
        "ts_code": ["600519.SH", "000858.SZ", "830000.BJ", "600001.SH"],
        "pe_ttm": [22.5, 18.2, 10.0, 5.0],
        "pb": [7.8, 4.5, 1.0, 0.5],
        "dv_ttm": [2.1, 2.8, 0.5, 0.0],
        "total_mv": [210000.0, 58000.0, 1000.0, 500.0],   # 万元
        "circ_mv": [210000.0, 58000.0, 1000.0, 500.0],
        "turnover_rate": [0.35, 0.62, 0.10, 0.05],
    })


class _FakePro:
    def stock_basic(self, list_status=None, fields=None):
        return _fake_stock_basic()

    def daily_basic(self, trade_date=None):
        return _fake_daily_basic()


def test_stock_valuation_daily_filters_st_and_bse(monkeypatch):
    src = plugins.StockValuationDailySource("svd", pro_factory=lambda: _FakePro())
    df = src.fetch({"date": "2026-08-27"})

    codes = set(df["stock_code"])
    assert codes == {"600519", "000858"}          # 剔除北交所 830000 与 ST 600001
    assert list(df.columns) == [
        "stock_code", "stock_name", "pe_ttm", "pb",
        "dividend_yield", "total_mv", "circ_mv", "turnover_rate",
    ]
    # total_mv 万元 → 元
    row = df[df["stock_code"] == "600519"].iloc[0]
    assert row["total_mv"] == pytest.approx(2100000000000.0)
    assert row["stock_name"] == "贵州茅台"
```

- [ ] **Step 2: 运行确认红**

Run: `cd collector && pytest tests/test_sources_stock.py -q`
Expected: FAIL（`StockValuationDailySource` 不存在，AttributeError）。

- [ ] **Step 3: 实现 source**

在 `collector/collector/sources/plugins.py` 末尾追加：

```python
class StockValuationDailySource(Source):
    """全 A 个股估值日快照：tushare daily_basic 按交易日批量 + stock_basic 过滤 ST/退市/北交所。"""

    supports_range = False

    def __init__(self, source_id, pro_factory):
        self.source_id = source_id
        self.pro_factory = pro_factory

    def _valid_universe(self, pro):
        basic = pro.stock_basic(list_status="L", fields="ts_code,name")
        basic = basic[basic["ts_code"].str.endswith((".SH", ".SZ"))]  # 剔除北交所/老三板
        return basic[~basic["name"].str.contains("ST|退", na=False)]

    def fetch(self, params):
        pro = self.pro_factory()
        day = _date_param(params, "date")
        universe = self._valid_universe(pro)
        daily = pro.daily_basic(trade_date=day)
        df = daily.merge(universe, on="ts_code", how="inner")
        df = df.copy()
        df["stock_code"] = df["ts_code"].str.split(".").str[0]
        df["stock_name"] = df["name"]
        df["dividend_yield"] = df["dv_ttm"]
        df["total_mv"] = df["total_mv"] * 10000   # 万元 → 元
        df["circ_mv"] = df["circ_mv"] * 10000
        return df[["stock_code", "stock_name", "pe_ttm", "pb",
                   "dividend_yield", "total_mv", "circ_mv", "turnover_rate"]]
```

- [ ] **Step 4: 运行确认绿**

Run: `cd collector && pytest tests/test_sources_stock.py -q`
Expected: PASS。

- [ ] **Step 5: 注册 source + 提交**

在 `scheduler/jobs.py` 的 `build_registries` 的 `source_reg` plugins 字典中追加：

```python
"stock_valuation_daily": StockValuationDailySource("stock_valuation_daily", pro_factory=pro),
```

并更新 `jobs.py` 顶部 import（从 `collector.sources.plugins` 增补 `StockValuationDailySource`）。

```bash
git add collector/collector/sources/plugins.py collector/collector/scheduler/jobs.py collector/tests/test_sources_stock.py
git commit -m "feat(collector): 个股估值日快照 source（daily_basic + ST/北交所过滤）"
```

---

### Task 3: 个股财务季数据采集（source + 注册）

**Files:**
- Modify: `collector/collector/sources/plugins.py`（新增 `StockFinancialSource` + `_last_n_periods`）
- Modify: `collector/collector/scheduler/jobs.py`（注册 source）
- Test: `collector/tests/test_sources_stock.py`（追加）

**Interfaces:**
- Consumes: `Source`、`_ts_code`/`_date_param`（既有）。
- Produces: `StockFinancialSource(source_id, pro_factory)`，`fetch(params) -> pd.DataFrame`，列 `[report_date, stock_code, roe, roa, gross_margin, debt_to_assets, current_ratio, revenue_yoy, netprofit_yoy]`，回填近 3 年（12 季）。

- [ ] **Step 1: 写 source 单测（先红）**

在 `tests/test_sources_stock.py` 追加：

```python
def test_stock_financial_normalizes_and_backfills(monkeypatch):
    # 用 monkeypatch 替换 _last_n_periods，隔离真实的 12 期逻辑
    periods = ["20260630", "20260331"]
    monkeypatch.setattr(plugins, "_last_n_periods", lambda n: periods)

    def fake_fina(period):
        return pd.DataFrame({
            "ts_code": ["600519.SH", "830000.BJ"],
            "end_date": [period, period],
            "roe": [24.5, 10.0],
            "roa": [18.2, 5.0],
            "grossprofit_margin": [91.2, 20.0],
            "debt_to_assets": [21.3, 40.0],
            "current_ratio": [3.8, 1.5],
            "or_yoy": [16.8, 3.0],
            "netprofit_yoy": [15.2, 2.0],
        })

    class FakePro:
        def fina_indicator(self, period=None):
            return fake_fina(period)

    src = plugins.StockFinancialSource("sf", pro_factory=lambda: FakePro())
    df = src.fetch({})

    assert set(df.columns) == {
        "report_date", "stock_code", "roe", "roa", "gross_margin",
        "debt_to_assets", "current_ratio", "revenue_yoy", "netprofit_yoy",
    }
    assert set(df["stock_code"]) == {"600519"}   # 剔除北交所 830000
    assert list(df["report_date"].unique()) == periods
    assert df.iloc[0]["gross_margin"] == pytest.approx(91.2)
```

- [ ] **Step 2: 运行确认红**

Run: `cd collector && pytest tests/test_sources_stock.py -q`
Expected: FAIL（`StockFinancialSource`/`_last_n_periods` 不存在）。

- [ ] **Step 3: 实现 source**

在 `plugins.py` 末尾追加：

```python
def _last_n_periods(n):
    """最近 n 个季报期末日（YYYYMMDD 升序）：按今天倒推季度边界。"""
    today = dt.date.today()
    periods = []
    for offset in range(n - 1, -1, -1):
        quarter_index = (today.month - 1) // 3 - offset
        y = today.year
        m = quarter_index * 3 + 1
        if m <= 0:
            m += 12
            y -= 1
        period_end = dt.date(y, m + 2, 1) - dt.timedelta(days=1)
        periods.append(period_end.strftime("%Y%m%d"))
    return periods


class StockFinancialSource(Source):
    """全 A 个股财务指标季数据：tushare fina_indicator 按报告期批量，回填近 3 年（12 季）。"""

    supports_range = False

    def __init__(self, source_id, pro_factory, periods=12):
        self.source_id = source_id
        self.pro_factory = pro_factory
        self.periods = periods

    def fetch(self, params):
        pro = self.pro_factory()
        frames = []
        for period in _last_n_periods(self.periods):
            df = pro.fina_indicator(period=period)
            if df is None or df.empty:
                continue
            df = df[df["ts_code"].str.endswith((".SH", ".SZ"))]
            frames.append(df[["ts_code", "end_date", "roe", "roa", "grossprofit_margin",
                              "debt_to_assets", "current_ratio", "or_yoy", "netprofit_yoy"]])
        if not frames:
            return pd.DataFrame(columns=[
                "report_date", "stock_code", "roe", "roa", "gross_margin",
                "debt_to_assets", "current_ratio", "revenue_yoy", "netprofit_yoy"])
        result = pd.concat(frames, ignore_index=True)
        result["stock_code"] = result["ts_code"].str.split(".").str[0]
        result = result.rename(columns={
            "end_date": "report_date",
            "grossprofit_margin": "gross_margin",
            "or_yoy": "revenue_yoy",
        })
        return result[["report_date", "stock_code", "roe", "roa", "gross_margin",
                       "debt_to_assets", "current_ratio", "revenue_yoy", "netprofit_yoy"]]
```

- [ ] **Step 4: 运行确认绿**

Run: `cd collector && pytest tests/test_sources_stock.py -q`
Expected: PASS。

- [ ] **Step 5: 注册 source + 提交**

在 `build_registries` 的 plugins 字典追加：

```python
"stock_financial": StockFinancialSource("stock_financial", pro_factory=pro),
```

更新 `jobs.py` import 增补 `StockFinancialSource`。

```bash
git add collector/collector/sources/plugins.py collector/collector/scheduler/jobs.py collector/tests/test_sources_stock.py
git commit -m "feat(collector): 个股财务季数据 source（fina_indicator 近3年回填）"
```

---

### Task 4: 写入层 upsert（两张新表）

**Files:**
- Modify: `collector/collector/store/writer.py`
- Test: `collector/tests/test_writer_stock.py`

**Interfaces:**
- Consumes: `Store.upsert(conn, table, records)` 既有入口、`TABLE_COLUMNS`/`UPSERT_SQL` 映射。
- Produces: 两张新表可被 `Store` 幂等 upsert；P2 读侧消费其列。

- [ ] **Step 1: 写写入层集成测试（先红）**

创建 `collector/tests/test_writer_stock.py`：

```python
import datetime as dt
import os

import psycopg
import pytest

from collector.store.writer import Store

DB = os.environ.get("DATABASE_URL")


@pytest.mark.skipif(not DB, reason="需要 DATABASE_URL")
def test_upsert_stock_valuation_daily_idempotent():
    day = dt.date(2026, 8, 27)
    row = {"trading_day": day, "stock_code": "600519", "stock_name": "贵州茅台",
           "pe_ttm": 22.5, "pb": 7.8, "dividend_yield": 2.1,
           "total_mv": 2100000000000.0, "circ_mv": 2100000000000.0, "turnover_rate": 0.35}
    store = Store()
    with psycopg.connect(DB) as conn:
        store.upsert(conn, "stock_valuation_daily", [row])
        store.upsert(conn, "stock_valuation_daily", [row])  # 幂等
        with conn.cursor() as cur:
            cur.execute("SELECT count(*) FROM stock_valuation_daily WHERE trading_day=%s AND stock_code=%s",
                        (day, "600519"))
            assert cur.fetchone()[0] == 1


@pytest.mark.skipif(not DB, reason="需要 DATABASE_URL")
def test_upsert_stock_financial_idempotent():
    row = {"report_date": dt.date(2026, 6, 30), "stock_code": "600519",
           "roe": 24.5, "roa": 18.2, "gross_margin": 91.2,
           "debt_to_assets": 21.3, "current_ratio": 3.8,
           "revenue_yoy": 16.8, "netprofit_yoy": 15.2}
    store = Store()
    with psycopg.connect(DB) as conn:
        store.upsert(conn, "stock_financial", [row])
        store.upsert(conn, "stock_financial", [row])
        with conn.cursor() as cur:
            cur.execute("SELECT count(*) FROM stock_financial WHERE report_date=%s AND stock_code=%s",
                        (dt.date(2026, 6, 30), "600519"))
            assert cur.fetchone()[0] == 1
```

- [ ] **Step 2: 运行确认红**

Run: `cd collector && DATABASE_URL=postgresql://portfolio:portfolio@localhost:5432/portfolio pytest tests/test_writer_stock.py -q`
Expected: FAIL（`Store` 无这两张表的映射，KeyError「未知目标表」）；若未起 PG 则 skip（先 `docker compose up -d db`）。

- [ ] **Step 3: 实现 upsert**

在 `writer.py` 的 `UPSERT_SQL` 追加：

```python
"stock_valuation_daily": """
    INSERT INTO stock_valuation_daily (trading_day, stock_code, stock_name, pe_ttm, pb, dividend_yield, total_mv, circ_mv, turnover_rate)
    VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
    ON CONFLICT (trading_day, stock_code) DO UPDATE SET
      stock_name=EXCLUDED.stock_name, pe_ttm=EXCLUDED.pe_ttm, pb=EXCLUDED.pb,
      dividend_yield=EXCLUDED.dividend_yield, total_mv=EXCLUDED.total_mv,
      circ_mv=EXCLUDED.circ_mv, turnover_rate=EXCLUDED.turnover_rate
""",
"stock_financial": """
    INSERT INTO stock_financial (report_date, stock_code, roe, roa, gross_margin, debt_to_assets, current_ratio, revenue_yoy, netprofit_yoy)
    VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
    ON CONFLICT (report_date, stock_code) DO UPDATE SET
      roe=EXCLUDED.roe, roa=EXCLUDED.roa, gross_margin=EXCLUDED.gross_margin,
      debt_to_assets=EXCLUDED.debt_to_assets, current_ratio=EXCLUDED.current_ratio,
      revenue_yoy=EXCLUDED.revenue_yoy, netprofit_yoy=EXCLUDED.netprofit_yoy
""",
```

在 `TABLE_COLUMNS` 追加：

```python
"stock_valuation_daily": ["trading_day", "stock_code", "stock_name", "pe_ttm", "pb",
                          "dividend_yield", "total_mv", "circ_mv", "turnover_rate"],
"stock_financial": ["report_date", "stock_code", "roe", "roa", "gross_margin",
                    "debt_to_assets", "current_ratio", "revenue_yoy", "netprofit_yoy"],
```

- [ ] **Step 4: 运行确认绿**

Run: `cd collector && DATABASE_URL=postgresql://portfolio:portfolio@localhost:5432/portfolio pytest tests/test_writer_stock.py -q`
Expected: PASS（幂等 upsert 后 count=1）。

- [ ] **Step 5: 提交**

```bash
git add collector/collector/store/writer.py collector/tests/test_writer_stock.py
git commit -m "feat(collector): 个股基本面两表幂等 upsert 写入"
```

---

### Task 5: 任务 YAML + 转换器注册

**Files:**
- Create: `collector/tasks/stock_valuation_daily.yaml`
- Create: `collector/tasks/stock_financial.yaml`
- Modify: `collector/collector/scheduler/jobs.py`（`_field_columns()` 加两个转换器）

**Interfaces:**
- Consumes: Task 2/3 的 source、Task 4 的 writer 目标、`FieldMappingConverter`。
- Produces: 两个可被 `seed_tasks` 同步、`assemble_collector` 装配的任务定义；转换器 `field_mapping_stock_valuation`/`field_mapping_stock_financial`。

- [ ] **Step 1: 写转换器注册 + 任务 YAML**

在 `jobs.py` 的 `_field_columns()` 返回字典中追加：

```python
"field_mapping_stock_valuation": FieldMappingConverter(
    {
        "stock_code": {"from": "stock_code", "type": "str"},
        "stock_name": {"from": "stock_name", "type": "str"},
        "pe_ttm": {"from": "pe_ttm", "type": "numeric"},
        "pb": {"from": "pb", "type": "numeric"},
        "dividend_yield": {"from": "dividend_yield", "type": "numeric"},
        "total_mv": {"from": "total_mv", "type": "numeric"},
        "circ_mv": {"from": "circ_mv", "type": "numeric"},
        "turnover_rate": {"from": "turnover_rate", "type": "numeric"},
    }
),
"field_mapping_stock_financial": FieldMappingConverter(
    {
        "report_date": {"from": "report_date", "type": "str"},
        "stock_code": {"from": "stock_code", "type": "str"},
        "roe": {"from": "roe", "type": "numeric"},
        "roa": {"from": "roa", "type": "numeric"},
        "gross_margin": {"from": "gross_margin", "type": "numeric"},
        "debt_to_assets": {"from": "debt_to_assets", "type": "numeric"},
        "current_ratio": {"from": "current_ratio", "type": "numeric"},
        "revenue_yoy": {"from": "revenue_yoy", "type": "numeric"},
        "netprofit_yoy": {"from": "netprofit_yoy", "type": "numeric"},
    }
),
```

创建 `collector/tasks/stock_valuation_daily.yaml`：

```yaml
task_code: stock_valuation_daily
task_name: 个股估值日快照
source_ids:
  - { source_id: stock_valuation_daily, type: plugin, class: stock_valuation_daily }
converter: field_mapping_stock_valuation
validator:
  - { check: min_rows, value: 1000, level: hard }
target_table: stock_valuation_daily
schedule: { type: cron, cron: "35 15 * * 1-5" }
enabled: true
trading_day_gated: true
retry_max: 3
retry_backoff: exponential
```

创建 `collector/tasks/stock_financial.yaml`：

```yaml
task_code: stock_financial
task_name: 个股财务季数据
source_ids:
  - { source_id: stock_financial, type: plugin, class: stock_financial }
converter: field_mapping_stock_financial
validator:
  - { check: min_rows, value: 1000, level: hard }
target_table: stock_financial
schedule: { type: cron, cron: "40 15 * * 1-5" }
enabled: true
trading_day_gated: false
retry_max: 3
retry_backoff: exponential
```

> 说明：`trading_day` 由 executor 在写库前统一注入（`executor.py` 中 `for r in records: r.setdefault("trading_day", day)`），故 converter **不得**映射 `trading_day`——否则映射出 `trading_day=None` 会被 `setdefault` 视为「键已存在」不覆盖，导致 NOT NULL 违例。`stock_financial` 无 `trading_day` 概念，executor 注入的 `trading_day` 键会被 writer 按 `TABLE_COLUMNS` 忽略，安全。

- [ ] **Step 2: 运行确认任务可装配**

Run: `cd collector && python -m collector.cli list -e`
Expected: 输出含 `stock_valuation_daily  enabled=True  schedule=...` 与 `stock_financial`（需先 `python -m collector.cli seed`）。

- [ ] **Step 3: 提交**

```bash
git add collector/tasks/stock_valuation_daily.yaml collector/tasks/stock_financial.yaml collector/collector/scheduler/jobs.py
git commit -m "feat(collector): 个股基本面任务定义 + 转换器注册"
```

---

### Task 6: 行业 ROE/股息率扩展

**Files:**
- Modify: `collector/collector/sources/plugins.py`（扩展 `IndustryUniverseSource.fetch` 增补 roe/dividend_yield）
- Modify: `collector/collector/calc/snapshot.py`（扩展 `IndustryValuationCalc.compute` 输出 roe/dividend_yield）
- Modify: `collector/collector/scheduler/jobs.py`（`field_mapping_industry` 增补 roe/dividend_yield）
- Modify: `collector/collector/store/writer.py`（`industry_valuation` upsert 增补 roe/dividend_yield）
- Test: `collector/tests/test_calc_industry_fundamental.py`

**Interfaces:**
- Consumes: Task 2/3 写入的 `stock_valuation_daily`（dv_ttm、total_mv）、`stock_financial`（roe）、`shenwan_industry_mapping`。
- Produces: `industry_valuation` 表的 `roe`/`dividend_yield` 列为真实值（市值加权），供 P2 的 `/api/valuation/industries` 与 `/industry` 页消费。

- [ ] **Step 1: 写行业 ROE 加权单测（先红）**

创建 `collector/tests/test_calc_industry_fundamental.py`：

```python
from collector.calc.snapshot import IndustryValuationCalc


def _records():
    return [
        {"industry_code": "801780", "industry_name": "银行", "pe": 5.6, "pb": 0.62,
         "market_cap": 100.0, "roe": 11.8, "dividend_yield": 5.4},
        {"industry_code": "801780", "industry_name": "银行", "pe": 9.8, "pb": 1.05,
         "market_cap": 200.0, "roe": 16.4, "dividend_yield": 3.9},
        {"industry_code": "801120", "industry_name": "食品饮料", "pe": 22.5, "pb": 7.8,
         "market_cap": 300.0, "roe": 24.5, "dividend_yield": 2.1},
    ]


def test_industry_roe_dividend_weighted_by_market_cap():
    rows = IndustryValuationCalc().compute(_records())
    bank = next(r for r in rows if r["industry_code"] == "801780")
    # roe 加权 = (11.8*100 + 16.4*200) / (100+200) = 14.87
    assert round(bank["roe"], 2) == 14.87
    # dividend 加权 = (5.4*100 + 3.9*200) / 300 = 4.40
    assert round(bank["dividend_yield"], 2) == 4.40


def test_industry_roe_null_when_all_missing():
    rows = IndustryValuationCalc().compute([
        {"industry_code": "x", "industry_name": "X", "pe": 10.0, "pb": 1.0,
         "market_cap": 100.0, "roe": None, "dividend_yield": None},
    ])
    assert rows[0]["roe"] is None
    assert rows[0]["dividend_yield"] is None
```

- [ ] **Step 2: 运行确认红**

Run: `cd collector && pytest tests/test_calc_industry_fundamental.py -q`
Expected: FAIL（`compute` 返回的 dict 无 `roe`/`dividend_yield` 键，KeyError）。

- [ ] **Step 3: 实现 calc + source + converter + writer**

`calc/snapshot.py` 的 `IndustryValuationCalc.compute` 替换为：

```python
class IndustryValuationCalc(Calc):
    def compute(self, records):
        grouped = {}
        for r in records:
            if r.get("pe") is None or r["pe"] <= 0 or r.get("market_cap") is None:
                continue
            key = r["industry_code"]
            g = grouped.setdefault(key, {
                "industry_name": r.get("industry_name"),
                "pe": 0.0, "pb": 0.0, "roe": 0.0, "div": 0.0,
                "cap": 0.0, "roe_cap": 0.0, "div_cap": 0.0,
            })
            cap = r["market_cap"]
            g["cap"] += cap
            g["pe"] += r["pe"] * cap
            if r.get("pb") is not None:
                g["pb"] += r["pb"] * cap
            if r.get("roe") is not None:
                g["roe"] += r["roe"] * cap
                g["roe_cap"] += cap
            if r.get("dividend_yield") is not None:
                g["div"] += r["dividend_yield"] * cap
                g["div_cap"] += cap
        rows = []
        for code, g in grouped.items():
            if g["cap"] <= 0:
                continue
            rows.append({
                "industry_code": code,
                "industry_name": g["industry_name"],
                "pe": round(g["pe"] / g["cap"], 4),
                "pb": round(g["pb"] / g["cap"], 4),
                "roe": round(g["roe"] / g["roe_cap"], 4) if g["roe_cap"] > 0 else None,
                "dividend_yield": round(g["div"] / g["div_cap"], 4) if g["div_cap"] > 0 else None,
            })
        return rows
```

`plugins.py` 的 `IndustryUniverseSource.fetch` 在现有 merge 之后追加（读取两张新表的最新值并入）：

```python
        # 追加：读 stock_financial 最新 roe 与 stock_valuation_daily 最新 dividend_yield 并入
        with self.conn_factory() as conn, conn.cursor() as cur:
            cur.execute(
                "SELECT DISTINCT ON (stock_code) stock_code, roe "
                "FROM stock_financial ORDER BY stock_code, report_date DESC")
            fin = pd.DataFrame(cur.fetchall(), columns=["stock_code", "roe"])
            cur.execute(
                "SELECT stock_code, dividend_yield FROM stock_valuation_daily "
                "WHERE trading_day = (SELECT max(trading_day) FROM stock_valuation_daily)")
            val = pd.DataFrame(cur.fetchall(), columns=["stock_code", "dividend_yield"])
        universe = universe.merge(fin, left_on="stock_code", right_on="stock_code", how="left")
        universe = universe.merge(val, left_on="stock_code", right_on="stock_code", how="left")
        return universe
```

> 说明：`IndustryUniverseSource.fetch` 既有末行是 `return universe.merge(mapping, left_on="代码", right_on="stock_code", how="inner")`，其结果同时含 akshare「代码」列（converter 仍按「代码」映射 code）与 mapping 的 `stock_code` 列。实现时先把该 merge 结果赋给一个变量（如 `merged`），再在 `merged` 上按 `stock_code` 两次 left merge 追加 `roe`（stock_financial 最新）与 `dividend_yield`（stock_valuation_daily 最新）；**不要**把 akshare「代码」列改名，否则会破坏 converter 的 `{"from": "代码"}` 映射。

`jobs.py` 的 `field_mapping_industry` 转换器 columns 增补：

```python
"roe": {"from": "roe", "type": "numeric"},
"dividend_yield": {"from": "dividend_yield", "type": "numeric"},
```

`writer.py` 的 `industry_valuation` upsert 替换为：

```python
"industry_valuation": """
    INSERT INTO industry_valuation (trading_day, industry_code, industry_name, pe, pb, roe, dividend_yield)
    VALUES (%s, %s, %s, %s, %s, %s, %s)
    ON CONFLICT (trading_day, industry_code) DO UPDATE SET
      pe=EXCLUDED.pe, pb=EXCLUDED.pb, roe=EXCLUDED.roe, dividend_yield=EXCLUDED.dividend_yield
""",
```

`TABLE_COLUMNS` 的 `industry_valuation` 替换为：

```python
"industry_valuation": ["trading_day", "industry_code", "industry_name", "pe", "pb", "roe", "dividend_yield"],
```

- [ ] **Step 4: 运行确认绿**

Run: `cd collector && pytest tests/test_calc_industry_fundamental.py tests/test_calc.py -q`
Expected: PASS（既有 test_calc.py 若有 industry 断言需同步补 roe/dividend_yield 键）。

- [ ] **Step 5: 提交**

```bash
git add collector/collector/calc/snapshot.py collector/collector/sources/plugins.py collector/collector/scheduler/jobs.py collector/collector/store/writer.py collector/tests/test_calc_industry_fundamental.py
git commit -m "feat(collector): 行业加权 ROE/股息率扩展"
```

---

### Task 7: 全量验证

**Files:**
- Modify: 无（仅回归修复，若有）。

**Interfaces:**
- Consumes: Task 1–6 全部产出。
- Produces: 可交付的个股基本面数据底座。

- [ ] **Step 1: 跑 collector 全量测试 + 覆盖率**

Run: `cd collector && pytest -q --cov=collector --cov-fail-under=80`
Expected: 全绿，覆盖率 ≥ 80%（`make collect-test` 同样通过）。

- [ ] **Step 2: 手工触发端到端验证落库**

Run: `docker compose up -d db && cd backend && ./gradlew flywayMigrate -Dflyway.locations=classpath:db/migration 2>/dev/null || true`（确保 V7 已应用），然后 `cd collector && python -m collector.cli run stock_valuation_daily --force --date 2026-08-27 && python -m collector.cli run stock_financial --force`
Expected: 各打印 rows=N；`psql` 查 `stock_valuation_daily`、`stock_financial` 有行。

- [ ] **Step 3: 提交任何回归修复**

```bash
git add collector
git commit -m "fix(collector): P1 个股基本面回归修复"
```

---

## Self-Review

- **Spec 覆盖**：FR-A1（个股估值日快照）→ Task 2/4/5；FR-A2（个股财务季数据近 3 年回填）→ Task 3/4/5；FR-A3（行业 ROE/股息率扩展）→ Task 6。决策 #11（换手率）、#12（回填 3 年）、#14（剔除 ST/退市/北交所）、#18（dv_ttm）均在 source/口径中落实。
- **占位符扫描**：无 TBD；Task 5 对 `trading_day` 注入机制的「以 executor 实际行为为准」是显式说明，非空缺。
- **类型一致性**：两张表列名在 V7、`TABLE_COLUMNS`、`UPSERT_SQL`、`FieldMappingConverter`、source 返回列五处一致；`industry_valuation` 的 roe/dividend_yield 在 calc 输出、converter、writer 三处一致。
- **跨服务契约**：V7 表名/列类型 = P2 读侧 + 行业扩展的写入 schema；collector 零 DDL，仅 INSERT/UPDATE。
