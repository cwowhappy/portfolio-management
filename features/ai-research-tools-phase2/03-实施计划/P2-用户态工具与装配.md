# MS-12 P2 · 用户态工具与装配 实施计划（F09/F10）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付两个用户态 AI 工具（持仓分析/配置建议）并接入对话装配链（`UserToolkitFactory` per-会话构造注入 userId）与系统提示词。

**Architecture:** 方案 A：新建非 Spring bean 的 `agent/UserInvestTools`（构造注入 userId + Portfolio/Allocation 服务），`UserToolkitFactory.build(userId)` 注册；错误兜底复制 `InvestTools.runBlock/toError` 模式（private 不可共享）；F10 匹配面 = `RiskProfile.recommendedWeights`（保守/平衡/进取三档）对照 `PortfolioApplicationService.allocation` 当前分布。

**Tech Stack:** Spring Boot 4 / agentscope `@Tool` / Mockito inline（mock 具体类，I2 已实证）。

**Spec:** `features/ai-research-tools-phase2/02-设计规格/设计规格说明.md` §3.2（前置：P1 已合入——ChartSpecs 扩展方法可复用，构造器参数依赖 P1 最终形态）。

## Global Constraints

- 同 P1（GWT+DisplayName、B6 错误 JSON、emit 前守卫、`readOnly=true, concurrencySafe=true`、Co-Authored-By 提交尾注）。
- **userId 只经构造器注入，绝不进 `@ToolParam`**（LLM 不可见、不可伪造）。
- 读侧方法签名（已核实）：`PortfolioApplicationService`：`overview(Long)`→`PortfolioOverviewView(totalAssets,totalCost,totalPnl,todayPnl,cashTotal,totalCashDividend,positionCount,groupCount)`（BigDecimal 除 positionCount/groupCount）、`allocation(Long)`→`AssetAllocationView(List<Slice(category:AllocationSliceCategory,marketValue,ratio)>)`、`industryDistribution(Long)`→`IndustryDistributionView(List<Slice(industryName,marketValue,ratio)>)`、`concentration(Long)`→`ConcentrationView(List<Holding(stockCode,stockName,marketValue,ratio)>,top5Ratio)`；`AllocationApplicationService`：`latestAssessment(Long)`→`Optional<AssessmentView(totalScore,profile:RiskProfile,profileName,weights:List<WeightView(assetClass,weight)>,answers,assessedAt)>`、`deviation(Long)`→`DeviationView(List<DeviationSlice(assetClass,targetWeight,actualWeight,deviation)>)`（**无活跃方案返回空列表不抛**）；`AssetClass` 枚举 `STOCK/BOND/GOLD/CASH/REITS` 带 `label()`（股票/债券/黄金/现金/REITs）。

---

### Task 1: ChartSpecs.portfolioPie + allocationDeviationTable

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/agent/chart/ChartSpecs.java`（追加）
- Test: `backend/src/test/java/com/portfolio/invest/agent/chart/ChartSpecsTest.java`（追加）

**Interfaces:**
- Consumes: P1 后的 ChartSpecs（`Pie(int,String,String,String,List<Slice>,String)`、`Slice(String name, Double value)` 既有内部 record）。
- Produces（Task 2/3 消费，签名逐字一致）:
  - `ChartSpec portfolioPie(AssetAllocationView allocation)`
  - `String portfolioSummary(PortfolioOverviewView overview, ConcentrationView concentration, IndustryDistributionView distribution)`
  - `ChartSpec allocationDeviationTable(String profileName, List<WeightView> recommended, AssetAllocationView current)`
  - `String allocationSummary(AssessmentView assessment, DeviationView deviation)`

- [ ] **Step 1: 写失败测试**

```java
    @DisplayName("持仓配置饼图：资产类市值亿 + 单位亿元")
    @Test
    void givenAllocation_whenPortfolioPie_thenSerializesPie() throws Exception {
        var allocation = new com.portfolio.invest.application.portfolio.AssetAllocationView(List.of(
                new com.portfolio.invest.application.portfolio.AssetAllocationView.Slice(
                        com.portfolio.invest.domain.portfolio.AllocationSliceCategory.STOCK,
                        new BigDecimal("1.0E11"), new BigDecimal("0.8")),
                new com.portfolio.invest.application.portfolio.AssetAllocationView.Slice(
                        com.portfolio.invest.domain.portfolio.AllocationSliceCategory.CASH,
                        new BigDecimal("2.5E10"), new BigDecimal("0.2"))));
        String json = mapper.writeValueAsString(ChartSpecs.portfolioPie(allocation));
        assertThat(json).contains("\"type\":\"pie\"").contains("\"unit\":\"亿元\"");
        assertThat(json).contains("\"value\":10000.0").contains("\"value\":2500.0");
    }

    @DisplayName("持仓摘要：总资产/盈亏/集中度/行业分布要点")
    @Test
    void givenOverviewAndConcentration_whenPortfolioSummary_thenContainsKeyPoints() {
        var overview = new com.portfolio.invest.application.portfolio.PortfolioOverviewView(
                new BigDecimal("1.25E11"), new BigDecimal("1.0E11"), new BigDecimal("2.5E10"),
                new BigDecimal("1.0E9"), new BigDecimal("2.5E10"), new BigDecimal("5.0E9"), 5, 2);
        var concentration = new com.portfolio.invest.application.portfolio.ConcentrationView(
                List.of(new com.portfolio.invest.application.portfolio.ConcentrationView.Holding(
                        "600519", "贵州茅台", new BigDecimal("4.0E10"), new BigDecimal("0.32"))),
                new BigDecimal("0.68"));
        var distribution = new com.portfolio.invest.application.portfolio.IndustryDistributionView(
                List.of(new com.portfolio.invest.application.portfolio.IndustryDistributionView.Slice(
                        "白酒", new BigDecimal("6.0E10"), new BigDecimal("0.48"))));
        assertThat(ChartSpecs.portfolioSummary(overview, concentration, distribution))
                .contains("1250.0 亿").contains("5 只持仓").contains("前五大 68.0%")
                .contains("白酒 48.0%");
    }

    @DisplayName("配置对照表：推荐 vs 当前 vs 偏离（百分点）")
    @Test
    void givenRecommendedAndCurrent_whenAllocationDeviationTable_thenSerializesTable() throws Exception {
        var recommended = List.of(
                new com.portfolio.invest.application.allocation.WeightView(AssetClass.STOCK, new BigDecimal("0.6")),
                new com.portfolio.invest.application.allocation.WeightView(AssetClass.CASH, new BigDecimal("0.4")));
        var current = new com.portfolio.invest.application.portfolio.AssetAllocationView(List.of(
                new com.portfolio.invest.application.portfolio.AssetAllocationView.Slice(
                        com.portfolio.invest.domain.portfolio.AllocationSliceCategory.STOCK,
                        new BigDecimal("9.0E10"), new BigDecimal("0.9"))));
        String json = mapper.writeValueAsString(
                ChartSpecs.allocationDeviationTable("平衡", recommended, current));
        assertThat(json).contains("\"type\":\"table\"");
        assertThat(json).contains("股票").contains("60.0").contains("90.0").contains("30.0"); // 偏离 +30pp
        assertThat(json).contains("现金").contains("40.0").contains("0.0").contains("-40.0");
    }

    @DisplayName("配置建议摘要：画像 + 得分 + 活跃方案偏离提示")
    @Test
    void givenAssessmentAndDeviation_whenAllocationSummary_thenContainsProfileAndDeviation() {
        var assessment = new com.portfolio.invest.application.allocation.AssessmentView(
                62, com.portfolio.invest.domain.allocation.RiskProfile.BALANCED, "平衡",
                List.of(), java.util.Map.of(), java.time.Instant.parse("2026-09-01T00:00:00Z"));
        var deviation = new com.portfolio.invest.application.allocation.DeviationView(List.of(
                new com.portfolio.invest.application.allocation.DeviationView.DeviationSlice(
                        AssetClass.STOCK, new BigDecimal("0.6"), new BigDecimal("0.9"),
                        new BigDecimal("0.3"))));
        assertThat(ChartSpecs.allocationSummary(assessment, deviation))
                .contains("平衡").contains("62").contains("股票 +30.0pp");
    }
```

> ⚠ 三个 View 的内层 Slice/Holding 构造是**嵌套 record**，包名以上述 FQCN 为准；若 `AllocationSliceCategory` 的包位置不同（以 `AssetAllocationView.java` import 为准）照真实改写——构造形态不变。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.agent.chart.ChartSpecsTest" --console=plain`
Expected: 编译失败。

- [ ] **Step 3: 实现**（ChartSpecs.java 追加）

```java
    // ===== MS-12（F09/F10）=====

    /** F09 资产配置饼图（资产类市值，亿元）。 */
    public static ChartSpec portfolioPie(AssetAllocationView allocation) {
        List<ChartSpec.Slice> data = allocation.slices().stream()
                .map(s -> new ChartSpec.Slice(s.category().name(),
                        Math.round(s.marketValue().doubleValue() / 1e8 * 10) / 10.0))
                .toList();
        return new ChartSpec.Pie(SPEC_VERSION, "pie", "当前资产配置", "市值（亿元）", data, "亿元");
    }

    public static String portfolioSummary(PortfolioOverviewView overview,
                                          ConcentrationView concentration,
                                          IndustryDistributionView distribution) {
        StringBuilder sb = new StringBuilder("总资产 %.1f 亿（成本 %.1f 亿，浮动盈亏 %.1f 亿），共 %d 只持仓。"
                .formatted(overview.totalAssets().doubleValue() / 1e8,
                        overview.totalCost().doubleValue() / 1e8,
                        overview.totalPnl().doubleValue() / 1e8, overview.positionCount()));
        if (concentration.top5Ratio() != null) {
            sb.append("前五大占比 %.1f%%".formatted(concentration.top5Ratio().doubleValue() * 100));
            concentration.holdings().stream().findFirst().ifPresent(h ->
                    sb.append("，第一大 %s(%s) %.1f%%".formatted(
                            h.stockName(), h.stockCode(), h.ratio().doubleValue() * 100)));
            sb.append("。");
        }
        if (!distribution.slices().isEmpty()) {
            sb.append("行业分布：");
            for (int i = 0; i < Math.min(3, distribution.slices().size()); i++) {
                var s = distribution.slices().get(i);
                sb.append(i == 0 ? "" : "、").append("%s %.1f%%"
                        .formatted(s.industryName(), s.ratio().doubleValue() * 100));
            }
            sb.append("。配置结构饼图见附图。");
        }
        return sb.toString();
    }

    /** F10 推荐配置 vs 当前分布对照表（权重百分点）。 */
    public static ChartSpec allocationDeviationTable(String profileName, List<WeightView> recommended,
                                                    AssetAllocationView current) {
        Map<AssetClass, Double> actual = new LinkedHashMap<>();
        for (var s : current.slices()) {
            // AllocationSliceCategory 与 AssetClass 同名枚举（股票/债券/黄金/现金/REITs）
            actual.put(AssetClass.valueOf(s.category().name()),
                    s.ratio() == null ? null : s.ratio().doubleValue() * 100);
        }
        List<ChartSpec.Column> columns = List.of(
                new ChartSpec.Column("assetClass", "资产类别", null, null),
                new ChartSpec.Column("target", "推荐权重%", "right", null),
                new ChartSpec.Column("actual", "当前权重%", "right", null),
                new ChartSpec.Column("deviation", "偏离pp", "right", null));
        List<Map<String, Object>> rows = new ArrayList<>();
        java.util.EnumSet<AssetClass> seen = java.util.EnumSet.noneOf(AssetClass.class);
        for (WeightView w : recommended) {
            seen.add(w.assetClass());
            double target = w.weight().doubleValue() * 100;
            Double act = actual.get(w.assetClass());
            Map<String, Object> row = new LinkedHashMap<>();
            row.put("assetClass", w.assetClass().label());
            row.put("target", Math.round(target * 10) / 10.0);
            row.put("actual", act == null ? 0.0 : Math.round(act * 10) / 10.0);
            row.put("deviation", Math.round(((act == null ? 0.0 : act) - target) * 10) / 10.0);
            rows.add(row);
        }
        for (var e : actual.entrySet()) {
            if (seen.contains(e.getKey()) || e.getValue() == null) continue;
            Map<String, Object> row = new LinkedHashMap<>();
            row.put("assetClass", e.getKey().label());
            row.put("target", 0.0);
            row.put("actual", Math.round(e.getValue() * 10) / 10.0);
            row.put("deviation", Math.round(e.getValue() * 10) / 10.0);
            rows.add(row);
        }
        return new ChartSpec.Table(SPEC_VERSION, "table", "推荐配置 vs 当前（%s型）".formatted(profileName),
                null, columns, rows, null);
    }

    public static String allocationSummary(AssessmentView assessment, DeviationView deviation) {
        StringBuilder sb = new StringBuilder("你的风险测评结果：%s型（总分 %d），推荐配置见对照表。"
                .formatted(assessment.profileName(), assessment.totalScore()));
        if (!deviation.slices().isEmpty()) {
            sb.append("当前生效方案偏离：");
            boolean first = true;
            for (var s : deviation.slices()) {
                double pp = s.deviation().doubleValue() * 100;
                if (Math.abs(pp) < 0.05) continue;
                sb.append(first ? "" : "、").append("%s %+.1fpp".formatted(s.assetClass().label(), pp));
                first = false;
            }
            sb.append("。");
        } else {
            sb.append("尚无生效配置方案，可到配置页创建。");
        }
        return sb.toString();
    }
```

> 追加 import：`com.portfolio.invest.application.portfolio.AssetAllocationView`、`...PortfolioOverviewView`、`...ConcentrationView`、`...IndustryDistributionView`、`com.portfolio.invest.application.allocation.WeightView`、`...AssessmentView`、`...DeviationView`、`com.portfolio.invest.domain.allocation.AssetClass`。

- [ ] **Step 4: 跑测试确认通过**（Run 同 Step 2）

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/portfolio/invest/agent/chart/ChartSpecs.java \
        backend/src/test/java/com/portfolio/invest/agent/chart/ChartSpecsTest.java
git commit -m "feat(chart): portfolioPie 饼图 + 推荐配置对照表与摘要

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: UserInvestTools.analyze_portfolio（F09）

**Files:**
- Create: `backend/src/main/java/com/portfolio/invest/agent/UserInvestTools.java`
- Test: `backend/src/test/java/com/portfolio/invest/agent/UserInvestToolsTest.java`

**Interfaces:**
- Consumes: Task 1 `portfolioPie/portfolioSummary`；Global Constraints 所列四个读方法。
- Produces: `UserInvestTools(Long userId, PortfolioApplicationService portfolio, AllocationApplicationService allocation, ObjectMapper mapper)` 构造器——Task 4 `UserToolkitFactory` 消费；`@Tool analyze_portfolio`。

- [ ] **Step 1: 写失败测试**

```java
package com.portfolio.invest.agent;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.anyLong;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.portfolio.invest.application.allocation.AllocationApplicationService;
import com.portfolio.invest.application.portfolio.AssetAllocationView;
import com.portfolio.invest.application.portfolio.ConcentrationView;
import com.portfolio.invest.application.portfolio.IndustryDistributionView;
import com.portfolio.invest.application.portfolio.PortfolioApplicationService;
import com.portfolio.invest.application.portfolio.PortfolioOverviewView;
import com.portfolio.invest.domain.portfolio.AllocationSliceCategory;
import io.agentscope.core.message.ToolResultBlock;
import io.agentscope.core.tool.ToolEmitter;
import java.math.BigDecimal;
import java.util.List;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class UserInvestToolsTest {

    private final PortfolioApplicationService portfolio = mock(PortfolioApplicationService.class);
    private final AllocationApplicationService allocation = mock(AllocationApplicationService.class);
    private final UserInvestTools tools = new UserInvestTools(7L, portfolio, allocation, new ObjectMapper());

    @DisplayName("analyze_portfolio：emit 饼图，摘要含总资产/集中度；userId 从构造器传导")
    @Test
    void givenHoldings_whenAnalyzePortfolio_thenEmitsPieAndSummary() throws Exception {
        when(portfolio.overview(7L)).thenReturn(new PortfolioOverviewView(
                new BigDecimal("1.25E11"), new BigDecimal("1.0E11"), new BigDecimal("2.5E10"),
                null, null, null, 5, 2));
        when(portfolio.allocation(7L)).thenReturn(new AssetAllocationView(List.of(
                new AssetAllocationView.Slice(AllocationSliceCategory.STOCK,
                        new BigDecimal("1.0E11"), new BigDecimal("0.8")))));
        when(portfolio.concentration(7L)).thenReturn(new ConcentrationView(List.of(), new BigDecimal("0.68")));
        when(portfolio.industryDistribution(7L)).thenReturn(new IndustryDistributionView(List.of()));
        ToolResultBlock[] emitted = new ToolResultBlock[1];

        ToolResultBlock out = tools.analyzePortfolio(block -> emitted[0] = block);

        assertThat(emitted[0]).isNotNull();
        assertThat(new ObjectMapper().writeValueAsString(emitted[0])).contains("\"type\":\"pie\"");
        assertThat(new ObjectMapper().writeValueAsString(out)).contains("1250.0 亿").contains("68.0%");
        // userId 归属：四个读方法都以 7L 调用（防串号）
        verify(portfolio).overview(7L);
    }

    @DisplayName("空持仓：不 emit，返回引导摘要")
    @Test
    void givenEmptyPortfolio_whenAnalyzePortfolio_thenNoEmitGuidance() throws Exception {
        when(portfolio.overview(7L)).thenReturn(new PortfolioOverviewView(
                BigDecimal.ZERO, BigDecimal.ZERO, BigDecimal.ZERO, null, null, null, 0, 0));
        ToolResultBlock[] emitted = new ToolResultBlock[1];

        ToolResultBlock out = tools.analyzePortfolio(block -> emitted[0] = block);

        assertThat(emitted[0]).isNull();
        assertThat(new ObjectMapper().writeValueAsString(out)).contains("暂无持仓");
    }

    @DisplayName("服务异常：不 emit，返回错误 JSON")
    @Test
    void givenServiceError_whenAnalyzePortfolio_thenErrorJson() throws Exception {
        when(portfolio.overview(anyLong())).thenThrow(new IllegalStateException("db down"));
        ToolResultBlock[] emitted = new ToolResultBlock[1];

        ToolResultBlock out = tools.analyzePortfolio(block -> emitted[0] = block);

        assertThat(emitted[0]).isNull();
        assertThat(new ObjectMapper().writeValueAsString(out)).contains("error");
    }
}
```

> 需补 `import static org.mockito.Mockito.verify;`；`ConcentrationView(List.of(), BigDecimal)` 若构造校验拒绝空 holdings 则改为一条 0 值 Holding（以真实为准）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.agent.UserInvestToolsTest" --console=plain`
Expected: 编译失败。

- [ ] **Step 3: 实现**

```java
package com.portfolio.invest.agent;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.portfolio.invest.agent.chart.ChartSpecs;
import com.portfolio.invest.application.allocation.AllocationApplicationService;
import com.portfolio.invest.application.portfolio.PortfolioApplicationService;
import io.agentscope.core.message.TextBlock;
import io.agentscope.core.message.ToolResultBlock;
import io.agentscope.core.tool.Tool;
import io.agentscope.core.tool.ToolEmitter;
import io.agentscope.core.tool.ToolParam;
import java.util.LinkedHashMap;
import java.util.Map;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * 用户态投研工具（F09/F10）：userId 经 UserToolkitFactory 构造注入，不进 LLM 上下文（@ToolParam 永不出现 userId）。
 * 非 Spring bean（per-会话实例）；错误兜底沿用 InvestTools.runBlock 模式（private 不可共享，此处复制）。
 */
public class UserInvestTools {

    private static final Logger log = LoggerFactory.getLogger(UserInvestTools.class);

    private final Long userId;
    private final PortfolioApplicationService portfolio;
    private final AllocationApplicationService allocation;
    private final ObjectMapper mapper;

    public UserInvestTools(Long userId, PortfolioApplicationService portfolio,
                           AllocationApplicationService allocation, ObjectMapper mapper) {
        this.userId = userId;
        this.portfolio = portfolio;
        this.allocation = allocation;
        this.mapper = mapper;
    }

    @Tool(
            name = "analyze_portfolio",
            description = "读取当前用户持仓组合：总览（总资产/成本/浮动盈亏/持仓数）、资产配置饼图、行业分布、集中度（前五大占比）。用户问「我的持仓/组合怎么样、仓位结构如何」时调用。数据为用户私有，仅本人可见。",
            readOnly = true,
            concurrencySafe = true)
    public ToolResultBlock analyzePortfolio(ToolEmitter emitter) {
        return runBlock(() -> {
            var overview = portfolio.overview(userId);
            if (overview.positionCount() == 0) {
                return ToolResultBlock.text("暂无持仓。可到持仓页添加，或让我介绍怎么开始建立组合。");
            }
            var assetAllocation = portfolio.allocation(userId);
            var concentration = portfolio.concentration(userId);
            var industryDistribution = portfolio.industryDistribution(userId);
            emitter.emit(ToolResultBlock.builder()
                    .output(TextBlock.builder().text(mapper.writeValueAsString(
                            ChartSpecs.portfolioPie(assetAllocation))).build())
                    .build());
            return ToolResultBlock.text(ChartSpecs.portfolioSummary(overview, concentration, industryDistribution));
        });
    }

    /** 与 InvestTools 同款兜底：失败不 emit，返回错误 JSON 文本（前端 ChartCard 嗅探降级）。 */
    private ToolResultBlock runBlock(BlockSupplier supplier) {
        try {
            return supplier.get();
        } catch (Exception e) {
            log.error("用户态工具执行异常 userId={}", userId, e);
            return ToolResultBlock.text(toError("工具执行失败", "请稍后重试"));
        }
    }

    private String toError(String message, String hint) {
        try {
            Map<String, String> body = new LinkedHashMap<>();
            body.put("error", message);
            body.put("hint", hint);
            return mapper.writeValueAsString(body);
        } catch (Exception e) {
            return "{\"error\":\"工具执行失败\",\"hint\":\"请稍后重试\"}";
        }
    }

    @FunctionalInterface
    private interface BlockSupplier {
        ToolResultBlock get() throws Exception;
    }
}
```

- [ ] **Step 4: 跑测试确认通过**（Run 同 Step 2）

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/portfolio/invest/agent/UserInvestTools.java \
        backend/src/test/java/com/portfolio/invest/agent/UserInvestToolsTest.java
git commit -m "feat(agent): UserInvestTools.analyze_portfolio 持仓分析工具（userId 构造注入）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: UserInvestTools.suggest_allocation（F10）

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/agent/UserInvestTools.java`
- Test: `backend/src/test/java/com/portfolio/invest/agent/UserInvestToolsTest.java`（追加）

**Interfaces:**
- Consumes: Task 1 `allocationDeviationTable/allocationSummary`；`latestAssessment/deviation/allocation` 读方法。
- Produces: `@Tool suggest_allocation`。

- [ ] **Step 1: 写失败测试**

```java
    @DisplayName("suggest_allocation 有测评：emit 对照表，摘要含画像与偏离")
    @Test
    void givenAssessment_whenSuggestAllocation_thenEmitsDeviationTable() throws Exception {
        when(allocation.latestAssessment(7L)).thenReturn(java.util.Optional.of(new AssessmentView(
                62, RiskProfile.BALANCED, "平衡",
                List.of(new WeightView(AssetClass.STOCK, new BigDecimal("0.6")),
                        new WeightView(AssetClass.CASH, new BigDecimal("0.4"))),
                java.util.Map.of(), java.time.Instant.parse("2026-09-01T00:00:00Z"))));
        when(portfolio.allocation(7L)).thenReturn(new AssetAllocationView(List.of(
                new AssetAllocationView.Slice(AllocationSliceCategory.STOCK,
                        new BigDecimal("9.0E10"), new BigDecimal("0.9")))));
        when(allocation.deviation(7L)).thenReturn(new DeviationView(List.of()));
        ToolResultBlock[] emitted = new ToolResultBlock[1];

        ToolResultBlock out = tools.suggestAllocation(block -> emitted[0] = block);

        assertThat(emitted[0]).isNotNull();
        assertThat(new ObjectMapper().writeValueAsString(emitted[0])).contains("推荐配置");
        assertThat(new ObjectMapper().writeValueAsString(out)).contains("平衡").contains("62");
    }

    @DisplayName("suggest_allocation 无测评：不 emit，返回先完成测评引导")
    @Test
    void givenNoAssessment_whenSuggestAllocation_thenGuidance() throws Exception {
        when(allocation.latestAssessment(7L)).thenReturn(java.util.Optional.empty());
        ToolResultBlock[] emitted = new ToolResultBlock[1];

        ToolResultBlock out = tools.suggestAllocation(block -> emitted[0] = block);

        assertThat(emitted[0]).isNull();
        assertThat(new ObjectMapper().writeValueAsString(out)).contains("风险测评");
    }
```

> 追加测试 import：`AssessmentView/WeightView/DeviationView`（application.allocation）、`AssetClass/RiskProfile`（domain.allocation）。

- [ ] **Step 2: 跑测试确认失败**（Run 同 Task 2 Step 2；Expected: 编译失败）

- [ ] **Step 3: 实现**（UserInvestTools 追加方法）

```java
    @Tool(
            name = "suggest_allocation",
            description = "资产配置建议：读取用户风险测评结果（无则引导先完成测评），给出推荐配置（保守/平衡/进取三档内置权重）与当前资产分布对照表及偏离。用户问「我该怎么配置/建议仓位/资产怎么配」时调用。推荐权重来自 M07 问卷评分模型，非本工具编造。",
            readOnly = true,
            concurrencySafe = true)
    public ToolResultBlock suggestAllocation(ToolEmitter emitter) {
        return runBlock(() -> {
            var assessment = allocation.latestAssessment(userId);
            if (assessment.isEmpty()) {
                return ToolResultBlock.text("尚未完成风险测评。请先到配置页完成 M07 风险测评问卷，我再基于你的风险画像给出配置建议（不凭空编造建议）。");
            }
            var a = assessment.get();
            var current = portfolio.allocation(userId);
            var deviation = allocation.deviation(userId);
            emitter.emit(ToolResultBlock.builder()
                    .output(TextBlock.builder().text(mapper.writeValueAsString(
                            ChartSpecs.allocationDeviationTable(a.profileName(), a.weights(), current))).build())
                    .build());
            return ToolResultBlock.text(ChartSpecs.allocationSummary(a, deviation));
        });
    }
```

- [ ] **Step 4: 跑测试确认通过**（Run 同前）

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/portfolio/invest/agent/UserInvestTools.java \
        backend/src/test/java/com/portfolio/invest/agent/UserInvestToolsTest.java
git commit -m "feat(agent): suggest_allocation 配置建议工具（测评推荐权重对照当前分布）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: UserToolkitFactory 装配 + InvestSystemPrompt 提示词

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/agent/UserToolkitFactory.java`（构造器 + build 追加注册）
- Modify: `backend/src/main/java/com/portfolio/invest/agent/InvestSystemPrompt.java`（工具规范追加）
- Test: `backend/src/test/java/com/portfolio/invest/agent/UserToolkitFactoryTest.java`（追加断言）
- Test: `backend/src/test/java/com/portfolio/invest/agent/InvestSystemPromptTest.java`（追加断言）

**Interfaces:**
- Consumes: P1 Task 5/6/7 后的 `InvestTools`（注册不变）、Task 2/3 `UserInvestTools` 构造器。
- Produces: `build(userId)` 返回的 Toolkit 含 `search_stock…get_valuation` + `screen_stocks/analyze_financials/analyze_industry` + `analyze_portfolio/suggest_allocation` 共 **12 个内置工具**（7+5；P3 前端标签与 e2e 依赖）。

- [ ] **Step 1: 写失败测试**

`UserToolkitFactoryTest` 追加（**注意：构造器加参后该文件 6 处 `new UserToolkitFactory(investTools, repository, clientPool)` 调用——行 59/123/149/176/197/226——须全部同步加 `portfolioService, allocationService, mapper` 三个参数**，新增 `@Mock PortfolioApplicationService portfolioService`、`@Mock AllocationApplicationService allocationService` 字段与共享 `ObjectMapper mapper = new ObjectMapper()`）：

```java
    @DisplayName("装配后内置工具含 5 新工具（无 MCP 环境共 12 个）")
    @Test
    void givenInvestToolsAndUserServices_whenBuild_thenRegistersUserTools() {
        Toolkit toolkit = new UserToolkitFactory(investTools, repository, clientPool,
                portfolioService, allocationService, new com.fasterxml.jackson.databind.ObjectMapper())
                .build(1L);
        var names = toolkit.getToolNames();
        assertThat(names).contains("screen_stocks", "analyze_financials", "analyze_industry",
                "analyze_portfolio", "suggest_allocation");
        assertThat(names).hasSize(12);   // 7 既有 + 5 新
    }
```

`InvestSystemPromptTest` 追加（提示词为静态常量 `InvestSystemPrompt.TEXT`——既有断言 `assertThat(InvestSystemPrompt.TEXT)` 同款）：

```java
    @DisplayName("工具规范含五个新工具映射与私有数据口径")
    @Test
    void whenPromptText_thenContainsNewToolGuidance() {
        assertThat(InvestSystemPrompt.TEXT)
                .contains("screen_stocks").contains("analyze_portfolio")
                .contains("suggest_allocation").contains("analyze_financials")
                .contains("analyze_industry")
                .contains("私有").contains("MCP");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.agent.UserToolkitFactoryTest" --tests "com.portfolio.invest.agent.InvestSystemPromptTest" --console=plain`
Expected: 新断言失败。

- [ ] **Step 3: 实现**

`UserToolkitFactory`：

```java
    // 字段与构造器追加：
    private final PortfolioApplicationService portfolioService;
    private final AllocationApplicationService allocationService;
    private final ObjectMapper mapper;
    // 构造器追加三个参数并赋值

    public Toolkit build(Long userId) {
        Toolkit toolkit = new Toolkit();
        toolkit.registerTool(investTools);
        toolkit.registerTool(new UserInvestTools(userId, portfolioService, allocationService, mapper)); // 用户态工具
        // ……既有 MCP 装配不动
```

`InvestSystemPrompt` 工具使用规范（既有 5 条后追加）：

```
6. 选股/筛选（如「找 ROE>15% 且 PE<20 的股票」）用 screen_stocks（至少一个条件）；结果优先报命中数与头部标的
7. 用户问自己的持仓/组合/仓位结构时用 analyze_portfolio（私有数据，仅本人会话可用）
8. 用户问怎么配置资产/建议仓位时用 suggest_allocation；无测评时引导先完成，不虚构建议
9. 用户问某只股票财报/赚钱能力拆解时用 analyze_financials（名称先 search_stock 换码）
10. 用户问行业（如「银行怎么样/哪些行业便宜」）用 analyze_industry：先无参看板面对齐行业码，再带参下钻头部企业
11. analyze_portfolio/suggest_allocation 返回的用户私有数据，禁止作为参数传给任何 MCP 工具
```

- [ ] **Step 4: 跑测试确认通过**

Run: 同 Step 2；随后 `cd backend && ./gradlew check --console=plain`（ArchUnit：agent→application 依赖白名单内，零改动预期通过）。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/portfolio/invest/agent/UserToolkitFactory.java \
        backend/src/main/java/com/portfolio/invest/agent/InvestSystemPrompt.java \
        backend/src/test/java/com/portfolio/invest/agent/UserToolkitFactoryTest.java \
        backend/src/test/java/com/portfolio/invest/agent/InvestSystemPromptTest.java
git commit -m "feat(agent): 用户态工具装配（build 注册 UserInvestTools）+ 提示词五工具映射与私有数据口径

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## P2 完成门槛

- `cd backend && ./gradlew check --console=plain` exit 0；
- `UserToolkitFactoryTest` 断言 14 内置工具名齐全（P3 前端标签的契约面）。
