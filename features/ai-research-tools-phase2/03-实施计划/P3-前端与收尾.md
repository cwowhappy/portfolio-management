# MS-12 P3 · 前端与收尾 实施计划（标签/e2e/smoke/文档）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 工具卡标签与渲染打通前端验证链路（vitest → 真浏览器 e2e → `make smoke`），并执行文档同步清单。

**Architecture:** 前端零新组件——`ToolCallCard` 两处标签映射 +5；表格/饼图走 ChartCard 既有渲染通道（P1/P2 的 spec 类型是既有 table/pie 变体）。e2e 沿 chat.spec 双门控范式（DEEPSEEK_API_KEY + 种子管理员）；smoke §5 扩 5 工具提问。

**Tech Stack:** Next.js / vitest + RTL / Playwright / bash smoke。

**Spec:** `features/ai-research-tools-phase2/02-设计规格/设计规格说明.md` §四/§六/§七（前置：P1/P2 已合入，12 个内置工具名已注册）。

## Global Constraints

- 工具名 → 中文标签固定映射（P2 产出）：`screen_stocks`→筛选选股、`analyze_portfolio`→持仓分析、`suggest_allocation`→配置建议、`analyze_financials`→财报解读、`analyze_industry`→行业分析。
- 定向测试命令（仓库陷阱）：前端单测用 `cd frontend && pnpm vitest run tests/<file>`；e2e 用 `npx playwright test <spec>`；**勿用** `pnpm test -- <file>` / `pnpm test:e2e -- <spec>`（过滤失效实跑全套）。
- e2e 组件改动后必须 `E2E_FRESH_BUILD=1` 净构建（memory 既有教训）；smoke 依赖 `DEEPSEEK_API_KEY` + `ADMIN_USERNAME/ADMIN_PASSWORD`（缺省跳过段落，既有门控不动）。
- 引用计数一律 git 实证（conventions 02 G9）；交付文档数字用 `git diff` 数测试方法核对。
- 提交信息末尾加 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。

---

### Task 1: 工具卡标签 + 渲染器注册（标签/Zod schema/useRenderTool 三件套）

**Files:**
- Modify: `frontend/components/chat/ToolCallCard.tsx:5`（`TOOL_LABELS`）与 `:17` 附近第二映射表（建议问句表——两处都加同样五个键）
- Modify: `frontend/lib/tool-params.ts`（追加 5 个 Zod schema）
- Modify: `frontend/components/chat/toolRenderers.tsx`（`ChartToolRenderers` 内追加 5 个 `useRenderTool`）
- Test: Create `frontend/tests/chat/ToolCallCard.test.tsx`

**Interfaces:**
- Consumes: P2 注册的 5 个工具名。**已核实的机制**（`toolRenderers.tsx` 头注释 + `ChartCard.tsx:33`）：图表卡按工具名经 `useRenderTool({ name, parameters: ZodSchema, render })` 注册，`parameters` 必填（空参工具 `z.object({})`——`ValuationParamsSchema` 先例）；table 变体不传 builder（ChartCard 内接 DataTable）；pie 传 `buildPieOption`（`optionBuilders.ts:22` 既有）。
- Produces: 五个工具的进度卡中文标签 + 图表卡渲染（e2e Task 2 断言依赖「筛选选股」等文案与图表出现）。

- [ ] **Step 1: 写失败测试**

```tsx
import { afterEach, describe, expect, it } from "vitest";
import { cleanup, render, screen } from "@testing-library/react";
import ToolCallCard from "@/components/chat/ToolCallCard";

afterEach(() => cleanup());

describe("ToolCallCard MS-12 标签", () => {
  it.each([
    ["screen_stocks", "筛选选股"],
    ["analyze_portfolio", "持仓分析"],
    ["suggest_allocation", "配置建议"],
    ["analyze_financials", "财报解读"],
    ["analyze_industry", "行业分析"],
  ])("%s 显示中文标签 %s", (tool, label) => {
    render(<ToolCallCard toolCallId="tc1" toolName={tool} status="executing" />);
    expect(screen.getByText(label)).toBeTruthy();
  });
});
```

> props 以 `ToolCallCard.tsx:47-65` 为准：`toolCallId`/`toolName`/`status?: "inProgress" | "executing" | "complete"`（`parameters`/`result` 可选）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run tests/chat/ToolCallCard.test.tsx`
Expected: FAIL（标签缺失，显示原始工具名）。

- [ ] **Step 3: 实现**

`ToolCallCard.tsx` 两处映射各追加：

```ts
  screen_stocks: "筛选选股",
  analyze_portfolio: "持仓分析",
  suggest_allocation: "配置建议",
  analyze_financials: "财报解读",
  analyze_industry: "行业分析",
```

`lib/tool-params.ts` 追加（与后端 `@ToolParam` 对齐）：

```ts
export const ScreeningParamsSchema = z.object({
  peTtmMax: z.number().optional(), pbMax: z.number().optional(),
  dividendYieldMin: z.number().optional(), roeMin: z.number().optional(),
  roaMin: z.number().optional(), grossMarginMin: z.number().optional(),
  debtToAssetsMax: z.number().optional(), currentRatioMin: z.number().optional(),
  revenueYoyMin: z.number().optional(), netprofitYoyMin: z.number().optional(),
  totalMvMin: z.number().optional(), turnoverRateMin: z.number().optional(),
  industryCode: z.string().optional(), indexCode: z.string().optional(),
  sortBy: z.string().optional(), sortDirection: z.string().optional(),
  limit: z.number().optional(),
});
export const FinancialsTrendParamsSchema = z.object({
  code: z.string().describe("6位A股代码"),
});
export const IndustryParamsSchema = z.object({
  industryCode: z.string().optional().describe("申万一级行业码，空=全行业板面"),
});
export const PortfolioParamsSchema = z.object({});
export const AllocationParamsSchema = z.object({});
```

`toolRenderers.tsx` 的 `ChartToolRenderers` 内追加（import 补 `buildPieOption` 与 5 个 schema；4 个 table 工具不传 builder，同 `get_financials` 分支）：

```tsx
  useRenderTool({
    name: "screen_stocks",
    parameters: ScreeningParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} />,
  });
  useRenderTool({
    name: "analyze_financials",
    parameters: FinancialsTrendParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} />,
  });
  useRenderTool({
    name: "analyze_industry",
    parameters: IndustryParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} />,
  });
  useRenderTool({
    name: "suggest_allocation",
    parameters: AllocationParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} />,
  });
  useRenderTool({
    name: "analyze_portfolio",
    parameters: PortfolioParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} builder={buildPieOption as ChartCardBuilder} />,
  });
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `cd frontend && pnpm vitest run tests/chat/ToolCallCard.test.tsx && pnpm vitest run`
Expected: 5 passed + 全量全绿（`tests/chat/toolRenderers.test.tsx` 既有渲染器测试不得回归）。

- [ ] **Step 5: 提交**

```bash
git add frontend/components/chat/ToolCallCard.tsx frontend/lib/tool-params.ts \
        frontend/components/chat/toolRenderers.tsx frontend/tests/chat/ToolCallCard.test.tsx
git commit -m "feat(chat): 五工具中文标签 + 图表卡渲染注册（schema/渲染器三件套）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: e2e chat 工具链路（真浏览器，双门控）

**Files:**
- Create: `frontend/e2e/chat-tools.spec.ts`

**Interfaces:**
- Consumes: Task 1 标签文案；chat.spec 既有范式（`registerAndApprove`/`uniqueUsername`/`TEST_PASSWORD`，发送按钮 30s 就绪、停止按钮 180s 隐藏——`e2e/chat.spec.ts:30-48`）。
- Produces: MS-12 验收「纳入工具进度卡片」的真浏览器证据。

- [ ] **Step 1: 写用例**

```ts
import { test, expect } from "@playwright/test";
import { registerAndApprove, TEST_PASSWORD, uniqueUsername } from "./helpers";

const hasAdminSeed = !!(process.env.ADMIN_USERNAME && process.env.ADMIN_PASSWORD);

// MS-12：5 个新工具纳入对话链路（真实 LLM，双门控同 chat.spec）
test.describe("AI 工具二期", () => {
  test.skip(!process.env.DEEPSEEK_API_KEY, "未配置 DEEPSEEK_API_KEY，跳过真实对话");
  test.skip(!hasAdminSeed, "未配置 ADMIN_USERNAME/ADMIN_PASSWORD，跳过");

  test("筛选工具：提问选股 → 工具卡出现「筛选选股」", async ({ page }) => {
    await registerAndApprove(page, uniqueUsername("mst"), TEST_PASSWORD);
    const input = page.getByPlaceholder(/问行情、看走势、读财报/);
    await input.fill("帮我筛选 ROE 大于 15% 且 PE 小于 20 的股票");
    const sendBtn = page.getByRole("button", { name: "发送" });
    await expect(sendBtn).toBeEnabled({ timeout: 30_000 });
    await sendBtn.click();
    await expect(page.getByText("筛选选股").first()).toBeVisible({ timeout: 120_000 });
    await expect(page.getByRole("button", { name: "■ 停止" })).toBeHidden({ timeout: 180_000 });
  });

  test("持仓工具：空持仓用户问组合 → 持仓分析卡 + 引导回复", async ({ page }) => {
    await registerAndApprove(page, uniqueUsername("msp"), TEST_PASSWORD);
    const input = page.getByPlaceholder(/问行情、看走势、读财报/);
    await input.fill("帮我分析一下我的持仓组合怎么样");
    const sendBtn = page.getByRole("button", { name: "发送" });
    await expect(sendBtn).toBeEnabled({ timeout: 30_000 });
    await sendBtn.click();
    await expect(page.getByText("持仓分析").first()).toBeVisible({ timeout: 120_000 });
    await expect(page.getByRole("button", { name: "■ 停止" })).toBeHidden({ timeout: 180_000 });
  });
});
```

- [ ] **Step 2: 跑用例**

Run: `cd frontend && E2E_FRESH_BUILD=1 npx playwright test chat-tools.spec`
Expected: 2 passed（真 LLM；若 LLM 偶发未调工具，检查提示词条目是否生效——Task P2-4 的回归信号）。

- [ ] **Step 3: 全量 e2e 回归**

Run: `cd frontend && E2E_FRESH_BUILD=1 npx playwright test`
Expected: 既有 48 + 新 2 = 50 全绿（门控 skipped 除外）。

- [ ] **Step 4: 提交**

```bash
git add frontend/e2e/chat-tools.spec.ts
git commit -m "test(chat): e2e 五工具链路真浏览器用例（筛选/持仓两旅程，双门控）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: make smoke 覆盖 5 工具链路

**Files:**
- Modify: `scripts/smoke.sh`（§5「AI 对话」段内追加）

**Interfaces:**
- Consumes: §5 既有门控（`DEEPSEEK_API_KEY` + 管理员登录 COOKIE_JAR）；AG-UI 事件名 `TOOL_CALL_START`/`TEXT_MESSAGE`（`frontend/tests/agui-stream.test.ts:64-70` 已钉）。
- Produces: 产品落地计划 MS-12 验收第三条「make smoke 覆盖新工具链路」。

- [ ] **Step 1: 实现**（§5 段内、既有「用一句话介绍你自己」断言之后追加；复用同一 COOKIE_JAR，5 问各起 runId）

```bash
  # MS-12：5 个新工具链路（明确指令降低模型不调工具的偶发；断言工具调用事件）
  tool_smoke() {
    local run_id="$1" question="$2" label="$3"
    local resp
    resp=$(curl -s --max-time 120 -b "$COOKIE_JAR" -X POST "$BASE/agui/run" \
      -H "Content-Type: application/json" \
      -d "{\"threadId\":\"smoke-ms12\",\"runId\":\"$run_id\",\"messages\":[{\"id\":\"m-$run_id\",\"role\":\"user\",\"content\":\"$question\"}],\"state\":{},\"tools\":[]}")
    echo "$resp" | grep -q "TOOL_CALL_START" && pass "$label 工具调用" || fail "$label 未观察到工具调用: $(echo "$resp" | head -c 200)"
    echo "$resp" | grep -q "TEXT_MESSAGE" && pass "$label 文本回答" || fail "$label 无文本回答"
  }
  tool_smoke smoke-ms12-1 "请调用筛选工具：帮我筛选 ROE 大于 15% 且 PE 小于 20 的股票" "筛选"
  tool_smoke smoke-ms12-2 "请调用持仓分析工具分析我的持仓组合" "持仓分析"
  tool_smoke smoke-ms12-3 "请调用配置建议工具给我资产配置建议" "配置建议"
  tool_smoke smoke-ms12-4 "请调用财报解读工具分析 600519 的财报" "财报解读"
  tool_smoke smoke-ms12-5 "请调用行业分析工具看看银行行业" "行业分析"
```

- [ ] **Step 2: 实跑验证**

Run: `cd /Users/lixiaoyi/GitRepository/portfolio-management && make smoke`
Expected: 含「筛选 工具调用 … pass」等 10 行（5 工具 × 2 断言）+ 既有段落全 pass，`echo "全部通过"` 出现。（需本地 `.env` 有 DEEPSEEK_API_KEY；若门控跳过则 CI 首跑补验，PR 描述留痕。）

- [ ] **Step 3: 提交**

```bash
git add scripts/smoke.sh
git commit -m "test(smoke): AI 对话段覆盖 MS-12 五工具链路（TOOL_CALL_START 断言）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: 文档同步清单（设计规格 §七）

**Files:**
- Modify: `docs/function/modules/05-AI投研能力.md`（F08~F12 置 ✅ + 落地说明；模块状态 9/14 → 14/14；F12 融资动态口径调整留痕）
- Modify: `docs/function/00-功能模块概览.md`（M05 行 + 基线统计）
- Modify: `docs/plans/2026-08-27-产品落地计划.md`（MS-12 节 ⏳→✅ + 交付说明；基线表 63→68/115；变更记录行）
- Modify: `features/README.md`（索引行「已交付 2026-09-XX + PR #N」——PR 号合并后回填）

**Interfaces:**
- Consumes: 真实 PR 号（合并后回填，先审发后回填先例 `29fdae7`）；测试计数以 `git diff main --stat` + `git log -S` 实证（G9）。
- Produces: 交付文档一致性（终审检查项）。

- [ ] **Step 1: 更新四份文档**（照 MS-11 交付批次 `d2e480a` 同款写法：功能点 ✅ + 一段落地说明；落地说明须包含：5 工具名与形态、装配方式、F12 口径调整、smoke 覆盖、验证数据——数字全部 git 实证后填）。

- [ ] **Step 2: 核对与实证**

Run: `git log main..HEAD --oneline | wc -l`；`git diff main --stat -- 'backend/src/test' 'frontend/tests' | tail -3`；`grep -c "@Test" <新增测试文件>` —— 交付文档中的「新增 N 用例」须与上述输出一致（G9）。

- [ ] **Step 3: 提交**

```bash
git add docs/function/modules/05-AI投研能力.md docs/function/00-功能模块概览.md \
        docs/plans/2026-08-27-产品落地计划.md features/README.md
git commit -m "docs(ai-tools): MS-12 交付文档同步（模块/概览/落地计划/索引）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## P3 完成门槛（= MS-12 整体验证门槛）

- `make test` 三端全绿（后端 JaCoCo ≥80%、前端 V8、collector 零改动）；
- `make test-e2e`（`E2E_FRESH_BUILD=1`）全绿：既有 48 + 新 2；
- `make smoke` 含 5 工具段且全 pass（key 门控内）；
- 文档同步四件套完成、PR 号回填；
- `features/ai-research-tools-phase2/08-复盘总结/复盘总结.md` 在分支终审后回填（含偏差清单——独立任务，不在本计划内）。
