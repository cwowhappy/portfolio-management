# 契约与渲染骨架（toolMessage 配对 + ChartCard） 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 renderToolCall 未配对 toolMessage 的现状缺陷（FR-7），建立聊天流图表渲染骨架：ChartSpec 契约补全（candlestick/table 变体）→ K 线 optionBuilder → ChartCard 状态机壳 → 按工具名注册三个图表渲染器（mock 数据先行，后端双通道在 P3）。

**Architecture:** `ThreadArea` 的 AssistantMessage（memo）增加 `toolMessageByCallId` Map 配对（官方 `CopilotChatToolField` 范式，自定义 memo 比较器保住流式期间的 memo 隔离）；`lib/chart-spec.ts` 扩为五变体判别联合（前端权威）；`components/charts/optionBuilders.ts` 追加 `buildCandlestickOption`；新组件 `components/chat/charts/ChartCard.tsx`（inProgress 骨架 / complete 解析渲染 / 错误与解析失败降级折叠卡）；`components/chat/toolRenderers.tsx` 用 `useRenderTool` 注册 `get_kline`/`get_valuation`/`get_market_overview`（精确名压过通配，其余工具继续 ToolCallCard 兜底）。`get_financials` 的 DataTable 在 P4，本期 table 变体走降级折叠卡。

**Tech Stack:** zod 3.25（实现 Standard Schema v1，满足 useRenderTool 的 `parameters` 类型）· echarts 6 按需（P1 已装）· vitest 3 + @testing-library/react（jsdom 无 canvas——EChart 在组件测试中一律 mock，逻辑测试集中在 builders 纯函数）

**Spec:** `features/chat-rich-content/02-design/设计规格说明.md`（§一 前置修复、§三 前端设计）与 `docs/technology/research/05-富文本场景技术方案.md`（§2.1 契约、§4.1-§4.6）。执行者须先读两份 Spec 的对应节。

## Global Constraints

- 需求映射（2026-09-11 澄清）：`get_kline` → candlestick、`get_valuation` → line（全A PE/PB 中位数双系列）、`get_market_overview` → bar（仅涨跌幅）；`get_financials` 本期不注册（P4）。渲染器**不传 agentId**（05 §4.1：headless 查询值恒 "default"，无 agentId 的具名渲染器仍压过通配；`useInterrupt` 必须显式传 agentId 的教训不适用于纯渲染 hook，见 ThreadArea.tsx:391 注释）。
- K 线数据顺序 **[开, 收, 低, 高]**（ECharts 官方约定）；MA 线数据允许 null（预热期缺口，与 dates 对齐）。
- 图表色值来自 `getPalette()` 运行时解析（`[data-theme="light"]` 覆盖，非 `.light` 类）；不得在 option 里写 `var(--x)`。
- 卡片外壳复用 `.tool-card` 类（globals.css:211-230，已有 `.running` 态）。
- 后端错误内容嗅探沿用 ToolCallCard 模式（`/error/i`）并收紧为 `/tool execution failed|"error":/i`（避免误伤正常 JSON 内容）。
- 所有命令在 `frontend/` 下执行；测试命令 `pnpm test` = `vitest run --coverage`（跑单文件用 `pnpm vitest run <path>`）；coverage 阈值 80%（components/**、lib/**）。
- 提交信息用 conventional + 中文描述，一个 Task 一个 commit。
- 既有 mock 边界（ThreadArea.test.tsx:10-43 的 `vi.mock("@copilotkit/react-core/v2")`）扩展时不得破坏既有用例。

---

### Task 1: FR-7 toolMessage 配对修复

**Files:**
- Modify: `frontend/components/chat/ThreadArea.tsx`（AssistantMessage 定义 ~198-241、消息渲染处 ~563、useAgent ~378-383）
- Test: `frontend/tests/ThreadArea.test.tsx`（追加用例）

**Interfaces:**
- Produces: AssistantMessage 新增 prop `toolMessageByCallId: Map<string, ToolMessage>`（`ToolMessage = Extract<Message, { role: "tool" }>`）；`renderToolCall({ toolCall, toolMessage })` 配对调用。Task 6 的渲染器依赖 complete 态的 `result`（toolMessage.content）。

- [ ] **Step 1: 追加失败测试（配对传递 + memo 比较器）**

在 `frontend/tests/ThreadArea.test.tsx` 追加（沿用既有 `mocks` 与渲染 harness；工具调用消息构造参照既有用例 168-184 行）：

```tsx
describe("toolMessage 配对（FR-7）", () => {
  const toolCall = { id: "tc1", type: "function" as const, function: { name: "get_kline", arguments: "{}" } };

  function renderThread(messages: Message[]) {
    mocks.agent.messages = messages;
    // 既有用例的通用渲染入口（mocks.renderToolCall 已 mockReturnValue(<div data-testid="tool-rendered" />)）
    return render(<ChatRuntime><ThreadArea llmReady /></ChatRuntime>);
  }

  it("把 role:tool 消息按 toolCallId 配对传给 renderToolCall", () => {
    renderThread([
      { id: "a1", role: "assistant", content: "查一下", toolCalls: [toolCall] },
      { id: "tr1", role: "tool", toolCallId: "tc1", content: '{"specVersion":1}' },
    ]);
    expect(mocks.renderToolCall).toHaveBeenCalledWith(
      expect.objectContaining({
        toolCall: expect.objectContaining({ id: "tc1" }),
        toolMessage: expect.objectContaining({ id: "tr1", toolCallId: "tc1" }),
      }),
    );
  });

  it("无 tool 消息时 toolMessage 为 undefined（不崩）", () => {
    renderThread([{ id: "a1", role: "assistant", content: "hi", toolCalls: [toolCall] }]);
    expect(mocks.renderToolCall).toHaveBeenCalledWith(
      expect.objectContaining({ toolMessage: undefined }),
    );
  });

  it("消息数组变化但本条 toolCall 的 toolMessage 未变时不重渲染（memo 比较器）", () => {
    const tool = { id: "tr1", role: "tool" as const, toolCallId: "tc1", content: "{}" };
    const { rerender } = renderThread([
      { id: "a1", role: "assistant", content: "查一下", toolCalls: [toolCall] },
      tool,
    ]);
    expect(mocks.renderToolCall).toHaveBeenCalledTimes(1);
    // 追加无关 user 消息 → 新数组、新 Map，但 tc1 的 toolMessage 引用不变 → AssistantMessage 不重渲染
    mocks.agent.messages = [
      { id: "a1", role: "assistant", content: "查一下", toolCalls: [toolCall] },
      tool,
      { id: "u2", role: "user", content: "再问一句" },
    ];
    rerender(<ChatRuntime><ThreadArea llmReady /></ChatRuntime>);
    expect(mocks.renderToolCall).toHaveBeenCalledTimes(1);
    // tool 消息内容更新（新引用）→ 重渲染
    mocks.agent.messages = [
      { id: "a1", role: "assistant", content: "查一下", toolCalls: [toolCall] },
      { ...tool, content: '{"specVersion":1,"type":"candlestick"}' },
    ];
    rerender(<ChatRuntime><ThreadArea llmReady /></ChatRuntime>);
    expect(mocks.renderToolCall).toHaveBeenCalledTimes(2);
  });
});
```

注：`ChatRuntime`/渲染 harness 以文件内既有 describe 的写法为准（勿新造 Provider；照抄「渲染助手消息」用例的渲染方式）。`Message` 类型从文件顶部既有 import 取。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd frontend && pnpm vitest run tests/ThreadArea.test.tsx
```

预期：新用例 FAIL（`toolMessage` 收到 undefined / 调用次数不符——现状只传 `{ toolCall: tc }`）。

- [ ] **Step 3: 实现 Map 配对 + memo 比较器**

`ThreadArea.tsx` 三处修改：

① 文件内 AssistantMessage 上方补类型与 Map 构建（memo 隔离说明见原注释 198-200）：

```tsx
/** ag-ui 的 role:"tool" 消息（配对键 toolCallId，@ag-ui/core ToolMessage 的本地形态） */
type ToolMessage = Extract<Message, { role: "tool" }>;

function buildToolMessageMap(messages: Message[]): Map<string, ToolMessage> {
  const map = new Map<string, ToolMessage>();
  for (const m of messages) if (m.role === "tool" && m.toolCallId) map.set(m.toolCallId, m);
  return map;
}
```

② AssistantMessage 改签名 + 配对渲染 + 自定义比较器（memo 第二参；不写则 Map 新引用会让全部历史消息在每次流式 delta 重渲染，废掉 198-200 行注释声明的隔离）：

```tsx
const AssistantMessage = memo(function AssistantMessage({ message, toolMessageByCallId }: {
  message: Message;
  toolMessageByCallId: Map<string, ToolMessage>;
}) {
  const renderToolCall = useRenderToolCall();
  const toolCalls = message.role === "assistant" ? (message.toolCalls ?? []) : [];
  const content = typeof message.content === "string" ? message.content : "";
  return (
    <div className="animate-rise my-5 flex gap-3.5">
      {/* …头像等原有内容不动… */}
      <div className="min-w-0 flex-1">
        {toolCalls.map((tc) => (
          // FR-7：complete 依赖 toolMessage 配对（官方 CopilotChatToolCallsView 范式，05 §4.2）
          <div key={tc.id}>{renderToolCall({ toolCall: tc, toolMessage: toolMessageByCallId.get(tc.id) })}</div>
        ))}
        {/* …原 markdown 正文不动… */}
      </div>
    </div>
  );
}, (prev, next) => {
  if (prev.message !== next.message) return false;
  const tcs = next.message.toolCalls ?? [];
  // 只有本条消息涉及的 toolMessage 变化才重渲染
  return tcs.every((tc) => prev.toolMessageByCallId.get(tc.id) === next.toolMessageByCallId.get(tc.id));
});
```

③ 消息渲染处（~563 `const messages = agent.messages ?? []`）建 Map 并传 prop：

```tsx
const toolMessageByCallId = useMemo(() => buildToolMessageMap(messages), [messages]);
// 渲染 assistant 消息处：
<AssistantMessage key={m.id} message={m} toolMessageByCallId={toolMessageByCallId} />
```

- [ ] **Step 4: 跑测试确认通过（含既有用例不回归）**

```bash
cd frontend && pnpm vitest run tests/ThreadArea.test.tsx tests/ThreadArea.stream.test.tsx tests/agui-stream.test.ts
```

预期：PASS（新增 3 用例 + 既有全绿）。

- [ ] **Step 5: Commit**

```bash
git add frontend/components/chat/ThreadArea.tsx frontend/tests/ThreadArea.test.tsx
git commit -m "fix(chat): renderToolCall 配对 toolMessage——后端工具卡可达 complete 态（FR-7）"
```

---

### Task 2: ChartSpec 契约补全（candlestick/table 变体）+ 工具参数 schema

**Files:**
- Modify: `frontend/lib/chart-spec.ts`（P1 建立的 3 变体扩为 5）
- Create: `frontend/lib/tool-params.ts`
- Test: `frontend/tests/lib/chart-spec.test.ts`（追加）、`frontend/tests/lib/tool-params.test.ts`

**Interfaces:**
- Produces: `ChartSpecSchema`（5 变体）、`ChartSpec`、`CandlestickSpec`、`TableSpec`（`Extract` 别名，2026-09-11 澄清：单一契约不建独立 schema）；`KlineParamsSchema`、`ValuationParamsSchema`、`OverviewParamsSchema`（Task 6 useRenderTool 的 `parameters` 必填）。

- [ ] **Step 1: 追加失败测试 `chart-spec.test.ts`**

```ts
describe("candlestick 变体", () => {
  const valid = {
    specVersion: 1, type: "candlestick", title: "600519 贵州茅台 日K",
    symbol: "600519 贵州茅台", period: "day",
    dates: ["2026-09-09", "2026-09-10"],
    klines: [[1800.5, 1850.2, 1790.1, 1860.0], [1850.2, 1840.0, 1830.5, 1870.3]],
    volumes: [120_000, 98_000],
    mas: [{ name: "MA5", data: [null, 1830.1] }],           // null = 预热缺口，与 dates 对齐
  };
  it("解析合法 candlestick（含 null MA 预热）", () => {
    expect(ChartSpecSchema.safeParse(valid).success).toBe(true);
  });
  it("klines 必须是 4 元数值元组（[开,收,低,高]）", () => {
    expect(ChartSpecSchema.safeParse({ ...valid, klines: [[1, 2, 3]] }).success).toBe(false);
    expect(ChartSpecSchema.safeParse({ ...valid, klines: [["1", "2", "3", "4"]] }).success).toBe(false);
  });
  it("period/day-week-month 之外的字符串仍可解析（后端契约展示字段）", () => {
    expect(ChartSpecSchema.safeParse({ ...valid, period: "week" }).success).toBe(true);
  });
});

describe("table 变体（单一 ChartSpec 的变体，非独立 schema）", () => {
  const valid = {
    specVersion: 1, type: "table", title: "贵州茅台 财务指标",
    columns: [
      { key: "reportDate", label: "报告期" },
      { key: "eps", label: "每股收益EPS", align: "right" as const, sortable: true },
    ],
    rows: [
      { reportDate: "2026-06-30", eps: 24.2, roi: null },
      { reportDate: "2026-03-31", eps: 11.8 },
    ],
  };
  it("解析合法 table（列定义 + 行记录，值允许 string/number/null）", () => {
    expect(ChartSpecSchema.safeParse(valid).success).toBe(true);
  });
  it("行值为对象/数组时拒绝", () => {
    expect(ChartSpecSchema.safeParse({ ...valid, rows: [{ reportDate: { v: 1 } }] }).success).toBe(false);
  });
  it("列 align 只允许 left/right/center", () => {
    expect(ChartSpecSchema.safeParse({ ...valid, columns: [{ key: "a", label: "A", align: "middle" }] }).success).toBe(false);
  });
});
```

- [ ] **Step 2: 追加失败测试 `tests/lib/tool-params.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { KlineParamsSchema, ValuationParamsSchema, OverviewParamsSchema } from "@/lib/tool-params";

describe("tool-params（useRenderTool parameters 用的入参 schema）", () => {
  it("kline：code 必填，period/limit 可选", () => {
    expect(KlineParamsSchema.safeParse({ code: "600519" }).success).toBe(true);
    expect(KlineParamsSchema.safeParse({ code: "600519", period: "day", limit: 500 }).success).toBe(true);
    expect(KlineParamsSchema.safeParse({ period: "day" }).success).toBe(false);
  });
  it("valuation/overview 无必填参数", () => {
    expect(ValuationParamsSchema.safeParse({}).success).toBe(true);
    expect(OverviewParamsSchema.safeParse({}).success).toBe(true);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
cd frontend && pnpm vitest run tests/lib/chart-spec.test.ts tests/lib/tool-params.test.ts
```

预期：FAIL（candlestick/table 用例与 tool-params 模块不存在）。

- [ ] **Step 4: 实现**

`chart-spec.ts`：在 discriminatedUnion 数组追加两个 member（字段与 P1 三变体同风格），并在文件尾补别名：

```ts
  z.object({
    specVersion: z.literal(1),
    type: z.literal("candlestick"),
    title: z.string(),
    subtitle: z.string().optional(),
    symbol: z.string(),                                  // "600519 贵州茅台"
    period: z.string(),                                  // "day"|"week"|"month"（与工具入参一致，展示用）
    dates: z.array(z.string()),                          // 交易日
    klines: z.array(z.tuple([z.number(), z.number(), z.number(), z.number()])),
    //                        ⚠ 顺序 = [开, 收, 低, 高]（ECharts 官方约定）
    volumes: z.array(z.number()).optional(),             // 成交量副图
    mas: z.array(z.object({
      name: z.string(),
      data: z.array(z.union([z.number(), z.null()])),    // null = 预热缺口，与 dates 对齐
    })).optional(),                                      // MA 线后端算好
  }),
  z.object({
    specVersion: z.literal(1),
    type: z.literal("table"),
    title: z.string(),
    subtitle: z.string().optional(),
    columns: z.array(z.object({
      key: z.string(), label: z.string(),
      align: z.enum(["left", "right", "center"]).optional(),
      sortable: z.boolean().optional(),                  // 默认 true
    })),
    rows: z.array(z.record(z.string(), z.union([z.string(), z.number(), z.null()]))),
    defaultPageSize: z.number().optional(),              // 卡片内表格可视高度（P4 消费）
  }),
]);
// …既有 PieSpec/BarSpec/LineSpec 别名后追加：
export type CandlestickSpec = Extract<ChartSpec, { type: "candlestick" }>;
export type TableSpec = Extract<ChartSpec, { type: "table" }>;
```

`frontend/lib/tool-params.ts`（新建）：

```ts
import { z } from "zod";

// useRenderTool 具名重载的 parameters 必填（@copilotkit/react-core 1.70.1 .d.mts 核实；
// zod 3.25 实现 Standard Schema v1）。入参与后端 @ToolParam 定义对齐（InvestTools.java）。
export const KlineParamsSchema = z.object({
  code: z.string().describe("6位A股代码"),
  period: z.string().optional().describe("day/week/month，默认 day"),
  limit: z.number().optional().describe("返回根数，默认 120，最大 500"),
});
export const ValuationParamsSchema = z.object({});
export const OverviewParamsSchema = z.object({});
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd frontend && pnpm vitest run tests/lib/chart-spec.test.ts tests/lib/tool-params.test.ts
```

预期：PASS。

- [ ] **Step 6: Commit**

```bash
git add frontend/lib/chart-spec.ts frontend/lib/tool-params.ts frontend/tests/lib/chart-spec.test.ts frontend/tests/lib/tool-params.test.ts
git commit -m "feat(chart): ChartSpec 补 candlestick/table 变体与工具入参 schema（05 §2.1）"
```

---

### Task 3: echarts-setup 补注册（candlestick + dataZoom）

**Files:**
- Modify: `frontend/lib/echarts-setup.ts`（P1 建立）

**Interfaces:**
- Produces: `ECOption` 类型扩展（含 `CandlestickSeriesOption`、`DataZoomComponentOption`）——Task 4 builder 与 P4/P5 消费；`echarts.use` 注册 `CandlestickChart` + `DataZoomComponent`（= Inside + Slider）。

- [ ] **Step 1: 修改 `echarts-setup.ts`**

在 P1 版本基础上追加（import 与 use 数组同步扩；**不加 DatasetComponent**——builders 不用 dataset transform，YAGNI，与 05 §4.4 聊天期清单的偏差在文件注释记录）：

```ts
import { CandlestickChart } from "echarts/charts";        // 追加到既有 charts import
import { DataZoomComponent } from "echarts/components";   // 追加到既有 components import
import type { CandlestickSeriesOption } from "echarts/charts";
import type { DataZoomComponentOption } from "echarts/components";

echarts.use([
  // …P1 既有：PieChart, BarChart, LineChart, Tooltip/Grid/LegendComponent, LabelLayout, UniversalTransition, CanvasRenderer…
  CandlestickChart,
  DataZoomComponent,
]);

export type ECOption = ComposeOption<
  | PieSeriesOption
  | BarSeriesOption
  | LineSeriesOption
  | CandlestickSeriesOption      // 新增
  | TooltipComponentOption
  | GridComponentOption
  | LegendComponentOption
  | DataZoomComponentOption      // 新增
>;
```

顶部注释同步改为「聊天流阶段已补 candlestick + dataZoom」。

- [ ] **Step 2: 类型与 lint 验证（setup 无单测，P1 惯例：由消费方类型 + e2e 兜底）**

```bash
cd frontend && pnpm eslint lib/echarts-setup.ts && pnpm vitest run tests/charts/
```

预期：eslint 无错误；P1 既有 charts 测试全绿（无回归）。

- [ ] **Step 3: Commit**

```bash
git add frontend/lib/echarts-setup.ts
git commit -m "feat(chart): echarts 按需注册补 candlestick 与 dataZoom（聊天流阶段）"
```

---

### Task 4: buildCandlestickOption（纯函数）

**Files:**
- Modify: `frontend/components/charts/optionBuilders.ts`（P1 建立）
- Test: `frontend/tests/charts/optionBuilders.test.ts`（追加）

**Interfaces:**
- Consumes: `CandlestickSpec`（Task 2）、`ECOption`（Task 3）
- Produces: `BuilderStyle` 扩展 `{ up?: string; down?: string }`（向后兼容）；`buildCandlestickOption(spec: CandlestickSpec, style?: BuilderStyle): ECOption`（Task 5 ChartCard 消费）

- [ ] **Step 1: 追加失败测试**

```ts
import { buildCandlestickOption } from "@/components/charts/optionBuilders";

describe("buildCandlestickOption", () => {
  const spec = {
    specVersion: 1 as const, type: "candlestick" as const, title: "600519 贵州茅台 日K",
    symbol: "600519 贵州茅台", period: "day",
    dates: ["09-09", "09-10", "09-11"],
    klines: [[1800, 1850, 1790, 1860], [1850, 1840, 1830, 1870], [1840, 1880, 1835, 1890]],
    volumes: [120_000, 98_000, 111_000],
    mas: [{ name: "MA5", data: [null, 1830.1, 1855.0] }],
  };
  const style = { up: "#e85b55", down: "#2fbe8f" };

  it("主副图双 grid、双 xAxis、inside+slider dataZoom 联动", () => {
    const opt = buildCandlestickOption(spec, style);
    expect(opt.grid).toHaveLength(2);
    expect(opt.xAxis).toHaveLength(2);
    const zoom = opt.dataZoom as { type: string; xAxisIndex: number[] }[];
    expect(zoom.map((z) => z.type)).toEqual(["inside", "slider"]);
    expect(zoom.every((z) => z.xAxisIndex.includes(0) && z.xAxisIndex.includes(1))).toBe(true);
  });

  it("烛台 series 涨红跌绿四色 itemStyle；klines 原样透传（[开,收,低,高]）", () => {
    const opt = buildCandlestickOption(spec, style);
    const candle = (opt.series as { type: string }[]).find((s) => s.type === "candlestick") as {
      data: number[][]; itemStyle: { color: string; color0: string; borderColor: string; borderColor0: string };
    };
    expect(candle.data).toEqual(spec.klines);
    expect(candle.itemStyle).toMatchObject({ color: "#e85b55", color0: "#2fbe8f", borderColor: "#e85b55", borderColor0: "#2fbe8f" });
  });

  it("MA 线与成交量副图叠加；无 volumes 时不出成交量 series", () => {
    const opt = buildCandlestickOption(spec, style);
    const names = (opt.series as { name?: string }[]).map((s) => s.name);
    expect(names).toContain("MA5");
    expect(names).toContain("成交量");
    const noVol = buildCandlestickOption({ ...spec, volumes: undefined }, style);
    expect((noVol.series as { name?: string }[]).map((s) => s.name)).not.toContain("成交量");
  });

  it("style 缺省时回退深色主题字面值（ChartCard 生产路径必传 getPalette 解析值）", () => {
    const opt = buildCandlestickOption(spec);
    const candle = (opt.series as { type: string }[]).find((s) => s.type === "candlestick") as { itemStyle: { color: string } };
    expect(candle.itemStyle.color).toBe("#e85b55");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd frontend && pnpm vitest run tests/charts/optionBuilders.test.ts
```

预期：FAIL（`buildCandlestickOption` 未导出）。

- [ ] **Step 3: 实现（追加到 `optionBuilders.ts`）**

```ts
import type { CandlestickSpec } from "@/lib/chart-spec";

// BuilderStyle 追加两字段（向后兼容：P1 的 seriesColors 不动）
export interface BuilderStyle {
  /** 解析后的具体色值（来自 chart-theme palette）；不传则用主题配色循环 */
  seriesColors?: string[];
  /** 涨色（K 线实体/描边）；缺省回退 globals.css 深色默认——生产路径 ChartCard 必传运行时解析值 */
  up?: string;
  /** 跌色；同上 */
  down?: string;
}

/** 深色默认回退值（globals.css:14-15；仅测试/异常路径兜底，组件不得依赖） */
const FALLBACK_UP = "#e85b55";
const FALLBACK_DOWN = "#2fbe8f";

export function buildCandlestickOption(spec: CandlestickSpec, style?: BuilderStyle): ECOption {
  const up = style?.up ?? FALLBACK_UP;
  const down = style?.down ?? FALLBACK_DOWN;
  return {
    animationDuration: 200,
    axisPointer: { link: [{ xAxisIndex: "all" }] },
    tooltip: { trigger: "axis", axisPointer: { type: "cross" } },
    legend: { data: ["日K", ...spec.mas?.map((m) => m.name) ?? []] },
    grid: [
      { left: 48, right: 16, top: 32, height: "58%" },
      { left: 48, right: 16, top: "74%", height: "16%" },   // 成交量副图
    ],
    xAxis: [
      { type: "category", data: spec.dates, boundaryGap: true },
      { type: "category", gridIndex: 1, data: spec.dates, axisLabel: { show: false } },
    ],
    yAxis: [
      { scale: true },
      { gridIndex: 1, axisLabel: { show: false }, splitLine: { show: false } },
    ],
    dataZoom: [
      { type: "inside", xAxisIndex: [0, 1], start: 60, end: 100 },
      { type: "slider", xAxisIndex: [0, 1], start: 60, end: 100 },
    ],
    series: [
      {
        type: "candlestick", name: "日K", data: spec.klines,
        itemStyle: { color: up, color0: down, borderColor: up, borderColor0: down },
      },
      ...(spec.mas?.map((m) => ({
        type: "line" as const, name: m.name, data: m.data,
        showSymbol: false, smooth: true, lineStyle: { width: 1 },
      })) ?? []),
      ...(spec.volumes ? [{
        type: "bar" as const, xAxisIndex: 1, yAxisIndex: 1, name: "成交量", data: spec.volumes,
      }] : []),
    ],
  };
}
```

注：文件内已有 `import type { ECOption }` 与 `BuilderStyle` 定义——**替换**原 `BuilderStyle` 为上面的扩展版（P1 尚未执行的话直接按本文件最终形态实现；若 P1 已执行则只追加字段）。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd frontend && pnpm vitest run tests/charts/optionBuilders.test.ts
```

预期：PASS（P1 既有 6 用例 + 新增 4 用例）。

- [ ] **Step 5: Commit**

```bash
git add frontend/components/charts/optionBuilders.ts frontend/tests/charts/optionBuilders.test.ts
git commit -m "feat(chart): buildCandlestickOption——主副图/dataZoom 联动/涨红跌绿（05 §4.5）"
```

---

### Task 5: ChartCard 状态机壳

**Files:**
- Create: `frontend/components/chat/charts/ChartCard.tsx`
- Test: `frontend/tests/chat/ChartCard.test.tsx`

**Interfaces:**
- Consumes: `ChartSpecSchema`（Task 2）、`getPalette`（P1 Task 3）、builders（P1 Task 5 + 本计划 Task 4）
- Produces: `ChartCard({ status, result, name, parameters?, builder? })`；`ChartCardBuilder = (spec: ChartSpec, style?: BuilderStyle) => ECOption`（Task 6 消费）。P4 将把 table 分支从降级卡换成 DataTable。

- [ ] **Step 1: 写失败测试 `frontend/tests/chat/ChartCard.test.tsx`**

```tsx
import { afterEach, describe, it, expect, vi } from "vitest";
import { cleanup, render, screen } from "@testing-library/react";

// jsdom 无 canvas：mock EChart 壳捕获 option（模式对齐 05 §六）
vi.mock("@/components/charts/EChart", () => ({
  EChart: (p: { option: unknown; testid?: string; height?: number }) => (
    <div data-testid={p.testid ?? "echart"} data-height={p.height} data-option={JSON.stringify(p.option)} />
  ),
}));

import { ChartCard } from "@/components/chat/charts/ChartCard";
import { buildCandlestickOption } from "@/components/charts/optionBuilders";

const klineSpec = {
  specVersion: 1, type: "candlestick", title: "600519 贵州茅台 日K",
  symbol: "600519 贵州茅台", period: "day",
  dates: ["09-09"], klines: [[1800, 1850, 1790, 1860]],
};
const errorResult = JSON.stringify({ error: "Tool execution failed: 数据源超时", hint: "请稍后重试" });

afterEach(() => cleanup());

describe("ChartCard（状态机壳，05 §4.6）", () => {
  it("inProgress 渲染 .tool-card.running 骨架，不渲染图表", () => {
    render(<ChartCard status="inProgress" name="get_kline" builder={buildCandlestickOption} />);
    const card = document.querySelector(".tool-card.running");
    expect(card).toBeTruthy();
    expect(screen.queryByTestId("chart-card-chart")).toBeNull();
  });

  it("complete + 合法 candlestick → EChart 收到 builder 产物（含 palette 涨跌色）", () => {
    render(<ChartCard status="complete" name="get_kline" result={JSON.stringify(klineSpec)} builder={buildCandlestickOption} />);
    const el = screen.getByTestId("chart-card-chart");
    expect(el).toBeTruthy();
    const option = JSON.parse(el.dataset.option!);
    const candle = option.series.find((s: { type: string }) => s.type === "candlestick");
    // jsdom 取不到 CSS 变量 → 回退深色字面值（生产路径为运行时解析值，Task 6/e2e 覆盖）
    expect(candle.itemStyle.color).toBeTruthy();
  });

  it("complete + 错误内容（后端 error JSON）→ 降级折叠卡，原文截断 2000", () => {
    render(<ChartCard status="complete" name="get_kline" result={errorResult} builder={buildCandlestickOption} />);
    expect(screen.queryByTestId("chart-card-chart")).toBeNull();
    const details = document.querySelector("details.tool-card")!;
    expect(details).toBeTruthy();
    expect(details.textContent).toContain("Tool execution failed");
  });

  it("complete + 非法 spec（specVersion 错/JSON 坏）→ 降级折叠卡", () => {
    render(<ChartCard status="complete" name="get_kline" result='{"specVersion":2,"type":"pie"}' builder={buildCandlestickOption} />);
    expect(document.querySelector("details.tool-card")).toBeTruthy();
    render(<ChartCard status="complete" name="get_kline" result="不是JSON" builder={buildCandlestickOption} />);
    expect(document.querySelectorAll("details.tool-card")).toHaveLength(2);
  });

  it("complete + table 变体 → 本期降级折叠卡（P4 换 DataTable）", () => {
    const tableSpec = {
      specVersion: 1, type: "table", title: "财务指标",
      columns: [{ key: "a", label: "A" }], rows: [{ a: 1 }],
    };
    render(<ChartCard status="complete" name="get_financials" result={JSON.stringify(tableSpec)} builder={buildCandlestickOption} />);
    expect(screen.queryByTestId("chart-card-chart")).toBeNull();
    expect(document.querySelector("details.tool-card")).toBeTruthy();
  });

  it("complete 但 result undefined（RUN_ERROR 流级错误，无 toolMessage）→ 维持骨架", () => {
    render(<ChartCard status="complete" name="get_kline" builder={buildCandlestickOption} />);
    expect(document.querySelector(".tool-card.running")).toBeTruthy();
  });

  it("candlestick 高度 360、其余 240", () => {
    const { rerender } = render(<ChartCard status="complete" name="get_kline" result={JSON.stringify(klineSpec)} builder={buildCandlestickOption} />);
    expect(screen.getByTestId("chart-card-chart").dataset.height).toBe("360");
    const lineSpec = { specVersion: 1, type: "line", title: "PE", categories: ["d1"], series: [{ name: "PE", data: [1] }] };
    rerender(<ChartCard status="complete" name="get_valuation" result={JSON.stringify(lineSpec)} builder={buildCandlestickOption} />);
    expect(screen.getByTestId("chart-card-chart").dataset.height).toBe("240");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd frontend && pnpm vitest run tests/chat/ChartCard.test.tsx
```

预期：FAIL（模块不存在）。

- [ ] **Step 3: 实现 `ChartCard.tsx`**

```tsx
"use client";
// 状态机壳（05 §4.6，2026-09-11 澄清版）：单一 ChartSpecSchema，按 type 分发。
// table 变体本期降级折叠卡（DataTable 在 P4）；option 在单一 useMemo 内构建（不 conditional hook）。
import { useMemo } from "react";
import { ChartSpecSchema, type ChartSpec } from "@/lib/chart-spec";
import { getPalette } from "@/lib/chart-theme";
import { EChart } from "@/components/charts/EChart";
import type { ECOption } from "@/lib/echarts-setup";
import type { BuilderStyle } from "@/components/charts/optionBuilders";

export type ChartCardBuilder = (spec: ChartSpec, style?: BuilderStyle) => ECOption;

/** 后端错误内容嗅探（05 §3.5：AguiEvent$ToolCallResult 无 state/error 字段，只能嗅探内容）。
 *  收紧为明确错误形态，避免误伤含 "error" 字样的正常数据。 */
function isToolError(result: string): boolean {
  return /tool execution failed|"error"\s*:|工具执行失败/i.test(result);
}

export function ChartCard({ status, result, name, builder }: {
  status: "inProgress" | "executing" | "complete";
  result?: string;
  name: string;
  parameters?: unknown;
  builder?: ChartCardBuilder;
}) {
  const parsed = useMemo(() => {
    if (status !== "complete" || typeof result !== "string") return null;
    if (isToolError(result)) return { degrade: true as const };
    try {
      const check = ChartSpecSchema.safeParse(JSON.parse(result));
      if (!check.success) return { degrade: true as const };
      const spec = check.data;
      if (spec.type === "table" || !builder) return { degrade: true as const }; // table → P4
      const p = getPalette();
      return { spec, option: builder(spec, { up: p.up, down: p.down }) };
    } catch {
      return { degrade: true as const };
    }
  }, [status, result, builder]);

  if (parsed == null)
    return (
      <div className="tool-card running my-2 w-full max-w-[560px] px-3 py-2 text-xs text-[color:var(--color-ink-faint)]">
        {name} 执行中…
      </div>
    );
  if (parsed.degrade)
    return (
      <details className="tool-card my-2 w-full max-w-[560px] px-3 py-2 text-xs">
        <summary className="cursor-pointer text-[color:var(--color-ink-dim)]">数据异常（原始结果折叠）</summary>
        <pre className="mt-2 max-h-[320px] overflow-auto whitespace-pre-wrap break-all text-[color:var(--color-ink-faint)]">
          {result!.slice(0, 2000)}
        </pre>
      </details>
    );
  const height = parsed.spec.type === "candlestick" ? 360 : 240;
  return <EChart option={parsed.option} height={height} testid="chart-card-chart" />;
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd frontend && pnpm vitest run tests/chat/ChartCard.test.tsx
```

预期：PASS（7 用例）。

- [ ] **Step 5: Commit**

```bash
git add frontend/components/chat/charts/ChartCard.tsx frontend/tests/chat/ChartCard.test.tsx
git commit -m "feat(chat): ChartCard 状态机壳——骨架/渲染/降级三态（05 §4.6）"
```

---

### Task 6: toolRenderers 注册三个图表渲染器

**Files:**
- Create: `frontend/components/chat/toolRenderers.tsx`
- Modify: `frontend/components/chat/ThreadArea.tsx`（ToolCallRenderers 内挂载，~70-85）
- Test: `frontend/tests/chat/toolRenderers.test.tsx`

**Interfaces:**
- Consumes: `ChartCard`（Task 5）、`buildCandlestickOption`/`buildLineOption`/`buildBarOption`、params schemas（Task 2）
- Produces: `ChartToolRenderers` 组件（ThreadArea 内挂载）；注册名 `get_kline`/`get_valuation`/`get_market_overview`。其余工具继续 `useDefaultRenderTool` → ToolCallCard（现状不动）。

- [ ] **Step 1: 写失败测试 `frontend/tests/chat/toolRenderers.test.tsx`**

```tsx
import { afterEach, describe, it, expect, vi } from "vitest";
import { cleanup, render } from "@testing-library/react";

const renderToolConfigs: { name: string; render: (p: Record<string, unknown>) => React.ReactElement }[] = [];
vi.mock("@copilotkit/react-core/v2", () => ({
  useRenderTool: (config: { name: string; render: (p: Record<string, unknown>) => React.ReactElement }) => {
    renderToolConfigs.push(config);
  },
}));

vi.mock("@/components/charts/EChart", () => ({
  EChart: (p: { option: unknown; testid?: string }) => (
    <div data-testid={p.testid ?? "echart"} data-option={JSON.stringify(p.option)} />
  ),
}));

import { ChartToolRenderers } from "@/components/chat/toolRenderers";

afterEach(() => { cleanup(); renderToolConfigs.length = 0; });

const klineSpec = {
  specVersion: 1, type: "candlestick", title: "600519 贵州茅台 日K",
  symbol: "600519", period: "day", dates: ["09-09"], klines: [[1800, 1850, 1790, 1860]],
};
const lineSpec = {
  specVersion: 1, type: "line", title: "全A 估值中位数走势",
  categories: ["08-01", "08-02"], series: [{ name: "PE", data: [25.1, 25.3] }, { name: "PB", data: [2.1, null] }],
};
const barSpec = {
  specVersion: 1, type: "bar", title: "主要指数涨跌幅", unit: "%",
  categories: ["上证指数", "深证成指", "创业板指"], series: [{ name: "涨跌幅", data: [0.8, -0.3, 1.2] }],
};

describe("ChartToolRenderers", () => {
  it("注册 get_kline/get_valuation/get_market_overview 三个具名渲染器（无 agentId，05 §4.1）", () => {
    render(<ChartToolRenderers />);
    expect(renderToolConfigs.map((c) => c.name)).toEqual(["get_kline", "get_valuation", "get_market_overview"]);
    expect(renderToolConfigs.every((c) => "parameters" in c)).toBe(true);
  });

  it("get_kline complete → candlestick option（klines 透传）", () => {
    render(<ChartToolRenderers />);
    const kline = renderToolConfigs.find((c) => c.name === "get_kline")!;
    const { getByTestId } = render(kline.render({ status: "complete", result: JSON.stringify(klineSpec) }) as React.ReactElement);
    const option = JSON.parse(getByTestId("chart-card-chart").dataset.option!);
    expect(option.series.find((s: { type: string }) => s.type === "candlestick").data).toEqual(klineSpec.klines);
  });

  it("get_valuation → line（null 缺口保留）；get_market_overview → bar（类目=指数名）", () => {
    render(<ChartToolRenderers />);
    const valuation = renderToolConfigs.find((c) => c.name === "get_valuation")!;
    const { getByTestId: g1 } = render(valuation.render({ status: "complete", result: JSON.stringify(lineSpec) }) as React.ReactElement);
    expect(JSON.parse(g1("chart-card-chart").dataset.option!).series[0].data).toEqual([25.1, 25.3]);
    const overview = renderToolConfigs.find((c) => c.name === "get_market_overview")!;
    const { getByTestId: g2 } = render(overview.render({ status: "complete", result: JSON.stringify(barSpec) }) as React.ReactElement);
    expect(JSON.parse(g2("chart-card-chart").dataset.option!).xAxis.data).toEqual(barSpec.categories);
  });

  it("inProgress 半截参数（Partial）→ 骨架不崩", () => {
    render(<ChartToolRenderers />);
    const kline = renderToolConfigs.find((c) => c.name === "get_kline")!;
    const { container } = render(kline.render({ status: "inProgress", parameters: { code: "60" } }) as React.ReactElement);
    expect(container.querySelector(".tool-card.running")).toBeTruthy();
  });

  it("错误 result → 降级折叠卡", () => {
    render(<ChartToolRenderers />);
    const kline = renderToolConfigs.find((c) => c.name === "get_kline")!;
    const { container } = render(kline.render({ status: "complete", result: '{"error":"Tool execution failed: x"}' }) as React.ReactElement);
    expect(container.querySelector("details.tool-card")).toBeTruthy();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd frontend && pnpm vitest run tests/chat/toolRenderers.test.tsx
```

预期：FAIL（模块不存在）。

- [ ] **Step 3: 实现 `toolRenderers.tsx`**

```tsx
"use client";
// 图表渲染器注册集合（05 §4.3，2026-09-11 澄清：三个有真实数据源的工具）。
// 不传 agentId：headless 无 ChatConfigurationProvider，useRenderTool 匹配链中「无 agentId 的具名渲染器」
// 仍压过通配（05 §4.1 源码核实）。注意与 useInterrupt 不同——后者内部经 useAgent 解析 agentId，
// 缺省会抛「Agent 'default' not found」（ThreadArea.tsx:391 注释），渲染 hook 无此问题。
import { useRenderTool } from "@copilotkit/react-core/v2";
import { ChartCard } from "@/components/chat/charts/ChartCard";
import { buildCandlestickOption, buildLineOption, buildBarOption } from "@/components/charts/optionBuilders";
import { KlineParamsSchema, ValuationParamsSchema, OverviewParamsSchema } from "@/lib/tool-params";

export function ChartToolRenderers() {
  useRenderTool({
    name: "get_kline",
    parameters: KlineParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} parameters={p.parameters} builder={buildCandlestickOption} />,
  });
  useRenderTool({
    name: "get_valuation",
    parameters: ValuationParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} parameters={p.parameters} builder={buildLineOption} />,
  });
  useRenderTool({
    name: "get_market_overview",
    parameters: OverviewParamsSchema,
    render: (p) => <ChartCard status={p.status} result={p.result} name={p.name} parameters={p.parameters} builder={buildBarOption} />,
  });
  return null;
}
```

- [ ] **Step 4: ThreadArea 挂载**

`ThreadArea.tsx` 的 `ToolCallRenderers`（~70-85）内追加一行（import `ChartToolRenderers` from `./toolRenderers`）：

```tsx
function ToolCallRenderers() {
  useDefaultRenderTool({ /* …既有 ToolCallCard 兜底不动… */ });
  return (
    <>
      <ChartToolRenderers />
    </>
  );
}
```

（具名渲染器与通配的优先级由库保证：精确名 > 通配，注册顺序无关。）

- [ ] **Step 5: 跑测试确认通过（含 ThreadArea 既有用例）**

```bash
cd frontend && pnpm vitest run tests/chat/toolRenderers.test.tsx tests/ThreadArea.test.tsx
```

预期：PASS。若 ThreadArea 既有用例因 `@copilotkit/react-core/v2` mock 缺 `useRenderTool` 导出而挂：在该 mock 工厂（ThreadArea.test.tsx:16-42）追加 `useRenderTool: () => {}`。

- [ ] **Step 6: Commit**

```bash
git add frontend/components/chat/toolRenderers.tsx frontend/components/chat/ThreadArea.tsx frontend/tests/chat/toolRenderers.test.tsx frontend/tests/ThreadArea.test.tsx
git commit -m "feat(chat): 按工具名注册图表渲染器——kline/valuation/overview（05 §4.3）"
```

---

### Task 7: 全量回归 + 类型门

- [ ] **Step 1: 全量测试 + lint + 构建（build 含 TS 类型检查，钉住 useRenderTool 的 StandardSchemaV1 兼容）**

```bash
cd frontend && pnpm test && pnpm eslint . && pnpm build
```

预期：vitest 全绿（coverage 80% 门槛）、eslint 无错误、next build 成功。若 `parameters` 类型不兼容（zod 3.25 Standard Schema）：确认 zod 实际解析版本 ≥3.25.76（`pnpm why zod`），仍不兼容则升级 zod 小版本，禁止 `as never` 绕过。

- [ ] **Step 2: 手工冒烟（可选，dev 起前后端发一句「贵州茅台最近走势」）**

预期：get_kline 卡片走 ChartCard——inProgress 骨架 → complete 后因后端还是 String JSON（无 specVersion）降级折叠卡。**这是预期行为**（P3 双通道前，工具返回不含 ChartSpec）；确认无崩溃、聊天流正常即可。

- [ ] **Step 3: Commit（如有修动）**

```bash
git add -A frontend && git commit -m "test(chat): P2 全量回归与类型门收尾"
```

---

## 自查记录（Self-Review）

1. **Spec 覆盖**：FR-7 → Task 1；FR-2 的三工具注册 + FR-9 三态 → Task 5/6；FR-3 单一契约五变体 → Task 2；05 §4.4 聊天期注册集（candlestick+dataZoom，不加 Dataset——builders 不用 dataset，YAGNI 偏差已注明）→ Task 3；05 §4.5 → Task 4；05 §4.6（2026-09-11 澄清版：单一 schema 按 type 分发、无 table prop）→ Task 5。get_financials/P4、后端 emit/P3、e2e/P5 不在本计划。
2. **占位符扫描**：无 TBD/TODO；每个代码步骤给完整代码。
3. **类型一致性**：`ChartCardBuilder = (spec: ChartSpec, style?: BuilderStyle) => ECOption` 与 P1 builders 签名 `(spec, style?)` 一致；`CandlestickSpec`/`TableSpec` 别名 Task 2 定义、Task 4/5 消费；`BuilderStyle` 扩展向后兼容（P1 已执行则追加字段，未执行则按最终形态实现）；testid `chart-card-chart` 跨 Task 5/6 一致。
4. **已知偏差**：MA 线数据允许 null（05 §2.1 原稿 `z.array(z.number())` 的修正——MA 预热期与 dates 对齐必须有缺口），实施后回改 05 §2.1 注释一行。
