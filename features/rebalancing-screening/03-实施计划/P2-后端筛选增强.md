# MS-08 P2 后端·筛选增强 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 M09-F03/F04/F05 后端——指数成分股限定（`indexCode` 条件）、自选观察列表 CRUD（`/api/watchlist`，登录）、股票搜索（`/api/screening/stocks/search`，公开）、CSV 导出（`/api/screening/stocks/export`，公开）。

**Architecture:** 全部在既有 `domain/screening` 域内扩展（白名单测试零改动）；自选走**独立路径 `/api/watchlist`**——不在 `PublicEndpointPaths` 公开清单即默认需登录，零安全配置改动；成分股限定是 `index_constituent` 表（V4 已有、collector 180 天刷新）的纯 SQL 消费；CSV 与筛选页复用同一 `screen()`（同校验同缓存）保证口径一致。

**Tech Stack:** Java 21 / Spring Boot（DDD 四层）、JdbcTemplate 筛选 SQL、JPA（自选表）、JUnit5 + AssertJ + Mockito、Testcontainers PG。

**Spec:** [需求规格说明](../01-需求规格/需求规格说明.md) | [设计规格说明](../02-设计规格/设计规格说明.md)——本计划从 spec 论证，执行者须两者都读。

## Global Constraints

- 覆盖率门槛 ≥80%（JaCoCo 双门槛，`make test`）；改代码需补测试。
- **无需新迁移**：`watchlist_item` 表已在 P1 的 `V14__rebalance_watchlist.sql` 建立。
- `domain/screening` 零 Spring 注解、零跨域依赖；`application/screening` 可依赖 `application/market`（`MarketDataService.quoteBatch`，ArchUnit 允许 application→application）。
- **`ScreeningCriteria` 是 record：加 `indexCode` 字段会破坏全部既有构造调用点**，本计划 Task 1 必须一次改齐（清单见该任务）。
- 后端测试惯例：中文 `@DisplayName`、`given…when…then…`、AssertJ、Mockito（对照 `ScreeningApplicationServiceTest`）；数据正确性集成测试用 `@DataJpaTest + @Import(ScreeningRepositoryImpl.class) + PostgresTestSupport` 模式（对照 `ScreeningRepositoryImplTest`）。
- CSV：UTF-8 + BOM、`\r\n` 行尾、RFC 4180 转义、总市值导出为亿元（2 位小数，与页面列头「总市值(亿)」同口径）。
- 每任务以提交结尾；commit scope `screening`。

---

### Task 1: ScreeningCriteria 加 indexCode + SQL 限定 + 白名单校验

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/domain/screening/ScreeningCriteria.java`
- Modify: `backend/src/main/java/com/portfolio/invest/domain/screening/ScreeningErrorCode.java`
- Modify: `backend/src/main/java/com/portfolio/invest/application/screening/ScreeningApplicationService.java`（cacheKey 纳入 indexCode）
- Modify: `backend/src/main/java/com/portfolio/invest/infrastructure/persistence/ScreeningRepositoryImpl.java`
- Modify: `backend/src/main/java/com/portfolio/invest/web/ScreeningController.java`
- Modify: `backend/src/main/java/com/portfolio/invest/web/GlobalExceptionHandler.java`（错误码清单显式化）
- Test: `backend/src/test/java/com/portfolio/invest/application/screening/ScreeningApplicationServiceTest.java`（追加 + 修既有构造调用）
- Test: `backend/src/integrationTest/java/com/portfolio/invest/infrastructure/persistence/ScreeningRepositoryImplTest.java`（追加 + 修既有构造调用）

**Interfaces:**
- Produces: `ScreeningCriteria` 新签名（在 `industryCode` 后插入 `String indexCode`）：`(peTtmMax, pbMax, dividendYieldMin, roeMin, roaMin, grossMarginMin, debtToAssetsMax, currentRatioMin, revenueYoyMin, netprofitYoyMin, totalMvMin, turnoverRateMin, industryCode, indexCode, sortBy, sortDirection, limit)`；`indexCode` 白名单 `{"000300", "000905"}`（与 `index_constituent.index_code` 存储格式一致，非法值抛 `SCREENING_INVALID_INDEX`→400）；`indexCode != null` 计入 `hasAnyCondition()`。P3 前端与 Task 6 导出端点复用此签名。

**既有构造调用点清单（Step 3 一次改齐，插入 `null` 或具体指数）：**
1. `web/ScreeningController.java:48-51`（stocks 端点，加 @RequestParam）
2. `ScreeningApplicationServiceTest.java:35-36`（criteria 工厂方法）
3. `ScreeningApplicationServiceTest.java:50-51`、`60-61`（非法排序/上限用例）
4. `ScreeningRepositoryImplTest.java:71-73`、`89-90`、`104-105`（三处集成用例）

- [ ] **Step 1: 写失败的测试**

`ScreeningApplicationServiceTest` 追加（criteria 工厂方法加 `null` 占位后可编译）：

```java
@DisplayName("indexCode 白名单外抛出异常")
@Test
void givenIndexCodeNotWhitelisted_whenScreen_thenThrowInvalidIndex() {
    var c = new ScreeningCriteria(new BigDecimal("20"), null, null, null, null, null, null, null,
            null, null, null, null, null, "000016", "pe_ttm", SortDirection.ASC, 200);
    ScreeningException ex = catchThrowableOfType(() -> service.screen(c), ScreeningException.class);
    assertThat(ex.code()).isEqualTo(ScreeningErrorCode.INVALID_INDEX);
}

@DisplayName("单独指数限定算一个条件（浏览成分股是合法查询）")
@Test
void givenIndexCodeOnly_whenScreen_thenNotTreatedAsEmptyCondition() {
    var c = new ScreeningCriteria(null, null, null, null, null, null, null, null,
            null, null, null, null, null, "000300", "pe_ttm", SortDirection.ASC, 200);
    service.screen(c); // 不抛 NO_CONDITION 即通过（委托 mock 仓库）
    verify(repo).findStocks(c);
}

@DisplayName("缓存键纳入 indexCode：仅指数不同不误命中")
@Test
void givenDifferentIndexCode_whenScreen_thenCacheKeysDiffer() {
    var c1 = new ScreeningCriteria(null, null, null, null, null, null, null, null,
            null, null, null, null, null, "000300", "pe_ttm", SortDirection.ASC, 200);
    var c2 = new ScreeningCriteria(null, null, null, null, null, null, null, null,
            null, null, null, null, null, "000905", "pe_ttm", SortDirection.ASC, 200);
    when(repo.findStocks(any())).thenReturn(List.of());
    service.screen(c1);
    service.screen(c2);
    verify(cache).put(org.mockito.ArgumentMatchers.contains("000300"), any(), any(Duration.class));
    verify(cache).put(org.mockito.ArgumentMatchers.contains("000905"), any(), any(Duration.class));
}
```

`ScreeningRepositoryImplTest` 追加集成用例：

```java
private void seedConstituent(String indexCode, String code) {
    jdbcTemplate.update("INSERT INTO index_constituent(index_code, stock_code, stock_name, weight) VALUES (?,?,?,?)",
            indexCode, code, "", new BigDecimal("1.0"));
}

@DisplayName("指数限定：只返回该指数成分股")
@Test
@Transactional
void givenIndexConstituents_whenFilterByIndex_thenOnlyMembersReturned() {
    seedValuation("600519", "贵州茅台", "22.5", "0.35");
    seedValuation("601398", "工商银行", "5.6", "0.18");
    seedValuation("000858", "五粮液", "18.2", "0.62");
    seedConstituent("000300", "600519");
    seedConstituent("000300", "601398");
    // 000858 不属任何指数

    var criteria = new ScreeningCriteria(new BigDecimal("30"), null, null, null, null, null,
            null, null, null, null, null, null, null, "000300", "pe_ttm", SortDirection.ASC, 200);

    var results = screeningRepository.findStocks(criteria);
    assertThat(results).extracting(StockScreeningResult::stockCode)
            .containsExactlyInAnyOrder("600519", "601398");
}
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd backend && ./gradlew test --tests '*ScreeningApplicationServiceTest*'`（先改 criteria 工厂占位 null 使可编译）
Expected: FAIL（INVALID_INDEX 不存在、indexCode 不参与条件）。

- [ ] **Step 3: 实现**

1. `ScreeningErrorCode` 追加 `public static final String INVALID_INDEX = "SCREENING_INVALID_INDEX";`（`WATCHLIST_LIMIT_EXCEEDED`/`INVALID_STOCK` 留 Task 4）。
2. `ScreeningCriteria` 加字段 + 紧凑构造器校验 + 白名单常量 + `hasAnyCondition` 纳入：

```java
public record ScreeningCriteria(
        BigDecimal peTtmMax, BigDecimal pbMax, BigDecimal dividendYieldMin,
        BigDecimal roeMin, BigDecimal roaMin, BigDecimal grossMarginMin,
        BigDecimal debtToAssetsMax, BigDecimal currentRatioMin,
        BigDecimal revenueYoyMin, BigDecimal netprofitYoyMin,
        BigDecimal totalMvMin, BigDecimal turnoverRateMin,
        String industryCode, String indexCode, String sortBy, SortDirection sortDirection, int limit
) {
    public static final Set<String> SORTABLE_FIELDS = Set.of(/* 既有 12 项不变 */);
    /** 指数成分股范围白名单：与 index_constituent.index_code 存储格式一致（6 位无后缀）。 */
    public static final Set<String> INDEX_WHITELIST = Set.of("000300", "000905");

    public ScreeningCriteria {  // 紧凑构造器：白名单外指数码直接拒绝
        if (indexCode != null && !INDEX_WHITELIST.contains(indexCode)) {
            throw new ScreeningException(ScreeningErrorCode.INVALID_INDEX, "不支持的指数: " + indexCode);
        }
    }

    public boolean hasAnyCondition() { /* 既有 13 项 || indexCode != null */ }
}
```

3. `ScreeningApplicationService.cacheKey` 在 `industryCode` 后插 `.add(String.valueOf(c.indexCode()))`。
4. `ScreeningRepositoryImpl.findStocks` 在行业限定行后插：`and(sql, args, "d.stock_code IN (SELECT stock_code FROM index_constituent WHERE index_code = ?)", c.indexCode());`
5. `ScreeningController.stocks` 加 `@RequestParam(required = false) String indexCode` 并传入 criteria（位置对应）。
6. `GlobalExceptionHandler.screening(...)` 的 case 清单追加 `ScreeningErrorCode.INVALID_INDEX`（现状 default 也映射 400，显式化避免 warn 噪声）。
7. 按上方清单修齐全部既有构造调用点。

- [ ] **Step 4: 单测 + 集成测试确认绿**

Run: `cd backend && ./gradlew test --tests '*ScreeningApplicationServiceTest*' && ./gradlew integrationTest --tests '*ScreeningRepositoryImplTest*'`
Expected: PASS（含既有用例不回归）。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/ backend/src/test/java/com/portfolio/invest/application/screening/ backend/src/integrationTest/java/com/portfolio/invest/infrastructure/persistence/ScreeningRepositoryImplTest.java
git commit -m "feat(screening): 指数成分股范围限定——indexCode 条件/白名单/SQL 子查询"
```

---

### Task 2: 按代码集查询 + 搜索端点（自选与手动添加的数据底座）

**Files:**
- Create: `backend/src/main/java/com/portfolio/invest/domain/screening/StockSearchHit.java`
- Modify: `backend/src/main/java/com/portfolio/invest/domain/screening/ScreeningRepository.java`
- Modify: `backend/src/main/java/com/portfolio/invest/infrastructure/persistence/ScreeningRepositoryImpl.java`
- Modify: `backend/src/main/java/com/portfolio/invest/web/ScreeningController.java`
- Test: `backend/src/integrationTest/java/com/portfolio/invest/infrastructure/persistence/ScreeningRepositoryImplTest.java`（追加）

**Interfaces:**
- Produces: `ScreeningRepository.findStocksByCodes(List<String> codes)` → `List<StockScreeningResult>`（最新快照日全维度行，Task 4 自选列表与添加校验消费；空列表返回空）；`ScreeningRepository.searchLatestSnapshot(String keyword, int limit)` → `List<StockSearchHit>`；`StockSearchHit(String stockCode, String stockName, String industryName, BigDecimal peTtm, BigDecimal pb, BigDecimal totalMv)`；端点 `GET /api/screening/stocks/search?q=&limit=10`（公开，P3 搜索框消费）。

- [ ] **Step 1: 写失败的集成测试**

```java
@DisplayName("按代码集查询：只返回最新快照日命中行")
@Test
@Transactional
void givenSeededStocks_whenFindStocksByCodes_thenReturnOnlyRequested() {
    seedValuation("600519", "贵州茅台", "22.5", "0.35");
    seedValuation("601398", "工商银行", "5.6", "0.18");
    var results = screeningRepository.findStocksByCodes(List.of("600519", "999999"));
    assertThat(results).extracting(StockScreeningResult::stockCode).containsExactly("600519");
    assertThat(screeningRepository.findStocksByCodes(List.of())).isEmpty();
}

@DisplayName("搜索：代码前缀与名称包含")
@Test
@Transactional
void givenSeededStocks_whenSearch_thenMatchCodePrefixAndNameContains() {
    seedValuation("600519", "贵州茅台", "22.5", "0.35");
    seedValuation("601398", "工商银行", "5.6", "0.18");
    seedMapping("600519", "贵州茅台", "801120", "食品饮料");

    assertThat(screeningRepository.searchLatestSnapshot("600", 10))
            .extracting(StockSearchHit::stockCode).containsExactly("600519");
    assertThat(screeningRepository.searchLatestSnapshot("茅台", 10))
            .extracting(StockSearchHit::stockCode).containsExactly("600519");
    assertThat(screeningRepository.searchLatestSnapshot("茅台", 10).get(0).industryName()).isEqualTo("食品饮料");
}
```

- [ ] **Step 2: 跑集成测试确认红**

Run: `cd backend && ./gradlew integrationTest --tests '*ScreeningRepositoryImplTest*'`
Expected: 编译 FAIL（方法不存在）。

- [ ] **Step 3: 实现**

1. `StockSearchHit` record（domain/screening，字段见 Interfaces）。
2. `ScreeningRepository` 加两方法声明。
3. `ScreeningRepositoryImpl`（复用 `BASE_SQL` 前缀）：

```java
@Override
public List<StockScreeningResult> findStocksByCodes(List<String> codes) {
    if (codes == null || codes.isEmpty()) {
        return List.of();
    }
    String placeholders = String.join(",", java.util.Collections.nCopies(codes.size(), "?"));
    String sql = BASE_SQL + " AND d.stock_code IN (" + placeholders + ") ORDER BY d.stock_code";
    return jdbc.query(sql, ROW_MAPPER, codes.toArray());
}

@Override
public List<StockSearchHit> searchLatestSnapshot(String keyword, int limit) {
    String sql = """
            SELECT d.stock_code, d.stock_name, m.industry_name, d.pe_ttm, d.pb, d.total_mv
            FROM stock_valuation_daily d
            LEFT JOIN shenwan_industry_mapping m ON d.stock_code = m.stock_code
            WHERE d.trading_day = (SELECT max(trading_day) FROM stock_valuation_daily)
              AND (d.stock_code LIKE ? OR d.stock_name LIKE ?)
            ORDER BY d.stock_code LIMIT ?
            """;
    return jdbc.query(sql, (rs, i) -> new StockSearchHit(
                    rs.getString("stock_code"), rs.getString("stock_name"), rs.getString("industry_name"),
                    rs.getBigDecimal("pe_ttm"), rs.getBigDecimal("pb"), rs.getBigDecimal("total_mv")),
            keyword + "%", "%" + keyword + "%", limit);
}
```

实现时把 `findStocks` 现有行映射 lambda 提为私有常量 `ROW_MAPPER`（两处共用，避免复制）。

4. `ScreeningController` 加端点（公开前缀内，无需鉴权配置）：

```java
/** 股票搜索候选（自选手动添加用；公开只读）。 */
@GetMapping("/stocks/search")
public List<StockSearchHit> search(@RequestParam String q,
                                   @RequestParam(defaultValue = "10") int limit) {
    if (q == null || q.isBlank()) {
        throw new ScreeningException(ScreeningErrorCode.NO_CONDITION, "搜索词不能为空");
    }
    return screeningApplicationService.search(q.trim(), Math.min(Math.max(limit, 1), 20));
}
```

`ScreeningApplicationService` 加委托方法 `search(String q, int limit)` → `repository.searchLatestSnapshot(q, limit)`（薄委托，与 screen 同类）。

- [ ] **Step 4: 跑集成测试确认绿**

Run: `cd backend && ./gradlew integrationTest --tests '*ScreeningRepositoryImplTest*'`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/
git commit -m "feat(screening): 按代码集查询与最新快照搜索——自选数据底座"
```

---

### Task 3: WatchlistItem 聚合 + 仓储 + JPA

**Files:**
- Create: `backend/src/main/java/com/portfolio/invest/domain/screening/WatchlistItem.java`
- Create: `backend/src/main/java/com/portfolio/invest/domain/screening/WatchlistRepository.java`
- Modify: `backend/src/main/java/com/portfolio/invest/domain/screening/ScreeningErrorCode.java`（追加两码）
- Create: `backend/src/main/java/com/portfolio/invest/infrastructure/persistence/WatchlistItemJpaEntity.java`
- Create: `backend/src/main/java/com/portfolio/invest/infrastructure/persistence/WatchlistItemJpaRepository.java`
- Create: `backend/src/main/java/com/portfolio/invest/infrastructure/persistence/WatchlistRepositoryImpl.java`
- Test: `backend/src/integrationTest/java/com/portfolio/invest/infrastructure/persistence/WatchlistRepositoryImplTest.java`

**Interfaces:**
- Produces: `WatchlistItem(Long id, Long userId, String stockCode, Instant addedAt)` record + 常量 `MAX_SIZE = 100`；仓储端口 `findByUserId(Long): List<WatchlistItem>`（addedAt 倒序）、`existsByUserIdAndStockCode(Long, String)`、`countByUserId(Long)`、`save(WatchlistItem)`、`deleteByUserIdAndStockCode(Long, String)`；错误码 `SCREENING_WATCHLIST_LIMIT_EXCEEDED` / `SCREENING_INVALID_STOCK`。Task 4 应用服务消费。

- [ ] **Step 1: 写失败的仓储集成测试**

```java
package com.portfolio.invest.infrastructure.persistence;

import com.portfolio.invest.domain.screening.WatchlistItem;
import com.portfolio.invest.domain.screening.WatchlistRepository;
import com.portfolio.invest.support.PostgresTestSupport;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.autoconfigure.ImportAutoConfiguration;
import org.springframework.boot.data.jpa.test.autoconfigure.DataJpaTest;
import org.springframework.boot.flyway.autoconfigure.FlywayAutoConfiguration;
import org.springframework.boot.jdbc.test.autoconfigure.AutoConfigureTestDatabase;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;
import org.testcontainers.containers.PostgreSQLContainer;

import static org.assertj.core.api.Assertions.assertThat;

@DataJpaTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
@ImportAutoConfiguration(FlywayAutoConfiguration.class)
@Import(WatchlistRepositoryImpl.class)
class WatchlistRepositoryImplTest {

    @ServiceConnection
    static PostgreSQLContainer<?> postgres = PostgresTestSupport.postgres();

    @Autowired
    private WatchlistRepository repository;

    @DisplayName("保存与按用户查询（addedAt 倒序）、存在性、删除")
    @Test
    @Transactional
    void givenItems_whenFindExistsDelete_thenBehave() {
        Long user = seedUser();  // 私有辅助：INSERT INTO app_user(username,password,status,enabled) …并返回 id（对照库内 user 表列）
        repository.save(new WatchlistItem(null, user, "600519", java.time.Instant.now()));
        Thread.sleep(5);  // 或两次 now() 间隔构造顺序
        repository.save(new WatchlistItem(null, user, "601398", java.time.Instant.now()));

        assertThat(repository.findByUserId(user))
                .extracting(WatchlistItem::stockCode).containsExactly("601398", "600519"); // 倒序
        assertThat(repository.existsByUserIdAndStockCode(user, "600519")).isTrue();
        assertThat(repository.countByUserId(user)).isEqualTo(2);

        repository.deleteByUserIdAndStockCode(user, "600519");
        assertThat(repository.existsByUserIdAndStockCode(user, "600519")).isFalse();
        assertThat(repository.countByUserId(user)).isEqualTo(1);
    }
}
```

（`seedUser()` 辅助以库内 `app_user` 实际必填列为准——实现时 Read `V1__init.sql` 对照；如已有 `UserRepository` 集成测试可复用其造数写法。`Thread.sleep` 若引入不确定性，改为两行显式 `Instant` 参数控制顺序。）

- [ ] **Step 2: 跑集成测试确认红**

Run: `cd backend && ./gradlew integrationTest --tests '*WatchlistRepositoryImplTest*'`
Expected: 编译 FAIL。

- [ ] **Step 3: 实现六个类型**

1. `WatchlistItem` record（含 `MAX_SIZE` 常量与字段，见 Interfaces）。
2. `WatchlistRepository` 接口（五方法，javadoc 说明归属过滤在用例层）。
3. `ScreeningErrorCode` 追加 `WATCHLIST_LIMIT_EXCEEDED = "SCREENING_WATCHLIST_LIMIT_EXCEEDED"`、`INVALID_STOCK = "SCREENING_INVALID_STOCK"`。
4. `WatchlistItemJpaEntity`（对照 `RiskAssessmentJpaEntity` 风格：`@Entity @Table(name="watchlist_item")`，字段 id/userId/stockCode/addedAt，`fromDomain`/`toDomain`）。
5. `WatchlistItemJpaRepository extends JpaRepository<WatchlistItemJpaEntity, Long>`，方法声明：

```java
List<WatchlistItemJpaEntity> findByUserIdOrderByAddedAtDesc(Long userId);
boolean existsByUserIdAndStockCode(Long userId, String stockCode);
long countByUserId(Long userId);
void deleteByUserIdAndStockCode(Long userId, String stockCode);
```

6. `WatchlistRepositoryImpl`（对照 `RiskAssessmentRepositoryImpl`，薄映射）。

- [ ] **Step 4: 跑集成测试确认绿**

Run: `cd backend && ./gradlew integrationTest --tests '*WatchlistRepositoryImplTest*'`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/ backend/src/integrationTest/java/com/portfolio/invest/infrastructure/persistence/WatchlistRepositoryImplTest.java
git commit -m "feat(screening): WatchlistItem 聚合与仓储——V14 表 JPA 映射"
```

---

### Task 4: WatchlistApplicationService（列表 join 行情 + 添加校验 + 移除）

**Files:**
- Create: `backend/src/main/java/com/portfolio/invest/application/screening/WatchlistItemView.java`
- Create: `backend/src/main/java/com/portfolio/invest/application/screening/WatchlistApplicationService.java`
- Test: `backend/src/test/java/com/portfolio/invest/application/screening/WatchlistApplicationServiceTest.java`

**Interfaces:**
- Consumes: Task 2 的 `findStocksByCodes`、Task 3 的仓储端口、`MarketDataService.quoteBatch(List<String>): Map<String, Quote>`（`Quote.price()` 为 double，`BigDecimal.valueOf` 换算——对照 `PortfolioApplicationService.java:314`）。
- Produces: `List<WatchlistItemView> list(Long userId)`；`void add(Long userId, String stockCode)`（幂等/INVALID_STOCK/超限）；`void remove(Long userId, String stockCode)`（幂等）。`WatchlistItemView(stockCode, stockName, industryName, price, peTtm, pb, dividendYield, totalMv, addedAt)`——`price` 为实时价（可空），Task 5 端点与 P3 前端消费。

- [ ] **Step 1: 写失败的应用服务测试**

```java
package com.portfolio.invest.application.screening;

import com.portfolio.invest.application.market.MarketDataService;
import com.portfolio.invest.domain.market.Quote;
import com.portfolio.invest.domain.screening.*;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.time.Instant;
import java.util.List;
import java.util.Map;
import static org.assertj.core.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

class WatchlistApplicationServiceTest {

    private final WatchlistRepository watchlist = mock(WatchlistRepository.class);
    private final ScreeningRepository screening = mock(ScreeningRepository.class);
    private final MarketDataService market = mock(MarketDataService.class);
    private final WatchlistApplicationService service =
            new WatchlistApplicationService(watchlist, screening, market);

    private static StockScreeningResult row(String code, String name, String industry) {
        return new StockScreeningResult(code, name, "801780", industry,
                new BigDecimal("5.6"), new BigDecimal("0.62"), new BigDecimal("5.4"),
                null, null, null, null, null, null, null, new BigDecimal("1e10"), null);
    }

    @DisplayName("列表：快照行 join 实时现价，缺价与缺行都安全")
    @Test
    void givenWatchlistWithQuotes_whenList_thenMergedRows() {
        when(watchlist.findByUserId(1L)).thenReturn(List.of(
                new WatchlistItem(1L, 1L, "601398", Instant.parse("2026-09-16T00:00:00Z")),
                new WatchlistItem(2L, 1L, "600519", Instant.parse("2026-09-15T00:00:00Z"))));
        when(screening.findStocksByCodes(any())).thenReturn(List.of(
                row("601398", "工商银行", "银行"), row("600519", "贵州茅台", "食品饮料")));
        when(market.quoteBatch(any())).thenReturn(Map.of("601398", mock(Quote.class)));

        var rows = service.list(1L);

        assertThat(rows).hasSize(2);
        assertThat(rows.get(0).stockCode()).isEqualTo("601398"); // addedAt 倒序由仓储保证
        assertThat(rows.get(0).stockName()).isEqualTo("工商银行");
        assertThat(rows.get(1).price()).isNull(); // 无行情 → null，前端显示 —
    }

    @DisplayName("添加：不存在的代码拒绝；已存在幂等；超限拒绝")
    @Test
    void givenVariousStates_whenAdd_thenValidateIdempotentLimit() {
        when(screening.findStocksByCodes(List.of("999999"))).thenReturn(List.of());
        assertThatThrownBy(() -> service.add(1L, "999999"))
                .isInstanceOfSatisfying(ScreeningException.class,
                        e -> assertThat(e.code()).isEqualTo(ScreeningErrorCode.INVALID_STOCK));

        when(screening.findStocksByCodes(List.of("601398")))
                .thenReturn(List.of(row("601398", "工商银行", "银行")));
        when(watchlist.existsByUserIdAndStockCode(1L, "601398")).thenReturn(true);
        service.add(1L, "601398"); // 幂等：不再校验上限、不重复保存
        verify(watchlist, never()).save(any());

        when(watchlist.existsByUserIdAndStockCode(1L, "600519")).thenReturn(false);
        when(watchlist.countByUserId(1L)).thenReturn((long) WatchlistItem.MAX_SIZE);
        assertThatThrownBy(() -> service.add(1L, "600519"))
                .isInstanceOfSatisfying(ScreeningException.class,
                        e -> assertThat(e.code()).isEqualTo(ScreeningErrorCode.WATCHLIST_LIMIT_EXCEEDED));
    }

    @DisplayName("移除：幂等（不存在亦成功）")
    @Test
    void whenRemove_thenAlwaysOk() {
        service.remove(1L, "600519");
        verify(watchlist).deleteByUserIdAndStockCode(1L, "600519");
    }
}
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd backend && ./gradlew test --tests '*WatchlistApplicationServiceTest*'`
Expected: 编译 FAIL。

- [ ] **Step 3: 实现**

`WatchlistApplicationService`（`@Service`；`list` 不加事务——两次读无一致性要求；`add/remove` `@Transactional`）：

```java
public List<WatchlistItemView> list(Long userId) {
    List<WatchlistItem> items = watchlistRepository.findByUserId(userId);
    if (items.isEmpty()) {
        return List.of();
    }
    List<String> codes = items.stream().map(WatchlistItem::stockCode).toList();
    Map<String, StockScreeningResult> snapshot = screeningRepository.findStocksByCodes(codes).stream()
            .collect(java.util.stream.Collectors.toMap(StockScreeningResult::stockCode, r -> r, (a, b) -> a));
    Map<String, Quote> quotes = marketDataService.quoteBatch(codes);
    return items.stream().map(item -> {
        var row = snapshot.get(item.stockCode());
        Quote q = quotes.get(item.stockCode());
        return new WatchlistItemView(item.stockCode(),
                row == null ? null : row.stockName(),
                row == null ? null : row.industryName(),
                q == null ? null : BigDecimal.valueOf(q.price()),
                row == null ? null : row.peTtm(),
                row == null ? null : row.pb(),
                row == null ? null : row.dividendYield(),
                row == null ? null : row.totalMv(),
                item.addedAt());
    }).toList();
}

@Transactional
public void add(Long userId, String stockCode) {
    if (watchlistRepository.existsByUserIdAndStockCode(userId, stockCode)) {
        return; // 幂等
    }
    if (screeningRepository.findStocksByCodes(List.of(stockCode)).isEmpty()) {
        throw new ScreeningException(ScreeningErrorCode.INVALID_STOCK, "未知股票代码: " + stockCode);
    }
    if (watchlistRepository.countByUserId(userId) >= WatchlistItem.MAX_SIZE) {
        throw new ScreeningException(ScreeningErrorCode.WATCHLIST_LIMIT_EXCEEDED,
                "自选已达上限 " + WatchlistItem.MAX_SIZE + " 只");
    }
    watchlistRepository.save(new WatchlistItem(null, userId, stockCode, Instant.now()));
}

@Transactional
public void remove(Long userId, String stockCode) {
    watchlistRepository.deleteByUserIdAndStockCode(userId, stockCode);
}
```

`WatchlistItemView` record（字段见 Interfaces；`price` BigDecimal 可空）。

- [ ] **Step 4: 跑测试确认绿**

Run: `cd backend && ./gradlew test --tests '*WatchlistApplicationServiceTest*'`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/application/screening/ backend/src/test/java/com/portfolio/invest/application/screening/WatchlistApplicationServiceTest.java
git commit -m "feat(screening): 自选应用服务——快照 join 实时价/幂等添加/上限"
```

---

### Task 5: WatchlistController（/api/watchlist，默认需登录）

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/web/GlobalExceptionHandler.java`（screening case 清单补两码）
- Create: `backend/src/main/java/com/portfolio/invest/web/WatchlistController.java`
- Create: `backend/src/main/java/com/portfolio/invest/application/screening/AddWatchlistCommand.java`

**Interfaces:**
- Produces: `GET /api/watchlist` → `List<WatchlistItemView>`；`POST /api/watchlist`（body `{"stockCode":"601398"}`）→ 201；`DELETE /api/watchlist/{stockCode}` → 204。**路径不在 `PublicEndpointPaths` → 默认 401 匿名**（P3 反代与 e2e 依赖此行为）。

- [ ] **Step 1: 实现控制器与命令**

`AddWatchlistCommand`（对照 `CreatePlanCommand` 的 Bean Validation 风格）：

```java
public record AddWatchlistCommand(@NotBlank String stockCode) {}
```

`WatchlistController`（对照 `AllocationController` 的 currentUserId 模式）：

```java
@RestController
@RequestMapping("/api/watchlist")
public class WatchlistController {

    private final WatchlistApplicationService service;

    public WatchlistController(WatchlistApplicationService service) {
        this.service = service;
    }

    @GetMapping
    public List<WatchlistItemView> list(Authentication auth) {
        return service.list(currentUserId(auth));
    }

    @PostMapping
    public ResponseEntity<Void> add(Authentication auth, @Valid @RequestBody AddWatchlistCommand cmd) {
        service.add(currentUserId(auth), cmd.stockCode().trim());
        return ResponseEntity.status(HttpStatus.CREATED).build();
    }

    @DeleteMapping("/{stockCode}")
    public ResponseEntity<Void> remove(Authentication auth, @PathVariable String stockCode) {
        service.remove(currentUserId(auth), stockCode);
        return ResponseEntity.noContent().build();
    }

    private static Long currentUserId(Authentication auth) {
        return ((AuthenticatedUser) auth.getPrincipal()).user().id();
    }
}
```

`GlobalExceptionHandler.screening(...)` case 清单追加 `INVALID_INDEX`（Task 1 若已加则跳过）、`WATCHLIST_LIMIT_EXCEEDED`、`INVALID_STOCK` → 400。

- [ ] **Step 2: 匿名 401 行为验证（SecurityConfigTest 或手测）**

Run: `cd backend && ./gradlew integrationTest --tests '*SecurityConfigTest*'`
Expected: PASS（`/api/watchlist/**` 不在公开清单，默认认证——若 `SecurityConfigTest` 断言公开路径清单，追加一条 `/api/watchlist` 需认证的断言用例，对照该测试现有写法）。

- [ ] **Step 3: 全量单测防回归**

Run: `cd backend && ./gradlew test`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/
git commit -m "feat(screening): /api/watchlist 自选三端点——独立路径默认需登录"
```

---

### Task 6: CSV 导出（ScreeningCsv + export 端点）

**Files:**
- Create: `backend/src/main/java/com/portfolio/invest/web/ScreeningCsv.java`
- Modify: `backend/src/main/java/com/portfolio/invest/web/ScreeningController.java`
- Test: `backend/src/test/java/com/portfolio/invest/web/ScreeningCsvTest.java`

**Interfaces:**
- Produces: `ScreeningCsv.toCsv(List<StockScreeningResult>): byte[]`（package-private 静态工具，web 层表现职责）；端点 `GET /api/screening/stocks/export`（**公开**）——参数与 `/stocks` 完全一致（17 个），复用 `screeningApplicationService.screen(criteria)`（同校验同缓存），响应 `text/csv;charset=UTF-8` + `Content-Disposition: attachment; filename="screening-<yyyyMMdd-HHmmss>.csv"`。P3 前端 `<a href>` 直连下载。

- [ ] **Step 1: 写失败的 CSV 测试**

```java
package com.portfolio.invest.web;

import com.portfolio.invest.domain.screening.StockScreeningResult;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.nio.charset.StandardCharsets;
import java.util.List;
import static org.assertj.core.api.Assertions.assertThat;

class ScreeningCsvTest {

    private static StockScreeningResult row(String code, String name, String pe, String mv) {
        return new StockScreeningResult(code, name, "801780", "银行",
                pe == null ? null : new BigDecimal(pe), new BigDecimal("0.62"), new BigDecimal("5.4"),
                null, null, null, null, null, null, null,
                mv == null ? null : new BigDecimal(mv), null);
    }

    @DisplayName("BOM 首字节 + 中文表头 + CRLF + 总市值换算亿元")
    @Test
    void givenRows_whenToCsv_thenBomHeaderAndConvertedValues() {
        byte[] csv = ScreeningCsv.toCsv(List.of(row("601398", "工商银行", "5.6", "10000000000")));
        String text = new String(csv, StandardCharsets.UTF_8);
        assertThat(csv[0]).isEqualTo((byte) 0xEF); // BOM EF BB BF
        assertThat(csv[1]).isEqualTo((byte) 0xBB);
        assertThat(csv[2]).isEqualTo((byte) 0xBF);
        assertThat(text).contains("代码,名称,PE-TTM,PB,股息率,ROE,ROA,毛利率,资产负债率,流动比率,营收增速,净利增速,总市值(亿),换手率");
        assertThat(text).contains("601398,工商银行,5.6,0.62,5.4");
        assertThat(text).contains(",100.00,"); // 10000000000 元 → 100.00 亿
        assertThat(text).contains("\r\n");
    }

    @DisplayName("名称含逗号/引号按 RFC 4180 转义；null 输出空串")
    @Test
    void givenSpecialCharsAndNulls_whenToCsv_thenEscapedAndEmpty() {
        byte[] csv = ScreeningCsv.toCsv(List.of(row("600519", "A\"B,C公司", null, null)));
        String text = new String(csv, StandardCharsets.UTF_8);
        assertThat(text).contains("\"A\"\"B,C公司\"");
        assertThat(text).contains("600519,\"A\"\"B,C公司\",,,"); // null → 空串连续逗号
    }

    @DisplayName("空结果仅表头行")
    @Test
    void givenNoRows_whenToCsv_thenHeaderOnly() {
        String text = new String(ScreeningCsv.toCsv(List.of()), StandardCharsets.UTF_8);
        assertThat(text.stripTrailing()).isEqualTo("﻿代码,名称,PE-TTM,PB,股息率,ROE,ROA,毛利率,资产负债率,流动比率,营收增速,净利增速,总市值(亿),换手率");
    }
}
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd backend && ./gradlew test --tests '*ScreeningCsvTest*'`
Expected: 编译 FAIL。

- [ ] **Step 3: 实现 ScreeningCsv + export 端点**

`ScreeningCsv.java`（列 = 代码 + 结果表 13 列；`totalMv` 元→亿 scale 2）：

```java
package com.portfolio.invest.web;

final class ScreeningCsv {

    private static final String HEADER = "代码,名称,PE-TTM,PB,股息率,ROE,ROA,毛利率,资产负债率,流动比率,营收增速,净利增速,总市值(亿),换手率";
    private static final byte[] BOM = {(byte) 0xEF, (byte) 0xBB, (byte) 0xBF};

    private ScreeningCsv() {}

    static byte[] toCsv(java.util.List<com.portfolio.invest.domain.screening.StockScreeningResult> rows) {
        StringBuilder sb = new StringBuilder();
        sb.append(HEADER).append("\r\n");
        var bigDecimal = new java.math.BigDecimal("100000000");
        for (var r : rows) {
            sb.append(escape(r.stockCode())).append(',')
              .append(escape(r.stockName())).append(',')
              .append(num(r.peTtm())).append(',')
              .append(num(r.pb())).append(',')
              .append(num(r.dividendYield())).append(',')
              .append(num(r.roe())).append(',')
              .append(num(r.roa())).append(',')
              .append(num(r.grossMargin())).append(',')
              .append(num(r.debtToAssets())).append(',')
              .append(num(r.currentRatio())).append(',')
              .append(num(r.revenueYoy())).append(',')
              .append(num(r.netprofitYoy())).append(',')
              .append(r.totalMv() == null ? "" : r.totalMv().divide(bigDecimal, 2, java.math.RoundingMode.HALF_UP).toPlainString()).append(',')
              .append(num(r.turnoverRate())).append("\r\n");
        }
        byte[] body = sb.toString().getBytes(java.nio.charset.StandardCharsets.UTF_8);
        byte[] out = new byte[BOM.length + body.length];
        System.arraycopy(BOM, 0, out, 0, BOM.length);
        System.arraycopy(body, 0, out, BOM.length, body.length);
        return out;
    }

    private static String num(java.math.BigDecimal v) {
        return v == null ? "" : v.toPlainString();
    }

    /** RFC 4180：含逗号/引号/换行时整体加引号并双写内部引号。 */
    private static String escape(String v) {
        if (v == null) return "";
        if (v.contains(",") || v.contains("\"") || v.contains("\n") || v.contains("\r")) {
            return '"' + v.replace("\"", "\"\"") + '"';
        }
        return v;
    }
}
```

`ScreeningController` 加端点（参数与 `stocks` 完全一致 + `produces`）：

```java
@GetMapping(value = "/stocks/export", produces = "text/csv;charset=UTF-8")
public ResponseEntity<byte[]> export(
        @RequestParam(required = false) BigDecimal peTtmMax,
        @RequestParam(required = false) BigDecimal pbMax,
        @RequestParam(required = false) BigDecimal dividendYieldMin,
        @RequestParam(required = false) BigDecimal roeMin,
        @RequestParam(required = false) BigDecimal roaMin,
        @RequestParam(required = false) BigDecimal grossMarginMin,
        @RequestParam(required = false) BigDecimal debtToAssetsMax,
        @RequestParam(required = false) BigDecimal currentRatioMin,
        @RequestParam(required = false) BigDecimal revenueYoyMin,
        @RequestParam(required = false) BigDecimal netprofitYoyMin,
        @RequestParam(required = false) BigDecimal totalMvMin,
        @RequestParam(required = false) BigDecimal turnoverRateMin,
        @RequestParam(required = false) String industryCode,
        @RequestParam(required = false) String indexCode,
        @RequestParam(defaultValue = "pe_ttm") String sortBy,
        @RequestParam(defaultValue = "ASC") SortDirection sortDirection,
        @RequestParam(defaultValue = "200") int limit) {
    var criteria = new ScreeningCriteria(
            peTtmMax, pbMax, dividendYieldMin, roeMin, roaMin, grossMarginMin,
            debtToAssetsMax, currentRatioMin, revenueYoyMin, netprofitYoyMin,
            totalMvMin, turnoverRateMin, industryCode, indexCode, sortBy, sortDirection, limit);
    var rows = screeningApplicationService.screen(criteria); // 同校验（NO_CONDITION 等）同缓存
    String stamp = java.time.format.DateTimeFormatter.ofPattern("yyyyMMdd-HHmmss")
            .withZone(java.time.ZoneId.systemDefault()).format(java.time.Instant.now());
    return ResponseEntity.ok()
            .header(org.springframework.http.HttpHeaders.CONTENT_DISPOSITION,
                    "attachment; filename=\"screening-" + stamp + ".csv\"")
            .contentType(org.springframework.http.MediaType.parseMediaType("text/csv;charset=UTF-8"))
            .body(ScreeningCsv.toCsv(rows));
}
```

- [ ] **Step 4: 跑测试确认绿 + 全量单测**

Run: `cd backend && ./gradlew test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/web/
git commit -m "feat(screening): CSV 导出——BOM/RFC4180 转义/与页面同源同缓存"
```
