# 行业研究·未上市与竞争格局（industry-unlisted-research）设计规格说明

> 创建：2026-09-26 | 上游：[需求规格说明](../01-需求规格/需求规格说明.md)（grilling 定稿 8 项决策）、[数据源替代裁决与调研纪要](../09-调研报告/2026-09-26-数据源替代裁决与调研纪要.md)
> 约定：代码引用锚定当前工作区真实 file:line（Explore 采集 2026-09-26）；「实施级细化决策」见 §九，实施中如偏离须回写本文档（先例：MS-13 两处细化回写设计规格）。

## 一、总体架构

**零新域、零新前端路由**：全部能力落 `domain/application/industry` 既有分层 + `web` 根包两个新 Controller + `/industry/[industryCode]` 既有下钻页 tab 化扩展。

```
backend/src/main/java/com/portfolio/invest/
  domain/industry/          # 新增：UnlistedCompany / FundingEvent / IndustryChain 聚合 + 3 端口
                            #（现扁平 11 文件，照 IndustryWatchItem/IndustryWatchRepository 先例）
  application/industry/     # 新增：读聚合服务 + 策展写服务 + 2 个 CSV 解析器（无注解类）+ 2 导入服务
  web/IndustryController.java            # 既有公开端点，新增未上市/产业链读侧（同前缀零安全改动）
  web/IndustryCurationController.java    # 新增：/api/industry-curation/** 登录态写侧
  infrastructure/persistence/            # 新增：3 组 JpaEntity/JpaRepository/RepositoryImpl
```

**鉴权落位**（零 SecurityConfig 改动，照 industry-watch 先例）：
- 读侧挂 `/api/industry/**` 既有公开前缀（`PublicEndpointPaths.java:24` `"/api/industry/"`）；
- 写侧挂 `/api/industry-curation/**`——不进 `PublicEndpointPaths.PREFIXES`（`PublicEndpointPaths.java:20-26`）即自动落 `SecurityConfig.java:51-54` `anyRequest().authenticated()`（先例注释：`IndustryWatchController.java:20-23`）。

**复用面清单**（不重造）：
- CSV 导入五层管线模式：`application/portfolio/CsvImportParser.java:20-27`（L1/L2/L4）、`domain/portfolio/ImportSimulator.java:13-18`（L5 纯函数）、`PortfolioImportService.java:34-42`（编排）；
- 导入两端点形状：`web/PortfolioController.java:160-186`（模板下载 `Content-Disposition` + multipart POST；文件级 400 `ApiError` / 行级 200 + 行错误清单）；
- 登录态三件套：`IndustryBoard.tsx:50-54`（未登录 `router.push("/login?redirect=...")`）+ `AuthenticatedUser`（`IndustryWatchController.java:52-54` currentUserId helper）；
- tab 切换：`ScreenerBoard.tsx:135-157` 三 tab 按钮组 + 条件渲染先例；
- ECharts：唯一注册点 `lib/echarts-setup.ts:28-40`、共享组件 `components/charts/EChart.tsx:6-17`、纯函数构建器 `components/charts/optionBuilders.ts`（jsdom 可测）；
- Next 代理 multipart 分支：`frontend/app/api/portfolio/[...path]/route.ts:14-22`（arrayBuffer + 显式 contentType）。

## 二、数据模型（Flyway V19/V20）

命名 `V<n>__<snake_case>.sql`（V18 参照 `V18__etf_screening.sql`）；**必改** `FlywayMigrationIntegrationTest.java:34` 版本清单 `containsExactly("1",...,"19"[,"20"])` 并加表契约断言（模板 V18 :133-148）。

### V19__industry_unlisted.sql（策展企业 + 融资事件）

```sql
-- 未上市策展与融资事件（MS-10，决策 #1/#3）：全局公共研究数据（读侧公开、写侧登录），
-- 无 user 归属——与 wiki_entry（用户私有）相反，供公开端点/全景卡无歧义消费。
CREATE TABLE industry_unlisted_company (
    id                 BIGSERIAL PRIMARY KEY,
    industry_code      VARCHAR(16) NOT NULL,          -- 申万一级，如 801080
    company_name       VARCHAR(128) NOT NULL,
    segment            VARCHAR(64),                   -- 细分赛道文本标签（如 动力电池/CDMO）
    latest_round       VARCHAR(16) NOT NULL,          -- FundingRound 枚举
    last_funding_date  DATE,
    total_funding_yi   NUMERIC(14,2),                 -- 累计融资额（亿元），null=未知
    summary            VARCHAR(256),                  -- 一句话简介
    source_note        VARCHAR(128),                  -- 来源标注（如 爱企查人工核对/LLM草稿已核）
    updated_at         TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (industry_code, company_name)              -- CSV upsert 幂等键
);
CREATE INDEX idx_unlisted_company_industry ON industry_unlisted_company(industry_code, last_funding_date DESC);

CREATE TABLE industry_funding_event (
    id             BIGSERIAL PRIMARY KEY,
    event_date     DATE NOT NULL,
    company_name   VARCHAR(128) NOT NULL,
    round          VARCHAR(16) NOT NULL,              -- FundingRound 枚举
    amount_yi      NUMERIC(14,2),                     -- null=未披露
    investors      VARCHAR(256),                      -- 投资方（顿号分隔），null=未知
    industry_code  VARCHAR(16) NOT NULL,
    segment        VARCHAR(64),
    source_title   VARCHAR(256) NOT NULL,             -- 来源标题
    source_url     VARCHAR(512),                      -- 来源 URL（可缺省——月报类无逐条 URL）
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (event_date, company_name, round)          -- CSV upsert 幂等键（同日同企同轮视为同一事件）
);
CREATE INDEX idx_funding_event_industry ON industry_funding_event(industry_code, event_date DESC);
```

### V20__industry_chain.sql（产业链，P3）

```sql
-- 产业链图谱（MS-10 F11，决策 #6）：链/环节/成员三级；行业关联不建链表——由成员派生
-- （上市成员经 shenwan_industry_mapping、未上市成员经 industry_unlisted_company.industry_code）。
CREATE TABLE industry_chain (
    id          BIGSERIAL PRIMARY KEY,
    name        VARCHAR(64) NOT NULL UNIQUE,          -- 如 锂电池
    description VARCHAR(256)
);
CREATE TABLE industry_chain_stage (
    id         BIGSERIAL PRIMARY KEY,
    chain_id   BIGINT NOT NULL REFERENCES industry_chain(id) ON DELETE CASCADE,
    tier       VARCHAR(16) NOT NULL,                  -- UPSTREAM/MIDSTREAM/DOWNSTREAM 枚举
    name       VARCHAR(64) NOT NULL,                  -- 环节名（如 锂矿/正极材料/电芯/整车）
    sort_order INT NOT NULL DEFAULT 0,                -- 同 tier 内排序
    UNIQUE (chain_id, tier, name)
);
CREATE TABLE industry_chain_member (
    id                  BIGSERIAL PRIMARY KEY,
    stage_id            BIGINT NOT NULL REFERENCES industry_chain_stage(id) ON DELETE CASCADE,
    member_type         VARCHAR(8) NOT NULL,          -- LISTED/UNLISTED
    stock_code          VARCHAR(16),                  -- LISTED 必填（挂行情台跳转）
    unlisted_company_id BIGINT REFERENCES industry_unlisted_company(id) ON DELETE SET NULL,
    display_name        VARCHAR(128) NOT NULL,        -- 成员展示名快照（未上市取策展名/上市取证券名）
    CHECK ( (member_type = 'LISTED' AND stock_code IS NOT NULL)
         OR (member_type = 'UNLISTED' AND unlisted_company_id IS NOT NULL) )
);
CREATE INDEX idx_chain_member_stage ON industry_chain_member(stage_id);
```

### 种子数据（随迁移 INSERT，非独立 seeding 机制）

- V19 种子：~10 家示例策展企业（2 个行业）+ ~20 条融资事件——供 e2e 断言与首启演示；真实首批 5 行业名单走 CSV 导入装载（决策 #5b）。
- V20 种子：2 条示例链（锂电池：锂矿→材料→电芯→整车；创新药：CXO→Biotech→制药）。

## 三、领域模型与端口（domain/industry 新增）

```
FundingRound        # 枚举：SEED(种子轮)/ANGEL(天使轮)/PRE_A/A/A_PLUS/B/B_PLUS/C/C_PLUS/D/
                    #      STRATEGIC(战略投资)/PRE_IPO/IPO/ACQUIRED(被收购)/UNKNOWN
                    # 提供 label()（中文）与 order()（轮次序，全景卡分布排序用）——纯函数单测主战场
ChainTier           # 枚举 UPSTREAM/MIDSTREAM/DOWNSTREAM + label()
UnlistedCompany     # record(id, industryCode, companyName, segment, latestRound,
                    #        lastFundingDate, totalFundingYi, summary, sourceNote, updatedAt)
FundingEvent        # record(id, eventDate, companyName, round, amountYi, investors,
                    #        industryCode, segment, sourceTitle, sourceUrl, createdAt)
IndustryChain       # record(id, name, description, stages: List<ChainStage>)
ChainStage          # record(id, tier, name, sortOrder, members: List<ChainMember>)
ChainMember         # record(id, memberType, stockCode, unlistedCompanyId, displayName)

UnlistedCompanyRepository   # findByIndustry(code) 按 lastFundingDate DESC nulls last
                            # / upsertByIndustryAndName / delete(id) / countByIndustry(code)
FundingEventRepository      # findByIndustrySince(code, LocalDate) / upsertByNaturalKey
                            # / countAndRoundDistributionSince(code, LocalDate)（或读侧聚合在服务层做）
IndustryChainRepository     # findRelatedToIndustry(code)（成员派生关联查询）/ findByIdFull(id) / save(全量) / delete(id)
```

端口实现落 `infrastructure/persistence`（照 `IndustryWatchRepositoryImpl.java:10-40` @Repository + 先查后插 upsert 模式）；`@DataJpaTest + @ServiceConnection PostgresTestSupport` 集成测试（照 `IndustryWatchRepositoryImplTest.java:24-31`）。

## 四、API 契约

### 读侧（公开，扩 IndustryController，`/api/industry` 前缀零安全改动）

| 端点 | 响应 | 说明 |
|---|---|---|
| `GET /api/industry/{code}/unlisted/companies` | `List<UnlistedCompanyView>` | 策展名单（F07），lastFundingDate DESC |
| `GET /api/industry/{code}/unlisted/funding-events` | `List<FundingEventView>` + 响应头/体 `coverageNote` 口径字段 | 融资事件（F08），eventDate DESC，默认近 24 月 |
| `GET /api/industry/{code}/unlisted/overview` | `UnlistedOverviewView` | 全景卡（F06，口径见 §七） |
| `GET /api/industry/{code}/chains` | `List<ChainView>` | 该行业相关产业链全量数据（F11，成员派生关联，量小整包返回） |

行业码校验复用 `IndustryApplicationService.stocks` 的 `existsIndustry` 白名单路径（`IndustryApplicationService.java:84-97`），不存在抛既有 `INDUSTRY_NOT_FOUND`（BDD 既有场景口径）。

**F09 竞争格局不新增端点**：前端组合既有 `GET /api/industry/{code}/stocks?sortBy=total_mv&limit=30`（`IndustryController.java:36-42`）+ 上述 companies 端点渲染气泡图（YAGNI）。

### 写侧（登录，新 IndustryCurationController，`/api/industry-curation`）

| 端点 | 语义 |
|---|---|
| `POST /companies` / `PUT /companies/{id}` / `DELETE /companies/{id}` | 策展企业单条增删改（决策 #1 内嵌管理） |
| `GET /companies/import/template` | 模板下载（照 `PortfolioController.java:160-167` 头与常量模式） |
| `POST /companies/import`（multipart） | CSV 批量装载（决策 #5b），≤1MB |
| `GET /funding-events/import/template` + `POST /funding-events/import`（multipart） | 融资事件 CSV 导入（决策 #3） |
| `DELETE /funding-events/{id}` | 单条删除（录错修正） |
| `POST /chains` / `PUT /chains/{id}` / `DELETE /chains/{id}` | 产业链全文档式保存（stages+members 嵌套全量替换——链仅 2~3 条，收窄 CRUD 面） |

- 鉴权写法照 `IndustryWatchController.java:35`（`Authentication auth` 参数 + `currentUserId` 校验登录；策展为全局数据不按 userId 过滤）。
- 错误语义照 MS-14 双层：文件级（空/超 1MB/表头不匹配）→ 400 `ApiError`（`GlobalExceptionHandler.java:83-90` 分支模式，industry 分支 :162-174 加新枚举）；行级 → **200** + `CurationImportResult(insertedCount, updatedCount, rowErrors:[{row,reason}])`（行号+人话原因，不允许静默跳过——NFR #2）。

## 五、CSV 导入设计（两类，复用 MS-14 五层管线）

### 模板（常量三处同源规则：模板常量 ↔ 解析器测试样例 ↔ 集成测试样例，照 `PortfolioImportTemplate.java:5-7` javadoc 约定）

```
# industry_unlisted_companies.csv（UTF-8 BOM，八列）
industry_code,company_name,segment,latest_round,last_funding_date,total_funding_yi,summary,source_note
801730,示例电池科技,动力电池,B,2026-08-15,120.50,动力电池新锐,爱企查人工核对

# industry_funding_events.csv（UTF-8 BOM）
event_date,company_name,round,amount_yi,investors,industry_code,segment,source_title,source_url
2026-08-15,某生物科技公司,B_PLUS,3.20,高瓴·红杉,801150,CXO,睿兽分析2026-08月报,
```

> 回写（2026-09-26，P1 Task 5）：事件模板示例行轮次原文「B+」与本节 L2「round ∈ FundingRound 枚举」（下划线大写输入格式）自相矛盾，按枚举格式修正为 `B_PLUS`——模板/解析器测试/集成测试三处样例同源口径。

### 校验层映射（照 `CsvImportParser.java:20-27` 注释分层）

| 层 | 内容 |
|---|---|
| L1 文件级 | 表头列序精确匹配、BOM 剥离、≤2000 数据行、UTF-8 |
| L2 行级类型 | 日期 `yyyy-MM-dd` 且 ≤ 今日（L4 并入）、金额 NUMERIC、必填列非空 |
| L3 引用 | `industry_code` ∈ 申万 31 一级（复用既有行业白名单查询）；`latest_round`/`round` ∈ FundingRound 枚举 |
| L5 语义 | 文件内同幂等键重复行（第二行起报错）；策展企业 `industry_code+company_name`、事件 `event_date+company_name+round` |

### 幂等语义（与 MS-14 的关键差异，实施级决策 #2）

重复导入同月 CSV **upsert**：命中幂等键 → 更新非键字段，`CurationImportResult` 分别计 `insertedCount`/`updatedCount`（MS-14 交易导入为纯插入无此态——融资/策展数据是全局可修正的研究数据，重导修正属正常运维）。

### 解析器/服务落位

`application/industry/UnlistedCompanyCsvParser` 与 `FundingEventCsvParser`（无注解 final 类，service 内 `new` 持有，照 `PortfolioImportService.java:50`）；`IndustryCurationImportService`（`@Transactional` 编排，L3 引用校验注入行业白名单端口；全量预检后逐条 upsert——无跨行资金语义，不需要 ImportSimulator 式重放）。Commons CSV 依赖已在 `backend/build.gradle:88`。

## 六、前端设计

### 下钻页 tab 化（`components/industry/IndustryDrilldown.tsx:19-88` 扩展）

```
IndustryDrilldown
├─ 页头（既有：返回/行业名/景气箭头/成员数 + ResearchNoteDialog 入口 :77-81）
└─ tab 组（照 ScreenerBoard.tsx:135-157；data-testid="tab-listed"/"tab-unlisted"/"tab-chain"）
   ├─ 上市公司（既有 IndustryStockTable 原样移入）
   ├─ 未上市与融资
   │   ├─ UnlistedOverviewCard（F06 全景卡，口径脚注）
   │   ├─ UnlistedCompanyTable（F07 策展名单，排序：轮次/最近融资）
   │   ├─ FundingEventTable（F08 融资列表 + 「月度摘录·非全量」口径徽标）
   │   ├─ LandscapeChart（F09 竞争格局气泡图：ECharts scatter——上市市值气泡×策展轮次标签）
   │   └─ {user && <CurationPanel>}（编辑入口：单条对话框 + CSV 导入对话框，登录态照 :77-81 条件渲染）
   └─ 产业链（F11：每条相关链一张 ECharts graph 力导向卡 + {user && 链编辑器}）
```

### API 客户端与代理

- `lib/industryUnlistedApi.ts`（公开读：companies/funding-events/overview/chains，`lib/http.ts:10-46` get + zod）；`lib/industryCurationApi.ts`（写侧：JSON CRUD + 两模板 href + multipart 导入单独 fetch FormData——照 `lib/portfolioImportApi.ts:2-3` 注释口径）；schema 进 `lib/schemas.ts`、类型进 `lib/types.ts`。
- 新代理路由 `app/api/industry-curation/[...path]/route.ts`：GET/POST/PUT/DELETE + multipart 分支照 `app/api/portfolio/[...path]/route.ts:14-22`（arrayBuffer + 显式 contentType）+ `Content-Disposition` 透传（`lib/proxy.ts:80-84`）。

### ECharts 注册与构建器

- `lib/echarts-setup.ts`（:28-40 唯一注册点）追加 `ScatterChart`、`GraphChart`，`ECOption`（:43-52）追加 `ScatterSeriesOption`、`GraphSeriesOption`；**不动 chat ChartSpec 联合**（`lib/chart-spec.ts:7-70`）——graph/scatter 是页面私有能力非聊天双通道契约，spec 形状定义在 `components/charts/optionBuilders.ts` 内新增 `buildScatterOption(LandscapeSpec)` / `buildGraphOption(ChainGraphSpec)` 纯函数（jsdom 可测，:1 注释主战场口径）。
- 图谱布局：graph 力导向 `layout:'force'`，节点按 tier 分色（上游/中游/下游三色），上市节点点击经 router 跳 `/market?code=`（行情台联动口径与 F10 一致）；hover 展示 displayName/轮次/市值。
- 体积断言：`scripts/chart-bundle-entry.ts:1-7` 入口不变，注册新增后跑既有 chart gzip 断言（MS-19 口径 ≤240KB 为 chat 载荷，页面静态打包不涉）。

## 七、口径与数学（文档化，验收依据）

- **F06 全景卡聚合**：`listedCount/listedMarketCapYi` = 该申万一级成员现值（复用 `IndustryRepository.findIndustryStocks` 口径，市值单位亿元加总）；`curatedCount` = `countByIndustry`；`fundingHeat` = `[today−365d, today]` 窗口内事件数 + 按 `FundingRound.order()` 排序的轮次分布 `[{round, count}]`；`coverageNote` = 「策展名单与月度摘录融资事件，非全量口径」。
- **F07 排序**：`lastFundingDate DESC NULLS LAST`，同日按 `FundingRound.order()` 倒序；`totalFundingYi`/`amountYi` null 显示「未披露」。
- **F08 窗口**：默认近 24 月（`?months=` 可调 ≤60）；`sourceTitle` 必填保证来源留痕。
- **链-行业关联**：某行业相关链 = 存在成员满足「LISTED 且其 stock_code 经 shenwan_industry_mapping 属该行业」或「UNLISTED 且其策展企业 industry_code = 该行业」；SQL 一次 join 派生，无关联表（实施级决策 #3）。

## 八、测试策略（金字塔映射先例）

| 层 | 内容 | 先例锚点 |
|---|---|---|
| domain 单测 | `FundingRoundTest`（枚举解析/label/order 序）、（P3）`ChainTier` | `domain/industry/ProsperityTest.java` 纯 JUnit |
| application 单测 | 两解析器已知答案 CSV（六型样例+各类错误行全覆盖）、读聚合服务、策展服务 | `CsvImportParserTest.java:20,33-41` 纯 JUnit 零 Spring；`IndustryWatchApplicationServiceTest.java:22-50` Mockito 构造注入 |
| web 切片 | 两 Controller `@WebMvcTest`+MockMvc（multipart 用例含模板头断言） | `PortfolioImportControllerTest.java:43-44,84` |
| 仓储集成 | 3~4 个 RepositoryImpl `@DataJpaTest`+Testcontainers | `IndustryWatchRepositoryImplTest.java:24-31` |
| 全链集成 | 两导入 all-or-nothing + upsert 幂等重导（六表计数快照式对拍 + AfterEach 清账） | `CsvImportIntegrationTest.java:35-36,93-112,187-191` |
| 迁移契约 | V19/V20 版本清单 + 表契约 | `FlywayMigrationIntegrationTest.java:34,133-148` |
| BDD | industry.feature 追加 1 场景（公开查未上市名单） | `industry.feature` + `IndustrySteps.java:16-80`（JdbcTemplate 种子守共享口径） |
| 前端 vitest | schema、三 tab 切换、空态（未策展行业）、CurationPanel 登录门控、导入对话框三态、`buildScatter/buildGraph` 纯函数 | `tests/IndustryDrilldown.test.tsx:8-26` mock 模式；`tests/industryApi.test.ts:4-31` |
| e2e | 新 describe「MS-10」：公开 tab 渲染（`.or()` 空态双态）、登录后策展增删改、导入行错误清单、链图谱节点断言（V20 种子门控非 skip——迁移种子必在） | `industry.spec.ts:19-30` 门控；`.or()` 写法 `analytics.spec.ts:106-107`；登录注册 `helpers.ts:52-65` |

覆盖率门槛：后端 JaCoCo / 前端 V8 ≥80%（NFR #4）；e2e 前端须 `E2E_FRESH_BUILD=1`（memory：陈旧 .next 盲区）。

## 九、实施级细化决策记录

| # | 决策 | 理由 |
|---|---|---|
| 1 | 策展/融资数据**全局无 user 归属**（与 wiki_entry 用户私有相反） | 读侧公开端点（F06/F09 消费）需要无歧义单一数据面；写侧仅校验登录不按人过滤——个人研究工具语义 |
| 2 | CSV 导入 **upsert 幂等**（命中幂等键更新非键字段，报 inserted/updated 双计数） | 全局可修正研究数据，重导修正属正常运维；MS-14 纯插入语义适配交易流水不适配本场景 |
| 3 | 链-行业关联**成员派生**（join 查询），不建关联表 | 链仅 2~3 条、成员十级量级，派生零维护；显式关联表引入双处一致性负担 |
| 4 | 未上市读侧**无缓存**（直查库） | 表百~千行 + 个人规模流量；board 缓存源于 31 行业×5 年分位重计算，本场景无此量级；且策展编辑后立即可见（5min TTL 缓存会造成编辑不可见错觉） |
| 5 | F09 不新增后端端点（前端组合既有 stocks + companies） | 数据已具备，聚合纯展示层；YAGNI |
| 6 | graph/scatter **不进 chat ChartSpec 联合** | ChartSpec 是聊天双通道契约（MS-19），页面私有能力扩展联会污染契约并触发体积断言口径混乱 |
| 7 | 种子随 Flyway INSERT（非应用层 seeding） | e2e/CI 确定性（迁移必跑）+ 首启演示；wiki 应用层 seeding 先例源于「删光不复活」语义，本场景无此需求 |
| 8 | 企查查可选增强**本期零代码** | NFR #8 仅「不挡路」：未来以新迁移加列 + 新端口接入，现有架构无任何阻断点，无需预留抽象 |

## 十、分批建议（细化归 03-实施计划）

- **P1 数据面与导入**：V19 + domain/application/persistence 策展与事件 + IndustryCurationController + 两 CSV 导入 + 模板 + Next 代理路由 + 测试金字塔（不含前端 UI）。
- **P2 下钻页读侧**：公开 4 读端点 + 前端三 tab + 全景卡/策展名单/融资列表/竞争格局气泡 + e2e。
- **P3 产业链**：V20 + 链域/读侧/写侧全文档保存 + ECharts graph 注册与构建器 + 链编辑器 + 种子 + e2e。

依赖：P1 → P2 → P3 顺序（P2 消费 P1 表，P3 独立于 P2 可并行但共享 tab 壳，串行最稳）。
