# 03 · 接口设计

> 后端对外接口的**端点总览与指路**：认证/管理员/会话/行情/AG-UI/健康六组无专属域文档，细节在本篇；估值、筛选、持仓、资产配置、投研日志、收益分析、MCP、Skill 八组只列总览，参数与错误码细节由各域文档承载（§7），避免双头维护。
> 数据获取与保护见 [02-行情数据服务.md](02-行情数据服务.md)；认证与会话模型见 [ADR-0007](../decisions/0007-user-auth.md) / [ADR-0008](../decisions/0008-conversation-persistence.md)。
> 对应代码：`backend/src/main/java/com/portfolio/invest/web/`（13 个 `@RestController` + `GlobalExceptionHandler` 统一异常出口）；权限口径唯一来源为 `infrastructure/security/PublicEndpointPaths.java`。

前端统一经 Next.js 同源反代访问（`/api/**` 下的 auth / admin / conversations / market / valuation / screening / portfolio / allocation / journal / analytics / mcp / skills / agent 与 `/api/copilotkit`），反代透传 Cookie 头与 Set-Cookie（登录态落地浏览器）。

**权限口径**（对齐 `PublicEndpointPaths.java`，`SecurityConfig` 的 permitAll 与 `ActiveUserFilter` 共用同一清单）：

- **公开**：`/api/auth/login|register`、`/api/agent/health|status`（精确）；`/api/market/**`、`/api/valuation/**`、`/api/screening/**`、`/actuator/**`（前缀）。
- **登录**：其余全部（conversations / journal / portfolio / allocation / analytics / mcp / skills / auth/logout|me / `/agui/run` / `/api/copilotkit`）。
- **ADMIN**：`/api/admin/users/**`。

## 1. 认证接口

统一前缀 `/api/auth`：

| 端点 | 权限 | 说明 |
|---|---|---|
| `POST /api/auth/register` | 公开 | 注册（`{username, password}`）→ 201 待审核 |
| `POST /api/auth/login` | 公开 | 登录（`{username, password, rememberMe}`）；成功 200 + `UserView`，并落地会话/记住我 Cookie |
| `POST /api/auth/logout` | 登录 | 登出（Security 过滤器处理，清 JSESSIONID + 记住我） |
| `GET /api/auth/me` | 登录 | 当前用户 `{id, username, role, status, enabled, createdAt}`；未登录 401 |

**登录状态分流**（认证成功后按用户状态，返回 403 + 不同 code）：

| 状态 | HTTP / code |
|---|---|
| 待审核 PENDING | 403 `ACCOUNT_PENDING` |
| 已拒绝 REJECTED | 403 `ACCOUNT_REJECTED` |
| 已停用（enabled=false） | 403 `ACCOUNT_DISABLED` |
| 用户名或密码错误 | 401 `BAD_CREDENTIALS` |

## 2. 管理员接口

统一前缀 `/api/admin/users`，需 `ADMIN` 角色：

| 端点 | 说明 |
|---|---|
| `GET /api/admin/users` | 用户列表（含状态） |
| `POST /api/admin/users/{id}/approve` | 审核通过 |
| `POST /api/admin/users/{id}/reject` | 拒绝 |
| `POST /api/admin/users/{id}/enable` | 启用 |
| `POST /api/admin/users/{id}/disable` | 停用 |
| `POST /api/admin/users/{id}/reset-password` | 重置密码（`{newPassword}`） |

对 ADMIN 账号执行审核/停用/重置 → 403 `FORBIDDEN`（保护内置管理员）。

## 3. 会话接口

统一前缀 `/api/conversations`，需登录，按归属隔离（非本人 404）：

| 端点 | 说明 |
|---|---|
| `GET /api/conversations` | 我的会话列表 `[{id, title, updatedAt}]` |
| `POST /api/conversations` | 新建会话（`{id}` = threadId）→ 201 |
| `GET /api/conversations/{id}/messages` | 加载消息 `[{id, role, content, createdAt}]` |
| `PUT /api/conversations/{id}/messages` | 全量替换保存消息 → 204 |
| `DELETE /api/conversations/{id}` | 删除会话 → 204 |

`saveMessages` 会取首条 user 消息前 24 字生成会话标题（幂等）；会话 id 非本人访问返回 404（不泄露存在性）。

## 4. 行情 REST 接口

统一前缀 `/api/market`（公开）：

| 端点 | 说明 | 关键参数 |
|---|---|---|
| `GET /api/market/search?q=茅台` | 股票搜索 | `q` 必填 |
| `GET /api/market/quote/{code}` | 实时行情 | — |
| `GET /api/market/kline/{code}` | K线 | `period=day/week/month`（默认 day）；`limit` 默认 120，范围 [5, 500] |
| `GET /api/market/financials/{code}` | 财务指标 + PE/PB | — |
| `GET /api/market/news/{code}` | 个股新闻 | `limit` 默认 10，范围 [1, 20] |
| `GET /api/market/overview` | 指数速览 | — |

**错误码 → HTTP 状态**（`GlobalExceptionHandler`）：

| 错误码 | HTTP |
|---|---|
| `INVALID_CODE` / `INVALID_PERIOD` / `INVALID_QUERY` | 400 |
| `RATE_LIMITED` | 429 |
| 其他行情错误 | 502 |

## 5. AG-UI 对话接口

- **端点**：`POST /agui/run`（AgentScope AG-UI starter 自动注册），需登录。
- **请求**：`RunAgentInput`（`threadId`、`runId`、`messages`、`state`、`tools`、`forwardedProps`）。
- **响应**：AG-UI SSE 事件流 —— `RUN_STARTED` → `TEXT_MESSAGE_*`（正文增量）、`REASONING_MESSAGE_*`（思考）、`TOOL_CALL_START/ARGS/END` + `TOOL_CALL_RESULT`（工具进度）、`CUSTOM`（token_usage 等）→ `RUN_FINISHED` / `RUN_ERROR`。

前端经 CopilotKit 运行时 `/api/copilotkit` 反代到此端点（反代转发会话 Cookie，否则 401）。端点配置见 [01-Agent实现.md](01-Agent实现.md) 第 6 节。

## 6. 健康检查接口

`GET /api/agent/health`（公开，纯 liveness，零外呼，供 docker healthcheck / Playwright 就绪检测）：

```json
{
  "status": "up | degraded",
  "llm": { "provider": "deepseek", "model": "...", "baseUrl": "...", "keyConfigured": true }
}
```

`GET /api/agent/status`（公开，前端状态徽标使用）：

```json
{
  "status": "up | degraded",
  "llm": { "provider": "deepseek", "model": "...", "baseUrl": "...", "keyConfigured": true },
  "market": { "ok": true, "latencyMs": 123, "message": "..." }
}
```

- `/health` 只报进程存活与 LLM Key 是否配置，**不调用任何外部服务**；行情连通性探测在 `/status`。
- `status=up` 需 LLM Key 已配 **且** 行情源连通，否则 `degraded`。
- `market.ok` 通过真实请求一次 `600519` 行情并计时（绕过业务缓存）判断；探活结果带 30s TTL 缓存，防止匿名刷接口耗尽上游限流配额。
- 生产环境（`SPRING_PROFILES_ACTIVE=prod`）关闭健康详情外泄（`show-details: never`）。

## 7. 业务域端点总览（细节见各域文档）

以下八组只列「方法 + 路径 + 一句话 + 权限」；请求/响应字段、校验与错误码由链接的域文档承载，本表不重复维护。

### 7.1 估值 `/api/valuation`（细节见 [09-估值域.md](09-估值域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/valuation/overview` | 全A PE/PB 中位数与历史分位、ERP、情绪温度计速览 | 公开 |
| GET | `/api/valuation/industries?sort=pe` | 申万行业估值列表（pe/pb 排序） | 公开 |
| GET | `/api/valuation/history` | PE/PB 中位数历史序列（走势数据） | 公开 |

### 7.2 筛选 `/api/screening`（细节见 [10-筛选域.md](10-筛选域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/screening/stocks` | 12 项指标条件 + 行业 AND 组合选股，排序/限条 | 公开 |

### 7.3 持仓 `/api/portfolio`（细节见 [11-持仓域.md](11-持仓域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/portfolio/overview` | 组合总览（市值/成本/盈亏八字段） | 登录 |
| GET | `/api/portfolio/positions?groupId=` | 持仓列表（现价/市值/浮动盈亏，可按组过滤） | 登录 |
| GET | `/api/portfolio/groups` | 分组列表（含持仓数、现金余额） | 登录 |
| POST | `/api/portfolio/groups` | 建分组 → 201 | 登录 |
| PUT | `/api/portfolio/groups/{groupId}` | 重命名分组 | 登录 |
| DELETE | `/api/portfolio/groups/{groupId}` | 删分组（两道闸）→ 204 | 登录 |
| POST | `/api/portfolio/positions/buy` | 买入记账 → 201 | 登录 |
| POST | `/api/portfolio/positions/sell` | 卖出记账 → 201 | 登录 |
| POST | `/api/portfolio/positions/cash-dividend` | 现金分红 → 201 | 登录 |
| POST | `/api/portfolio/positions/stock-dividend` | 送股 → 201 | 登录 |
| DELETE | `/api/portfolio/positions/{positionId}` | 删持仓（级联删流水）→ 204 | 登录 |
| GET | `/api/portfolio/positions/{positionId}/trades` | 该持仓全部交易 | 登录 |
| PUT | `/api/portfolio/positions/{positionId}/trades/{tradeId}` | 编辑买入交易并重放 | 登录 |
| GET | `/api/portfolio/positions/{positionId}/dividends` | 该持仓全部分红 | 登录 |
| POST | `/api/portfolio/cash-transactions` | 现金存取 → 201 | 登录 |
| GET | `/api/portfolio/cash-transactions?groupId=` | 分组现金流水（groupId 必填） | 登录 |
| GET | `/api/portfolio/allocation` | 资产配置两片（股票/现金） | 登录 |
| GET | `/api/portfolio/industry-distribution` | 行业分布切片（申万映射） | 登录 |
| GET | `/api/portfolio/concentration` | Top5 重仓与占比 | 登录 |

### 7.4 资产配置 `/api/allocation`（细节见 [12-资产配置域.md](12-资产配置域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/allocation/templates` | 4 个内置配置模板 | 登录 |
| GET | `/api/allocation/plans` | 我的方案列表 | 登录 |
| POST | `/api/allocation/plans` | 新建方案 → 201 | 登录 |
| PUT | `/api/allocation/plans/{planId}` | 更新方案 | 登录 |
| POST | `/api/allocation/plans/{planId}/activate` | 激活方案（单激活） | 登录 |
| DELETE | `/api/allocation/plans/{planId}` | 删方案 → 204 | 登录 |
| GET | `/api/allocation/deviation` | 对照持仓的偏离度 | 登录 |

### 7.5 投研日志 `/api/journal`（细节见 [13-投研日志域.md](13-投研日志域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/journal/entries?type=` | 日志条目列表（四类可过滤） | 登录 |
| POST | `/api/journal/entries` | 新建条目 → 201 | 登录 |
| GET | `/api/journal/entries/{entryId}` | 单条目详情 | 登录 |
| PUT | `/api/journal/entries/{entryId}` | 更新条目 | 登录 |
| DELETE | `/api/journal/entries/{entryId}` | 删条目 → 204 | 登录 |
| GET | `/api/journal/timeline?from=&to=` | 三路合流时间线 | 登录 |

### 7.6 收益分析 `/api/analytics`（细节见 [14-收益分析域.md](14-收益分析域.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/analytics/overview` | 总资产/TWR 累计与年化/IRR/三基准同期与超额（无数据 204） | 登录 |
| GET | `/api/analytics/nav` | 组合日序列 + 三基准收盘序列（归一化留前端；无数据 204） | 登录 |
| GET | `/api/analytics/annual` | 年度 × 组合/基准 TWR 与超额（恒返回数组） | 登录 |
| GET | `/api/analytics/trade-stats` | 胜率/盈亏比/平均持有天数等九指标（无买卖流水 204） | 登录 |

### 7.7 MCP 配置 `/api/mcp`（细节见 [05-MCP数据源集成.md](05-MCP数据源集成.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/mcp/providers` | 内置 provider 目录（只读） | 登录 |
| GET | `/api/mcp/configs` | 我的配置列表 | 登录 |
| PUT | `/api/mcp/configs/{providerId}` | 保存个人配置（启用/工具级开关） | 登录 |
| DELETE | `/api/mcp/configs/{providerId}` | 删个人配置 → 204 | 登录 |
| POST | `/api/mcp/providers/test` | 连接测试（带工具清单与耗时） | 登录 |
| GET | `/api/mcp/configs/{providerId}/tools` | live 工具清单 | 登录 |

### 7.8 Skill 配置 `/api/skills`（细节见 [06-Skill系统.md](06-Skill系统.md)）

| 方法 | 路径 | 一句话 | 权限 |
|---|---|---|---|
| GET | `/api/skills` | 内置目录 + 我的启用态 | 登录 |
| PUT | `/api/skills/config` | 全量保存启用集 | 登录 |
