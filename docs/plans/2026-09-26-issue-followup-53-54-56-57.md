# Issue 跟进四连（#53/#54/#56/#57）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 PR #52/#55 终审 triage 立票的四个遗留 issue：#53 腾讯解析器语义非法时间戳穿透降级链、#54 腾讯主源切换收尾文案批处理、#56 ETF 导出 CSV 口径注 + TE>30% 遮蔽、#57 smoke 固定 threadId 卡死。

**Architecture:** 全部为小改动：#53/#56-后端走标准 TDD（解析器一行 try/catch、CSV 头部+注释行）；#56-前端在 FundResultsTable 单组件加阈值遮蔽（参与计算不展示假精度）+ vitest/e2e 兜底；#54 纯测试文案/断言/注释零行为变化；#57 smoke 脚本 threadId 按次加时间戳后缀。一个分支顺序提交，一个 PR 关闭四票。

**Tech Stack:** Java 21 + Spring Boot（Gradle test/bdd source set）、Next.js + vitest + Playwright、bash。

**Spec:** GitHub issues #53/#54/#56/#57（正文即验收标准）；本计划代码样例已逐一对照 main（4824f48）真实文件核验。

## Global Constraints

- 分支：`fix/issue-followup-53-54-56-57`，自 main=4824f48 切出；每任务一提交，中文 conventional commits；PR 描述结尾加 `🤖 Generated with [Claude Code](https://claude.com/claude-code)`，提交尾行 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。
- 后端定向测试：`cd backend && ./gradlew test --tests "<FQCN>"`；BDD 全量：`cd backend && ./gradlew bdd`。
- 前端定向测试：`cd frontend && pnpm vitest run <file>`（**禁用** `pnpm test --`，过滤失效会跑全套）。
- e2e：`cd frontend && npx playwright test e2e/screener.spec.ts`；本地复用已跑服务前先 `rm -rf frontend/.next`（陈旧构建盲区），且 Task 3 改后端后须重启本地 :8080 后端再跑 e2e（webServer `reuseExistingServer` 会复用旧进程）。
- #54 五项全部零生产行为变化；#53 仅改异常路径行为（畸形→空串口径统一，即修复目标）。
- 四票共同验收：后端 `./gradlew test bdd` 全绿；前端 `pnpm vitest run` 全绿；`bash -n scripts/smoke.sh` 通过。

---

### Task 1: #53 formatTencentTime 语义非法时间戳吞掉（不穿透降级链）

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/domain/market/MarketDataParser.java:372-377`
- Test: `backend/src/test/java/com/portfolio/invest/domain/market/MarketDataParserEdgeCasesTest.java:178-185`

**Interfaces:**
- Consumes: 既有测试构造器 `tencentQuoteWith(UnaryOperator<String[]>)`（EdgeCasesTest:143-151，f[30] 为时间戳位）。
- Produces: `formatTencentTime` 对 14 位语义非法输入返回 `""`（签名不变）；后续任务无依赖。

- [ ] **Step 1: 写失败测试（扩既有用例）**

`MarketDataParserEdgeCasesTest.java:178-185` 原样：

```java
    @DisplayName("parseTencentQuote时间戳空或畸形返回空串")
    @Test
    void givenBlankOrMalformedTime_whenParseTencentQuote_thenTimeEmpty() {
        assertThat(MarketDataParser.parseTencentQuote(
                tencentQuoteWith(f -> { f[30] = ""; return f; }), "600519").time()).isEmpty();
        assertThat(MarketDataParser.parseTencentQuote(
                tencentQuoteWith(f -> { f[30] = "abc"; return f; }), "600519").time()).isEmpty();
    }
```

改为（DisplayName 措辞扩 + 第三断言）：

```java
    @DisplayName("parseTencentQuote时间戳空、畸形或语义非法返回空串")
    @Test
    void givenBlankOrMalformedTime_whenParseTencentQuote_thenTimeEmpty() {
        assertThat(MarketDataParser.parseTencentQuote(
                tencentQuoteWith(f -> { f[30] = ""; return f; }), "600519").time()).isEmpty();
        assertThat(MarketDataParser.parseTencentQuote(
                tencentQuoteWith(f -> { f[30] = "abc"; return f; }), "600519").time()).isEmpty();
        // 14 位但语义非法（月 99）：LocalDateTime.parse 抛 DateTimeParseException，
        // 须与畸形同口径返回空串，否则穿透 withFallback（只捕 MarketDataException）直达 500（issue #53）
        assertThat(MarketDataParser.parseTencentQuote(
                tencentQuoteWith(f -> { f[30] = "99999999999999"; return f; }), "600519").time()).isEmpty();
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.domain.market.MarketDataParserEdgeCasesTest"`
Expected: FAIL——`givenBlankOrMalformedTime...` 抛 `DateTimeParseException`（解析 "9999-99-99" 月非法）。

- [ ] **Step 3: 最小实现**

`MarketDataParser.java:372-377` 原样：

```java
    private static String formatTencentTime(String s) {
        if (s == null || !s.matches("\\d{14}")) {
            return "";
        }
        return LocalDateTime.parse(s, DateTimeFormatter.ofPattern("yyyyMMddHHmmss")).format(TIME_FMT);
    }
```

改为：

```java
    private static String formatTencentTime(String s) {
        if (s == null || !s.matches("\\d{14}")) {
            return "";
        }
        try {
            return LocalDateTime.parse(s, DateTimeFormatter.ofPattern("yyyyMMddHHmmss")).format(TIME_FMT);
        } catch (DateTimeParseException e) {
            return ""; // 14 位但语义非法（如月 99）：与畸形返回空串同口径，不穿透降级链（issue #53）
        }
    }
```

并在文件头 import 区（现有 `java.time.format.DateTimeFormatter` 之后）加：

```java
import java.time.format.DateTimeParseException;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.domain.market.MarketDataParserEdgeCasesTest" --tests "com.portfolio.invest.domain.market.MarketDataParserTest"`
Expected: PASS 全绿（既有解析器用例不回归）。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/domain/market/MarketDataParser.java backend/src/test/java/com/portfolio/invest/domain/market/MarketDataParserEdgeCasesTest.java
git commit -m "fix(market): formatTencentTime 语义非法时间戳吞掉返回空串，不再穿透降级链（issue #53）

14 位但月/日非法（如 99999999999999）过 \\d{14} 正则后 LocalDateTime.parse 抛
DateTimeParseException，withFallback 只捕 MarketDataException，异常直达 500 绕过
东财/新浪兜底。与「畸形返回空串」口径统一。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: #54 腾讯主源切换收尾——五项文案/断言/注释批处理（零行为变化）

**Files:**
- Modify: `backend/src/bdd/java/com/portfolio/invest/bdd/steps/MarketSteps.java:54,83`
- Modify: `backend/src/test/java/com/portfolio/invest/application/market/OrchestratingMarketDataServiceTest.java:466-483,177-184`
- Modify: `backend/src/bdd/resources/features/market_fallback.feature:6`
- Modify: `scripts/smoke.sh:36`（`for pos in 39 46` 上方）

**Interfaces:**
- Consumes: 无（纯文案/断言/注释）。
- Produces: 无（后续任务无依赖）。BDD 场景标题改名无代码引用，安全。

- [ ] **Step 1: MarketSteps 陈旧口径（issue 清单 1）**

`MarketSteps.java:54` 异常文案（东财已是兜底，非主源）：

```java
                new MarketDataException(MarketDataErrorCode.UPSTREAM_UNAVAILABLE, "主源不可用（模拟故障）"));
```

改为：

```java
                new MarketDataException(MarketDataErrorCode.UPSTREAM_UNAVAILABLE, "东财兜底不可用（模拟故障）"));
```

`MarketSteps.java:83` 断言注释：

```java
        // 主源被请求过一次（失败后降级），备源被请求过一次（真正提供了数据）
```

改为：

```java
        // 兜底东财被请求过一次（主源腾讯失败后降级到东财），末级备源新浪被请求过一次（真正提供了数据）
```

- [ ] **Step 2: OrchestratingMarketDataServiceTest 两处（issue 清单 2、4）**

overview 两条用例方法名仍含陈旧 "Primary"（现状 :466-483）。将：

```java
    @DisplayName("overview降级东财主源成功")
    @Test
    void givenPrimaryOverview_whenOverview_thenReturnIndices() throws IOException {
```

改为：

```java
    @DisplayName("overview兜底东财成功")
    @Test
    void givenEastmoneyOverviewFallback_whenOverview_thenReturnIndices() throws IOException {
```

将：

```java
    @DisplayName("overview降级东财主源失败降级新浪")
    @Test
    void givenPrimaryOverviewFails_whenOverview_thenFallbackToSina() throws IOException {
```

改为：

```java
    @DisplayName("overview兜底东财失败降级新浪")
    @Test
    void givenEastmoneyOverviewFallbackFails_whenOverview_thenFallbackToSina() throws IOException {
```

kline 降级用例断言加强（现状 :177-184，verify 已锁路由、内容断言从 isNotEmpty 加强；fixture 同解析器用例：3 柱、首柱 2026-08-14）：

```java
    @DisplayName("kline腾讯失败降级东财")
    @Test
    void givenTencentKlineFails_whenKline_thenFallbackToEastmoney() throws IOException {
        when(source.kline("1.600519", 101, 120)).thenReturn(fixture("eastmoney-kline.json"));
        List<KlineBar> bars = service.kline("600519", "day", 120);
        assertThat(bars).isNotEmpty();
        verify(source).kline("1.600519", 101, 120);
    }
```

改为：

```java
    @DisplayName("kline腾讯失败降级东财")
    @Test
    void givenTencentKlineFails_whenKline_thenFallbackToEastmoney() throws IOException {
        when(source.kline("1.600519", 101, 120)).thenReturn(fixture("eastmoney-kline.json"));
        List<KlineBar> bars = service.kline("600519", "day", 120);
        // verify 已锁路由（东财兜底），内容断言加强：3 柱 + 首柱日期（与解析器用例同 fixture）
        assertThat(bars).hasSize(3);
        assertThat(bars.get(0).date()).isEqualTo("2026-08-14");
        verify(source).kline("1.600519", 101, 120);
    }
```

- [ ] **Step 3: feature 场景标题更准（issue 清单 3）**

`market_fallback.feature:6` 实际演练三级链（腾讯→东财→新浪），标题只说两级。将：

```gherkin
  场景: 主源故障时自动切换到备源返回行情
```

改为：

```gherkin
  场景: 三级行情链逐级降级到末级备源返回行情
```

- [ ] **Step 4: smoke.sh §2 补前提注释（issue 清单 5）**

`scripts/smoke.sh:36` 的 `for pos in 39 46; do` 上方插一行注释：

```bash
# 估值位置[39/46]（PE/PB）非空断言以 600519 恒有估值为前提：改探其他标的（指数/场内基金）时
# PE/PB 可合法为空，需放宽为允许空串（PR #52 终审 deferred #7，issue #54 留档）
for pos in 39 46; do
```

- [ ] **Step 5: 验证**

Run:
```bash
cd backend && ./gradlew test --tests "com.portfolio.invest.application.market.OrchestratingMarketDataServiceTest" && ./gradlew bdd
bash -n scripts/smoke.sh
```
Expected: 定向测试绿；BDD 15/15；bash -n 无输出（语法通过）。无任何生产代码行为变化。

- [ ] **Step 6: Commit**

```bash
git add backend/src/bdd/java/com/portfolio/invest/bdd/steps/MarketSteps.java backend/src/test/java/com/portfolio/invest/application/market/OrchestratingMarketDataServiceTest.java backend/src/bdd/resources/features/market_fallback.feature scripts/smoke.sh
git commit -m "test(market): 腾讯主源切换收尾——陈旧口径文案/方法名/断言加强/smoke 前提注释（issue #54）

五项均为文案/注释/断言，零生产行为变化：东财已是兜底非主源；
overview 两用例方法名去 Primary；kline 降级断言 hasSize(3)+首日期；
feature 场景标题对齐三级链；smoke §2 标注 600519 估值前提。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: #56 后端——ETF 导出 CSV 补 TE 收盘价口径注

**Files:**
- Modify: `backend/src/main/java/com/portfolio/invest/web/ScreeningCsv.java:14,44-46`
- Test: `backend/src/test/java/com/portfolio/invest/web/ScreeningCsvTest.java:54-84`
- Test: `backend/src/test/java/com/portfolio/invest/web/ScreeningControllerTest.java:99-113`

**Interfaces:**
- Consumes: 无。
- Produces: CSV 文本含两个精确片段（Task 4 e2e 内容断言消费，必须逐字一致）：
  - 表头末列：`跟踪误差(%，收盘价口径)`
  - 表头后注释行：`# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比`（无 ASCII 逗号，单单元格不加引号）
  - 个股 CSV（`toCsv`）不动。

- [ ] **Step 1: 写失败测试（ScreeningCsvTest 三处 funds 用例改期望）**

`ScreeningCsvTest.java:54-67` 主用例，将表头断言：

```java
        assertThat(text).contains("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%)");
```

改为：

```java
        assertThat(text).contains("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%，收盘价口径)");
        assertThat(text).contains("# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比");
```

`ScreeningCsvTest.java:78-84` 空结果用例，将：

```java
    @DisplayName("基金空结果仅表头行")
    @Test
    void givenNoFundRows_whenToFundCsv_thenHeaderOnly() {
        String text = new String(ScreeningCsv.toFundCsv(List.of()), StandardCharsets.UTF_8);
        assertThat(text.substring(1).stripTrailing())
                .isEqualTo("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%)");
    }
```

改为：

```java
    @DisplayName("基金空结果仅表头与口径注行")
    @Test
    void givenNoFundRows_whenToFundCsv_thenHeaderAndNoteOnly() {
        String text = new String(ScreeningCsv.toFundCsv(List.of()), StandardCharsets.UTF_8);
        assertThat(text.substring(1).stripTrailing())
                .isEqualTo("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%，收盘价口径)"
                        + "\r\n# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比");
    }
```

- [ ] **Step 2: 写失败测试（ScreeningControllerTest 导出断言）**

`ScreeningControllerTest.java:111` 将：

```java
        assertThat(text).contains("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%)");
```

改为：

```java
        assertThat(text).contains("代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%，收盘价口径)");
        assertThat(text).contains("# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比");
```

（其余两行数据断言 `510300,...,3.18` 与 `159915,...,` 不受注释行影响，保留。）

- [ ] **Step 3: 跑测试确认失败**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.web.ScreeningCsvTest" --tests "com.portfolio.invest.web.ScreeningControllerTest"`
Expected: FAIL——旧表头/无注释行不匹配新断言。

- [ ] **Step 4: 最小实现（ScreeningCsv）**

`ScreeningCsv.java:14` 将：

```java
    private static final String FUND_HEADER = "代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%)";
```

改为：

```java
    private static final String FUND_HEADER = "代码,名称,费率(%),规模(亿元),跟踪指数,类别,跟踪误差(%，收盘价口径)";
    /** TE 口径注（issue #56）：收盘价自算含分红/折溢价噪声；刻意无 ASCII 逗号，保单单元格免转义。 */
    private static final String FUND_TE_NOTE = "# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比";
```

`toFundCsv`（:44-46）将：

```java
        StringBuilder sb = new StringBuilder();
        sb.append(FUND_HEADER).append("\r\n");
```

改为：

```java
        StringBuilder sb = new StringBuilder();
        sb.append(FUND_HEADER).append("\r\n");
        sb.append(FUND_TE_NOTE).append("\r\n");
```

并在类注释（:10）口径说明补一句：「含 TE 收盘价口径注行」——将：

```java
/** 筛选结果 CSV 序列化（web 层表现职责）：UTF-8 + BOM、CRLF 行尾、RFC 4180 转义、总市值换算亿元。 */
```

改为：

```java
/** 筛选结果 CSV 序列化（web 层表现职责）：UTF-8 + BOM、CRLF 行尾、RFC 4180 转义、总市值换算亿元；
 * 基金 CSV 表头后带 TE 收盘价口径注行（issue #56）。 */
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && ./gradlew test --tests "com.portfolio.invest.web.ScreeningCsvTest" --tests "com.portfolio.invest.web.ScreeningControllerTest"`
Expected: PASS 全绿。

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/portfolio/invest/web/ScreeningCsv.java backend/src/test/java/com/portfolio/invest/web/ScreeningCsvTest.java backend/src/test/java/com/portfolio/invest/web/ScreeningControllerTest.java
git commit -m "feat(screening): ETF 导出 CSV 补 TE 收盘价口径注（issue #56）

全量分红型宽基 TE 收盘价口径读数系统性与官方 NAV 口径差 2-4 倍；
表头末列标注口径 + 表头后注释行（无逗号单单元格），页面三处口径标注由此对齐导出物。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: #56 前端——TE>30% 行遮蔽「—（疑似拆分/异常）」+ e2e 兜底

**Files:**
- Modify: `frontend/components/screening/FundResultsTable.tsx:25-26,64`
- Test: `frontend/tests/FundResultsTable.test.tsx`（新增 1 用例）
- Modify: `frontend/e2e/screener.spec.ts:108`（放宽正则）、新增遮蔽数据门控用例、:62-74 CSV 下载用例补内容断言

**Interfaces:**
- Consumes: Task 3 产出的两个 CSV 片段（e2e 内容断言逐字引用）；`TRACKING_ERROR_NOTE`（FundScreeningForm.tsx:10-11）。
- Produces: 遮蔽文案常量 `—（疑似拆分/异常）`（e2e 断言消费）；阈值 0.30（小数口径，与后端 trackingError1y 一致）。

- [ ] **Step 1: 写失败测试（vitest）**

`frontend/tests/FundResultsTable.test.tsx` 在「TE 0.0318 展示为 3.18」用例后新增：

```tsx
  it("TE>30% 遮蔽为「—（疑似拆分/异常）」不展示假精度", () => {
    renderTable({
      results: [{ ...ROW, fundCode: "159970", fundName: "半导体ETF", trackingError1y: 0.4512 }],
    });
    expect(screen.getByText("—（疑似拆分/异常）")).toBeTruthy();
    expect(screen.queryByText("45.12")).toBeNull();
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run tests/FundResultsTable.test.tsx`
Expected: FAIL——找不到「—（疑似拆分/异常）」，实际渲染 45.12。

- [ ] **Step 3: 最小实现（FundResultsTable）**

`FundResultsTable.tsx:25-26` 将：

```tsx
  // TE 后端为小数（0.0318 = 3.18%），展示 ×100 两位小数；null=未知「—」
  const fmtTe = (v: number | null) => (v == null ? "—" : (v * 100).toFixed(2));
```

改为：

```tsx
  // TE 后端为小数（0.0318 = 3.18%），展示 ×100 两位小数；null=未知「—」
  // TE>30% 疑似份额拆分污染（issue #56）：行仍参与筛选/排序计算，但不展示假精度
  const TE_SUSPECT_THRESHOLD = 0.3;
  const TE_SUSPECT_MASK = "—（疑似拆分/异常）";
  const isSuspectTe = (v: number | null) => v != null && v > TE_SUSPECT_THRESHOLD;
  const fmtTe = (v: number | null) => (isSuspectTe(v) ? TE_SUSPECT_MASK : v == null ? "—" : (v * 100).toFixed(2));
```

`:64` TE 单元格将：

```tsx
              <td className="text-right">{fmtTe(r.trackingError1y)}</td>
```

改为：

```tsx
              <td className="text-right" title={isSuspectTe(r.trackingError1y)
                ? "跟踪误差>30%：疑似份额拆分或数据异常已遮蔽（该行仍参与筛选计算）" : undefined}>
                {fmtTe(r.trackingError1y)}
              </td>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run tests/FundResultsTable.test.tsx`
Expected: PASS 全绿（含既有 null→3×「—」、3.18 等用例不回归）。

- [ ] **Step 5: e2e 更新（放宽既有正则 + 新增遮蔽门控用例 + CSV 内容断言）**

`screener.spec.ts` 顶部 import 区加：

```ts
import fs from "node:fs";
```

:108 行 TE 列断言（首行可能是遮蔽行）将：

```ts
    await expect(rows.first().locator("td").last()).toHaveText(/^(\d+\.\d{2}|—)$/);
```

改为：

```ts
    await expect(rows.first().locator("td").last()).toHaveText(/^(\d+\.\d{2}|—|—（疑似拆分\/异常）)$/);
```

`:62-74` CSV 下载用例，在 `expect(d.suggestedFilename())...` 之后追加内容断言（片段与 Task 3 逐字一致；空库下仅表头+注释行也成立）：

```ts
    // 口径注（issue #56）：表头末列口径 + 注释行（CI 空库仅表头也含两片段）
    const path = await d.path();
    const csvText = path ? fs.readFileSync(path, "utf-8").replace(/^\uFEFF/, "") : "";
    expect(csvText).toContain("跟踪误差(%，收盘价口径)");
    expect(csvText).toContain("# 注：跟踪误差为收盘价口径（含分红/折溢价噪声）与官方净值口径不可直接对比");
```

文件末尾 describe `/screener 基金 tab（MS-14 P3 数据链路）` 内新增（照 `fundScreeningHas510300` 门控范式）：

```ts
/** TE 遮蔽门控：探测降序首行是否 TE>0.3（CI 空库无数据则跳过；接口非 200 不门控，让回归可见失败）。 */
async function fundHasSuspectTe(request: APIRequestContext): Promise<boolean> {
  const res = await request.get("/api/screening/funds?trackingErrorMax=5&sortBy=tracking_error_1y&sortDirection=DESC&limit=1");
  if (!res.ok()) return true;
  const body: unknown = await res.json();
  const first = Array.isArray(body) ? (body[0] as { trackingError1y?: number | null }) : null;
  return !!first && typeof first.trackingError1y === "number" && first.trackingError1y > 0.3;
}

test("TE>30% 行遮蔽为疑似拆分/异常，不展示假精度数值", async ({ page, request }) => {
  test.skip(!(await fundHasSuspectTe(request)), "库中无 TE>30% 基金（CI 空库），跳过遮蔽用例");
  await page.goto("/screener");
  await page.getByTestId("tab-fund").click();
  // TE < 5（=500%）放行高 TE 行；提交后默认 TE 升序，点表头翻降序 → 首行即最高 TE
  await page.getByLabel("跟踪误差 <（小数，0.05=5%）").fill("5");
  await page.locator("form button[type=\"submit\"]").click();
  await expect(page.getByText(/筛选结果（\d+/)).toBeVisible({ timeout: 15_000 });
  await page.getByRole("columnheader", { name: /跟踪误差/ }).click();
  await expect(page.locator("table tbody tr").first().locator("td").last())
    .toHaveText("—（疑似拆分/异常）");
});
```

- [ ] **Step 6: 真浏览器 e2e 兜底（mock 盲区教训）**

前置：Task 3 已提交；本地 :8080 后端**重启**（webServer 复用旧进程则口径注断言必红）；清陈旧构建。

```bash
rm -rf frontend/.next
cd frontend && npx playwright test e2e/screener.spec.ts
```
Expected: 全绿（遮蔽用例本地有数据执行；CI 空库自动 skip）。若遮蔽用例 skip 且本地应跑（库有 TE>0.3 基金），检查门控 URL 与后端参数拼写。

- [ ] **Step 7: Commit**

```bash
git add frontend/components/screening/FundResultsTable.tsx frontend/tests/FundResultsTable.test.tsx frontend/e2e/screener.spec.ts
git commit -m "feat(screening): TE>30% 行遮蔽为疑似拆分/异常，CSV 口径注 e2e 断言（issue #56）

74/1227 只含份额拆分污染（~70 只 TE>30% 垃圾值）；方向保守（被 trackingErrorMax
排除不误入低误差）但页面曾显示有效数字假精度。行仍参与计算，展示遮蔽+悬浮说明；
e2e 照 510300 门控范式加数据门控用例，CSV 下载断言口径注两片段。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 5: #57 smoke threadId 按次唯一（残留 HITL 中断不再锁死共享 thread）

**Files:**
- Modify: `scripts/smoke.sh:56-84`（§5 内两处 threadId + RUN_TAG 定义）

**Interfaces:**
- Consumes: 无。
- Produces: 无（脚本自含）。残留清理见 Task 6 可选项。

- [ ] **Step 1: 改脚本**

§5 `else` 分支开头（`else` 之后、`COOKIE_JAR=$(mktemp)` 之前）插入：

```bash
  # 按次唯一 threadId（issue #57）：固定 threadId 会撞上残留未应答 HITL 中断，
  # 使该 thread 永久 RUN_ERROR AGUI_INTERRUPT_CONTRACT_ERROR 且重启不自愈（须删 .agentscope/state）
  RUN_TAG=$(date +%s)
```

基础对话请求（:66）将：

```bash
    -d "{\"threadId\":\"smoke\",\"runId\":\"smoke-1\",\"messages\":[{\"id\":\"m1\",\"role\":\"user\",\"content\":\"用一句话介绍你自己\"}],\"state\":{},\"tools\":[]}")
```

改为：

```bash
    -d "{\"threadId\":\"smoke-$RUN_TAG\",\"runId\":\"smoke-1\",\"messages\":[{\"id\":\"m1\",\"role\":\"user\",\"content\":\"用一句话介绍你自己\"}],\"state\":{},\"tools\":[]}")
```

`tool_smoke` 内（:76）将：

```bash
      -d "{\"threadId\":\"smoke-ms12\",\"runId\":\"$run_id\",\"messages\":[{\"id\":\"m-$run_id\",\"role\":\"user\",\"content\":\"$question\"}],\"state\":{},\"tools\":[]}")
```

改为：

```bash
      -d "{\"threadId\":\"$run_id-$RUN_TAG\",\"runId\":\"$run_id\",\"messages\":[{\"id\":\"m-$run_id\",\"role\":\"user\",\"content\":\"$question\"}],\"state\":{},\"tools\":[]}")
```

（threadId 变为 `smoke-ms12-1-<epoch>` 等，跨次运行彻底不共享；runId 保持原样。）

- [ ] **Step 2: 语法验证**

Run: `bash -n scripts/smoke.sh`
Expected: 无输出（通过）。可选实证：设 DEEPSEEK_API_KEY/ADMIN_* 跑两次 `bash scripts/smoke.sh`，确认 §5 绿且 `.agentscope/state/1/` 不再新增同名 smoke 目录。

- [ ] **Step 3: Commit**

```bash
git add scripts/smoke.sh
git commit -m "fix(smoke): threadId 按次加时间戳唯一，残留 HITL 中断不再锁死共享 thread（issue #57）

固定共享 threadId + AgentScope 持久化 state + 未应答中断三者叠加：新 run 打同一
thread 即 RUN_ERROR AGUI_INTERRUPT_CONTRACT_ERROR，重启不自愈须人工删 state 目录
（本机已残留 smoke/smoke-ms12/b/c 等 7 个目录）。改按次唯一彻底避免共享。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 6: 全量回归、PR、issue 回写

**Files:**
- Create: GitHub PR（分支 `fix/issue-followup-53-54-56-57` → `main`）

**Interfaces:**
- Consumes: Task 1-5 全部提交。
- Produces: PR 合并后自动关闭 #53/#54/#56/#57。

- [ ] **Step 1: 全量回归**

```bash
cd backend && ./gradlew test bdd
cd frontend && pnpm vitest run
bash -n scripts/smoke.sh
```
Expected: 后端单测+BDD 全绿；前端 vitest 全绿；smoke 语法通过。

- [ ] **Step 2: 推分支开 PR**

```bash
git push -u origin fix/issue-followup-53-54-56-57
gh pr create --base main --title "fix: 终审跟进四连——#53 解析器时间戳 / #54 收尾文案 / #56 ETF TE 口径+遮蔽 / #57 smoke threadId" --body "$(cat <<'EOF'
## 概要

PR #52/#55 终审 triage 立票的四个遗留 issue 一并修复：

- **#53** `formatTencentTime` 14 位语义非法时间戳吞掉返回空串（与畸形同口径），不再以 DateTimeParseException 穿透降级链直达 500；边界用例扩展。
- **#54** 腾讯主源切换收尾五项：东财「主源」陈旧文案、overview 用例方法名去 Primary、feature 场景标题对齐三级链、kline 降级断言加强 hasSize(3)+首日期、smoke §2 估值前提注释。零生产行为变化。
- **#56** ETF 导出 CSV 表头末列口径标注 + 注释行（收盘价口径，与官方净值不可直接对比）；前端 TE>30% 行遮蔽「—（疑似拆分/异常）」（参与计算不展示假精度）；vitest 新用例 + e2e 数据门控用例 + CSV 内容断言。
- **#57** smoke §5 threadId 按次加时间戳唯一，残留 HITL 中断不再锁死共享 thread；runId 不变。

## 验收

- [ ] `./gradlew test bdd` 全绿（BDD 15/15）
- [ ] `pnpm vitest run` 全绿
- [ ] `npx playwright test e2e/screener.spec.ts` 全绿（空库自动 skip 数据门控用例）
- [ ] `bash -n scripts/smoke.sh` 通过
- [ ] CI 绿

fixes #53 fixes #54 fixes #56 fixes #57

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: 残留 state 清理（可选项，破坏性，先向用户确认）**

`.agentscope/` 已 gitignore 属本地产物；修复后不再累积。如需清理历史残留（**先确认**）：

```bash
rm -rf backend/.agentscope/state/1/smoke backend/.agentscope/state/1/smoke-* backend/.agentscope/state/1/t-smoke
```

（列目录核对后再删：`ls backend/.agentscope/state/1/ | grep smoke`，现存 smoke/smoke-mcp/smoke-ms12/smoke-ms12b/smoke-ms12c/smoke-readonly/t-smoke。）

- [ ] **Step 4: 合并后回写**

CI 绿 → 合并 → 在四票各留一行评论指向 PR（`fixes #N` 自动关闭，评论补上下文即可）→ 通知用户。

---

## Self-Review 结论

- **覆盖**：#53（Task 1）、#54 五项（Task 2 Step 1-4 逐项）、#56 两项（Task 3/4）、#57（Task 5）——issue 验收条目全部有对应步骤；#53「58 条解析器用例不回归」落在 Task 1 Step 4 双类全跑。
- **占位符**：无 TBD/「适当处理」类步骤；所有代码块为对照 main 真实文件的最终形态。
- **类型一致**：CSV 两片段字符串 Task 3（Produces）与 Task 4 Step 5（Consumes）逐字一致；遮蔽文案常量与 e2e `toHaveText` 逐字一致；TE 阈值前后端同为小数 0.30 口径。
- **终审修正（2026-09-26）**：表头口径注片段原定半角逗号致 CSV 表头 8 列 vs 数据行 7 列，已全角化并 5 处字面量联动（终审 Important）。
