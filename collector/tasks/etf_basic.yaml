task_code: etf_basic
task_name: ETF目录费率规模
# 新浪目录枚举 + 天天基金移动端 Detail 逐只 enrich（1685 只 × 0.3s ≈ 9 分钟，周更）；
# 单只 enrich 失败该行字段 null 不阻断整批，全量异常由 retry/validator 兜底。
source_ids:
  - { source_id: etf_basic, type: plugin, class: etf_basic }
converter: field_mapping_etf_basic
calc: null
validator:
  - { check: min_rows, value: 1000, level: hard }
target_table: etf_basic
schedule: { type: cron, cron: "30 9 * * 1" }
enabled: true
trading_day_gated: false
retry_max: 3
retry_backoff: exponential
