import type { PrincipleRuleView } from "@/lib/types";

export interface RulePanelProps {
  rules: PrincipleRuleView[];
  onChanged: () => void;
}

/** Task 4 占位组件：让 WikiBoard 编译通过，Task 5/6 实现真体。 */
export default function RulePanel(_props: RulePanelProps) {
  return null;
}
